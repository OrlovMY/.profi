# PQ-01 — разбор ревизии свода 2026-08-24 (`ревизия-свода-2026-08-24.md`)

PQ-01 [2026-08-24 23:05]: предмет — первая ревизия свода от PE-01: 10 правок полной формой, 3 новых файла, 5 мелочей, N=4, M=0, строка ревизии для README, переоценка Д1. Разбор написан до какой-либо совместной редакции; в файлы свода не внесено ничего.

**Счёт вердиктов: отклонено 0 · принято 12 · принято с правкой 6.**
Принято: П1, П2, П5, П8, П9, П10; Н1, Н3; N=4; M=0; строка ревизии; мелочи раздела 7 (пакетом).
Принято с правкой: П3, П4, П6, П7; Н2; переоценка Д1.
«Не сошлись» — пусто (см. раздел 9).

---

## 1. Правки П1–П10

### П1 — принять

Проверено моими командами: `roles/big7/pe-01.md:3` действительно «Файл датирован 2026-08-19», а `git log -- roles/big7/pe-01.md` даёт последним 68fef96 2026-08-24 — шапка нарушает собственное третье предложение. `pq-01.md` не менялся с 49610ab 2026-08-19 (моя команда) — его не трогать, верно. Границу PE-01 («строку о снятии запрета Fable сам не открывал») я закрыл: `scenarios/big7-consultation.md:34` открыт мной — «24.08.2026 owner снял запрет» там стоит.

### П2 — принять

Основание — прямое указание владельца 24.08 существует: `knowledge/README.md:26` («Проектное всегда должно оставаться в проекте», помечено «прямое указание владельца, 24.08.2026») и `README.md:55` — открыты мной. Образец того же класса (`git-dr-mirror-sync.md:30` — частность в памяти, в своде датированный случай) — открыт мной, границу PE-01 закрыл. Смысл мандата 11.08 сохранён датированным случаем; защитное условие («сначала запись мандата в живой памяти MDMy, потом правка») — в самом тексте правки, без него указание владельца потеряло бы последний дом. Условие обязательно к исполнению, не к обсуждению.

### П3 — принять с правкой

«Было» сверено построчно с `README.md` (66 строк, прочитан целиком): заголовок без определённой «прошлой версии», обратная хронология, недатированный SEC-02, дубли — всё подтверждено. Датированные поводы (цитаты владельца) в «Стало» сохранены дословно. `grep -rn "Что нового"` по своду → только README.md:5 (моя команда). Порядок «перед удалением» по пяти строкам-механизмам соблюдён.

**Правка (обязательная):** в новой строке «Ключевых принципов» потеряно слово «обязательная» из удаляемой строки 15 — это сокращение обязательного. Стало:

> - **Resource tracking** — обязательная фиксация peak CPU/RAM/disk/bandwidth в финальных отчётах агентов; перенос в журнал — частью приёмки отчёта. → `playbooks/resource_tracking.md`

Две неточности не-вердиктного веса — в раздел 8 (мелкие).

### П4 — принять с правкой

Обе редакции «Было» сверены с файлами дословно (prod_escalation.md:47–52, INDEX «Минимальный кворум») — расхождение по SEC-02 реально: INDEX делает его частью кворума, плейбук — «дополнительно по необходимости». Выбор более строгой редакции правомерен: ослаблять PE-01 запрещено, усиление безопасности отклонению не подлежит. QA-01/AR-01 перенесены — случаи не теряются. `grep "Состав Big 7 для эскалации"` по своду → ровно prod_escalation.md:47 и :71 (моя команда).

**Правка (обязательная): у правки есть третий вход, и он остаётся.** PE-01 считал входы по имени заголовка, а rule-expiry п. 5 велит считать по формулировке: `grep -rn "PM-01 + DO-01 + SEC-01" playbooks/` и после обеих замен П4 даёт prod_escalation.md:71 — скобка в этой строке сама перечисляет кворум и остаётся независимо правимой редакцией (тот самый дубль, который П4 лечит). Стало (та же строка :71, скобка целиком):

> Big-7 (кворум — `roles/big7/INDEX.md`, «Минимальный кворум») отвечает: **allow** …

— дальше строка без изменений. Артефакт хода: `grep -rn "PM-01 + DO-01 + SEC-01" --include="*.md" playbooks/ scenarios/ templates/ roles/hr-d.md` → 0; перечень остаётся только в INDEX.

### П5 — принять

«Было» сверено с git-branch-discipline.md:56 дословно. Числа 23/37 и срок 07.09 — `ответ-MDMy-2026-08-24-v2.md`, шаг 6 и раздел 12 (прочитан мной целиком); необъяснённое падение 226→23 не прячется, а выносится на владельца (раздел 9 ревизии) — верно. Величина в тексте — датированный замер-случай при уже стоящих 226/239, не текущее состояние; rule-expiry п. 6 не нарушен. Граница PE-01 (запись `memory/project_branch_cleanup_debt_2026-08-24.md` не проверена существованием) названа честно; адрес подтверждён сообщением коммита 84640ad и строками «История правок» — это доверие к записи, и у меня оно кончается там же.

### П6 — принять с правкой

Факт «не сработало» подтверждён моей командой: `grep -n "Агенты:"` по обоим журналам разбора → PE-01-разбор:746 (цитата правила) и PQ-01-разбор:393 (вердикт о тексте правила), применений — ноль. Диагноз «артефакт назначен месту без даты изменения» верен, и лечение по Н1 — верное направление. Опорный образец «Нет строки — роль не созывалась» существует (big7-consultation.md:70, открыт мной).

**Правка (обязательная): носитель назван неточно, и для сессии без файла правило снова не может упасть.** «Итоговый отчёт» живёт в чате так же часто, как строка учёта, — у него нет даты изменения, и приёмке нечего не принять. У каждой сессии уже есть обязательный частый носитель с датой: запись памяти по итогам сессии (`scenarios/post-incident-memory-update.md`: «После каждой сессии … HR-D обязан зафиксировать уроки в памяти») — ровно требование Н1 п. 1а. Заменить скобку в «Стало» П6:

> …и **предъявляет её в записи сессии** — файле с датой изменения: файл разбора, сохранённый в репозиторий отчёт либо запись памяти по итогам сессии (`scenarios/post-incident-memory-update.md`), которая обязательна после каждой сессии и потому есть у любой из них. Чат местом записи не является.

Хвост про приёмку и покрытие соседней строки «жив, консультаций: N» (не сработало №3) — без изменений: цитируемый в той же записи ответ агента покрывает её, с правкой носителя покрытие становится проверяемым.

### П7 — принять с правкой

Все четыре входа подтверждены моей командой (`grep -rn "project_resource_tracking"` → те же 4 входа + memory_management.md:40, memory_index.md:34, resource_log.md:3 — верно отнесены к не-входам). Разделы «Правило HR-D» и «Что обязательно собрать в финальном отчёте deploy-агента» в resource_tracking.md существуют (файл открыт мной целиком). `grep -ci "ресурс"/"resource"` по заданию и ответу v2 → 0/0/0/0 — моя команда, сошлось. Возврат в работу вместо понижения обоснован: частый обязательный носитель есть.

**Правка (обязательная): дом сузился с «деплой или инцидент» до «отчёт deploy-агента».** Старая редакция ловила инцидент с heavy usage без выката; в новой этот случай остаётся только в триггере 6 сценария post-incident, который отсылает за порядком в дом, где инцидент больше не назван. Это регресс покрытия (моя роль, п. 10: сокращать текст можно, случаи — нельзя). Дописать в «Стало» П7 после первого предложения:

> Инцидент без выката — тот же порядок: запись разбора инцидента с heavy resource usage (`scenarios/post-incident-memory-update.md`, триггер 6) не считается принятой без строки в журнале.

### П8 — принять

Все входы проверены моими командами: `grep -rn "RESUME" scenarios/ playbooks/ templates/` → шесть вхождений (session-start:63, :97; memory_management:75, 84, 86; workflow_loop:9), в templates/ — ноль; каркас memory_index.md открыт — раздела нет. Чистое дополнение каркаса, производитель существует, регресса нет.

### П9 — принять

«Раз в N недель:» — post-incident-memory-update.md:34, открыто мной. Плейсхолдер без значения непроверяем по rule-expiry п. 2 — диагноз точен. Артефакт назван («Уборка памяти: <дата>» в MEMORY.md), календарь сведён к одному — тем же приёмом, что в rule-expiry:40. «Чаще — можно» сохраняет свободу проекта.

### П10 — принять

Дополнение, не замена — старая строка исполнена, не неверна; квалификация честная. Срок 07.09 и вопрос владельцу — ответ-файл, раздел 12 п. 1 (прочитан мной). Границу PE-01 (grep «реконструир» сам не гонял) я закрыл своей командой: по своду — только rule-expiry:40. Без этой записи вторая половина общего календаря молча уехала бы из свода — довод верен.

---

## 2. Новые файлы Н1–Н3

### Н1 `attach-recurring-step-to-mandatory-action.md` — принять

Базовый текст сверен мной построчно с черновиком (`PE-01-разбор-2026-08-24.md:1167`, блок прочитан) — совпадает дословно; врезки 1а/2а/3а и оговорка в «Связано» сверены с разделами 13(а)–(в) и «Ещё одно…» ответ-файла:

- **(а) частый носитель → 1а**: «частота носителя — не реже, чем допустимая давность пропущенного шага», случай «четыре выката за день», исход «носителя нужной частоты нет — правило не применимо, и это признаётся записью (п. 4)» — всё вошло; «признаётся записью» даже сильнее, чем «надо признать» у исполнителя. Не ослаблено.
- **(б) подсадка → 3а**: формула «показан отказывающим на подсадке, и подсадка описана» дословно; все три примера слепых проверок (секреты 1 из 3, счёт строк вместо нулей, `chmod 600`) и вывод «пункт 3 сам оказывается проверкой, которая не может упасть» — вошли. Не ослаблено.
- **(в) непроизносимый обход → 2а**: «отказ без обхода снимут целиком», «обход по привычке равносилен отсутствию», «нельзя применить машинально», «сам называет цену», образец `MDMY_MEMORY_GATE=skip-and-i-know-169-files-were-lost` — вошли. Не ослаблено.
- **Оговорка о гейте старта** — в «Связано», с усилением «равнять их нельзя» против «лучше не равнять» у исполнителя и с атрибуцией. Вошла.

Пункт 4 (граница) согласован с 1а; «Происхождение» называет врезки и их автора. Решение «заводить только вместе» — верное: дополнения адресованы этому файлу. Применение к двум не сработавшим правилам исполнимо: у П7 носитель — приёмка отчёта деплоя (частота = каждый выкат, исполнитель HR-D, права на память есть); у П6 — после моей правки носителя (запись памяти по итогам сессии). Оба носителя — процедурные, не кодовые: до силы «отказ выката кодом 4» они не дотягивают, и это честно покрыто оговоркой самого Н1 о носителях разной силы; сильнее в библиотеке правил сделать нечем. Файла attach-* в standards/ нет (моя команда).

### Н2 `recovered-data-acceptance.md` — принять с правкой

Признаки сверены: (а)–(в) — дословно из `задание-MDMy-2026-08-24-v2.md:15` (открыто мной; «хвост утрачен» сохранён, битая ссылка «как шаг 2.1» верно опущена, сломанный `grep -c $'\x00'` верно не воспроизведён как команда, а разобран в «Проверку предъявить отказу»); (г), (д) — раздел 5 ответа; (е) с originSessionId — раздел 6; порядок «git первым» с 15/20 и четырьмя ложно погибшими — раздел 4; «одинаковый размер ≠ одинаковое содержимое» и копии под суффиксами (`project_backlog_cpe_locate`, `_1`/`_4`) — задание:15 и раздел 7 ответа. Существующего дома нет: мой grep по undelete/восстановлению → 0; `validate-recovery-before-destructive.md` существует (мой `ls`). Проверка делом — раздел 5 ниже: и четыре мёртвых, и семь чужих файлов ловятся, причём (е) — несущий признак для чужих (остальные пять они проходят).

**Правка (обязательная): оговорка о git ослабляет приёмку сверх того, что делал сам исполнитель.** «К файлам, взятым из живого git-репозитория, не применяется: их целостность держит сам git» — git держит тождество байтов с момента фиксации, а не пригодность зафиксированного: мусор, закоммиченный мусором, git хранит верно. Исполнитель 24.08 git-версии фактически принимал по содержимому («с теми же размерами и нормальным содержимым» — раздел 4 ответа), то есть практика строже предложенного текста. Заменить хвост «Применимости»:

> К файлам, взятым из живого git-репозитория проекта, признак (е) считается выполненным самим репозиторием; остальные признаки применяются и к git-версии — git держит тождество байтов с момента фиксации, а не пригодность зафиксированного содержимого (24.08.2026 исполнитель git-версии так и принимал: по размеру и содержимому). См. «Порядок» — git проверяется раньше, чем фонд.

### Н3 `machine-read-names-in-latin.md` — принять

Случай трёх сред сохранён: оболочка (страж), ключи JSON-схем инструментов (дважды — у исполнителя среды 2 и 3 обе схемные, стандарт честно даёт открытый перечень), идентификаторы конфигов — из предложения исполнителя же (раздел 11); «имена в коде» — в духе его общей формулы «имена, которые читает машина», перечень объявлен открытым. Кириллица для человека явно не тронута: «НЕ применяется к текстам для человека … там кириллица законна и этим правилом не трогается» + отдельная оговорка о журнальных именах с решением за владельцем. Дома в своде нет: мой grep латиниц/кириллиц/cyrillic/latin → единственное совпадение — «escaLATINg» в agent_briefing.md:25, PE-01 верно назвал его нерелевантным. Ловушка quotePath **воспроизведена мной**: `git log --name-only` в этом worktree отдаёт кириллические имена octal-последовательностями, фильтр по кириллическому образцу даёт 0 совпадений; с `git -c core.quotePath=false` — 4. Границу PE-01 (safe-wording не открывал) закрыл: файл существует, мой `ls`. Мелкая неточность адреса — раздел 8.

---

## 3. N = 4 — принять

Каждый пункт проверен моими командами: №1 — 4 входа журнала, 84 дня/гибель/нереконструированность (греп 0/0/0/0); №2–3 — «Агенты:» только как цитаты правила (746/393); №4 — кворум продублирован (prod:47–52), 55 вхождений в 13 файлах — мой пересчёт сошёлся (раздел 6). Определение N («артефакт или заявленное состояние отсутствует на 24.08») и поимённый список вместо образца — по rule-expiry п. 5, верно. Решения для №1–3 остаются валидными с моими правками П6/П7.

## 4. M = 0 — принять

Условие снятия проверено моими руками во всех **пяти** заглушках, не в одной: `grep -c "после правки указателей"` → 1 в каждой (treat-owner-as-expert, confirm-understanding-before-acting, no-confirm-reasonable-nondestructive, challenge-suboptimal-owner-decisions, autonomous-self-verify-nondestructive) — граница PE-01 «остальные по линзам» закрыта. Перевод указателей — открытый хвост (PQ-01-разбор-2026-08-24.md, вердикт блока 3 — подтверждено). Архивировать до снятия условия — сломать адреса; вердикт «не архивировать сейчас» верен. Файлов `archive*` нет (мои команды) — заводить при первом реальном архивировании, верно по rule-expiry п. 7.

## 5. Строка ревизии — принять; переоценка Д1 — принять с правкой

**Строка.** Форма rule-expiry:45 воспроизведена дословно (`Ревизия свода: <дата> · проверено файлов: K · не сработало: N · архивировано: M · разбор: <файл>`). K=62 — моей командой, той же, что M в session-start.md:87, и сошлось с датированным числом в тексте Шага 1.5. Условие применения: `ревизия-свода-2026-08-24.md` сейчас не закоммичен — строка README и файл разбора коммитятся одним ходом, иначе README сошлётся на файл, которого в репозитории нет.

**Д1.** Пересчитано моей командой — образец PE-01 запущен дословно: **68 вхождений в 16 файлах**, разбивка по файлам совпала во всех 16, суммы 1+12+55 и 55 по 13 файлам сошлись. Все 12 «исторических» строк открыты мной (README:7,9,10; delegate-search:6,51; delegate-background-ops:4,50; deploy-provenance:4; transient-artifacts-cleanup:23) — все датированные записи о прошлом, классификация верна. Строчных `big`-форм вне путей и идентификаторов нет (моя проверка) — заглавное `\bBig` в образце нормативного не теряет. Вывод «условие стало исполнимым» и «запуск за владельцем» — принимаю; статус «ждёт владельца» подтверждён сообщением коммита 68fef96.

**Правка (обязательная): поручение (б) не покрывает уцелевшую часть прежних внешних входов.** Собственная разбивка PE-01 от утра 24.08 (PE-01-разбор-2026-08-24.md:509, :558) называет прежние 57 мест «в памяти/`CLAUDE.md` MDMy», и разбор 19.08 прямо говорит, что признак «пропустил 57 предписаний … и главный файл проекта заодно». Память погибла 22.08 — `CLAUDE.md` в репозитории пережил. Формулировка «тот же образец по живой памяти проекта» оставляет его вне хода — нарушение самого условия «одним ходом или никак». Стало:

> (б) одно поручение исполнителю MDMy: тот же образец по живой памяти проекта **и по `CLAUDE.md` и прочим файлам инструкций проекта** (тем же определением множества, что в исходном Д1), правка тем же ходом; исполнитель возвращает 0 по всему набору.

Историческое утверждение «прежних 57 входов больше не существует» — принято с границей: точный состав 57 невосстановим, но как минимум CLAUDE.md из них жив, что правка выше и закрывает.

## 6. Мелочи раздела 7 — принять пакетом

Все пять проверены существованием мной: двойные пустые строки — `cat -A` (git-branch-discipline:36–37; git-dr-mirror-sync:26–27 и :42–43); строки «История правок» с адресами `memory/*` — все три на месте (git-branch-discipline:104, git-dr-mirror-sync:44, session-start:128), пометка «(память проекта MDMy)» уместна; android-developer-verification.md:10 — будущее время «с сентября 2026 … можно будет», истекает 01.09, правка отложенная — верно; расхождение артефакта шага 3 задания v2 воспроизведено мной по тексту задания (строка 71 требует декларацию в `description` **и** первой строкой тела → 2 строки, строка 75 задаёт `grep -c` → 1) — журнальная пометка, а не переписывание истории, оформлена верно. Разряд всех пяти («не меняют смысла») подтверждаю.

---

## 7. Проверка делом

| Случай (файл, дата) | Что предписывает новая редакция | Вердикт |
|---|---|---|
| Следующий выкат MDMy (24.08 их было четыре — ответ, раздел 13а) с heavy usage | П7: отчёт deploy-агента не принят, пока строка не в журнале; журнала в MDMy нет → тот же текст предписывает сначала завести файл по `templates/resource_log.md` (поручение (а)) | **Приводит** — при исполнении поручения (а); путь к нему прописан в самом доме |
| Следующая сессия координатора со спавном агентов (прецедент — сегодняшняя: строка не предъявлена ни разу, мой grep 746/393) | П6 без правки: сессия без файла разбора → «итоговый отчёт» в чате, приёмке нечего не принять — **неоднозначно = не приводит**. П6 с правкой носителя (запись памяти по итогам сессии обязательна после каждой сессии): строка обязана лежать в файле с датой | Без правки — **не приводит**; с правкой — **приводит** (это и есть причина правки) |
| Восстановление файла из бэкапа в любом проекте: мёртвый файл класса 24.08 (фрагмент бинарника под правильным именем, 0 NUL, 0 переводов строки) | Н2: (в) проходит, (г) режет, (а)/(б) режут дополнительно → «погиб», в отчёт | **Приводит** — признак (г) добыт ровно этим отказом |
| Семь чужих файлов телеграм-бота (валидная разметка, свой согласованный frontmatter) | Н2: (а)–(д) проходят все; ловит только (е) — принадлежность по `originSessionId`, не по префиксу имени | **Приводит** — и показывает, что (е) несущий: без него слепой перенос по префиксам |
| Живая копия при мусорной основной (`project_backlog_cpe_locate`, `_1`/`_4`, 24.08) | Н2: «копии под суффиксами проверяются все — живая копия бывает при мусорной основной» | **Приводит** |

## 8. Мелкие несоответствия (строка ревизии-файла → замена; не влияют на вердикты)

- :63 «Phase A/B — „Ключевые принципы“ (строка 49)» → «(строка 48)» (49-я — «Верификация»; проверено `cat -n README.md`).
- :63 «Production escalation — … (строка 47, дословный дубль)» → «(строка 47, дубль по содержанию)» — формулировки различаются, содержание одно.
- :273 «существование по README.md:53» → «README.md:54» (строка «Безопасные формулировки» — 54-я; 53-я — «Новая роль и серьёзное задание»).
- :167 «+ 1 в PQ-01-разборе» → дописать «(строка 156 — упоминание поля, не заполненная форма)»: счёт 30/1 воспроизводится моей командой `grep -c "Регрессы:"`, но единица — не заполнение.
- :38 обрывок «плюс мой grep TEMP не выполнялся» → «grep по метке TEMP я не выполнял» (граница от этого не меняется; факт снятия запрета я подтвердил чтением big7-consultation.md:34).
- К применению Н1–Н3 (не мелочь, а недостающий вход правки класса): датированное число в `scenarios/session-start.md:87` «Объём набора на 24.08.2026 — 62 файла» устареет в момент заведения трёх файлов — обновить тем же ходом на «65 файлов» с датой применения. Ревизия называет рост K («до 65»), но этот вход не называет; гейт живёт командой и не сломается, датированный текст — соврёт.

## 9. Не сошлись

Пусто. Отклонённых пунктов нет; все шесть моих правок — уточнения, не споры о существе. Если PE-01 не примет любую из них — по порядку `pe-01.md` («Если вы с PQ-01 не сошлись») обе позиции идут владельцу одним текстом, правка до этого не применяется.

## 10. Граница моей проверки

Докуда дошла проверка: всё, что лежит в этом worktree, проверено моими командами и чтением — счёт K/N/M, образец Д1 с разбивкой, все «Было», все входы правок, все пять заглушек, все пять мелочей, воспроизводимость quotePath-ловушки, наличие пяти линз (journal.jsonl: 5 started + 5 result). Содержимое отчётов линз я не читал: всё несущее перепроверено моими командами, и линзы как источник моему вердикту не нужны.

Где начинается доверие к записи:
- **Числа и механизмы MDMy** (1/11/169 и расхождение 181≠180, 15/20, четыре ложно погибших, семь чужих, 28 NUL, 23/37, `originSessionId`, `sync-memory.sh`, код 4, обход `MDMY_MEMORY_GATE`, четыре выката за день, 11 агентов за 22 секунды) — из ответ-файла исполнителя, прочитанного мной целиком; из worktree не воспроизводимы, из одного источника без второго прогона. На них стоят тексты случаев Н1–Н3 и части П5/П10; сами правила стоят и при неточности этих чисел, а расхождение 181≠180 уже записано открытым вопросом (раздел 8 ревизии, поручение (г)).
- **Слова владельца** (поручение 24.08 «всё доделать», мандат 11.08, «Проектное — в проекте» 24.08, снятие запрета Fable 24.08) — по датированным записям в своде и разборах, не сами слова. Это ровно то место, где подтверждение по записи стоит дешевле всего; ослаблений этих указаний в ревизии нет — П2 переносит мандат с защитным условием, остальные правки указания только проводят.
- **`memory/project_branch_cleanup_debt_2026-08-24.md`** и запись мандата 11.08 в памяти MDMy — существованием не проверены (недоступны отсюда); адрес первой — по сообщению коммита 84640ad, вторая закрыта условием применения П2.

Сверх входов ревизия не вносит ничего, кроме двух мест, оба разобраны: оговорка о git в Н2 (правка выше) и «имена в коде» в Н3 (в пределах открытой формулы исполнителя — принято).

## 11. Где мой разбор мог быть пристрастен

- **П6 и П7 добавляют работы мне**: приёмка записей сессий и отчётов с проверкой строк — это в том числе мои будущие разборы; это склоняло к «отклонить». Принял с правками, потому что артефакт без даты изменения непроверяем — довод rule-expiry п. 2 сильнее моего неудобства, и моя правка П6 работу не убавляет, а прибавляет.
- **Определение K** («проверено = пройдено линзами по датам/срокам/артефактам, а не прочитан каждый байт») снижает планку и моей проверке — принять его было удобно. Принял не поэтому: определение напечатано в самой строке счёта, читатель видит, что куплено за «62»; требование «каждый байт каждым» было бы неисполнимым (моя роль, п. 6) и превратило бы строку в ритуал.
- **П4 называет мой вердикт «страховкой выбора»** строгой редакции SEC-02 — повышает вес роли. Вердикт от этого не зависит: выбор строгой редакции обязателен для PE-01 и без меня (ослаблять запрещено ему самому).

## 12. Итог

**Применять можно** — после внесения шести правок дословно (П3 — «обязательная», П4 — третий вход :71, П6 — носитель, П7 — инцидент, Н2 — git-оговорка, Д1 — поручение (б) с CLAUDE.md) и с двумя условиями применения, уже названными в тексте: подтверждение записи мандата 11.08 до П2; коммит строки README и файла разбора одним ходом. Отклонённого — ноль; счёт 12 «принять» + 6 «принять с правкой». Порядок дальше — прежний: совместная редакция с PE-01, применение отдельным шагом, история правки — в файлы.
