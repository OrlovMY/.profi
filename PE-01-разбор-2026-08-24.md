# PE-01 — разбор библиотеки `.profi`, 2026-08-24

PE-01 [2026-08-24 01:00]: разбор по заданию владельца «Изучи всё и используя эту же команду предложи улучшения. Они должны реально улучшать. Если всё и так хорошо, то честно будет ответить, что улучшить нечего».

**Предмет:** вся библиотека `.profi` в рабочей копии `worktree-profi-review-2026-08-24` (HEAD `49610ab`). **Ни один файл свода не изменён** — только этот файл. Применение — после разбора PQ-01 и решения владельца.

**Короткий ответ на вопрос владельца:** улучшать есть что, и это не вкусовщина. Найдено четыре противоречия, у каждого из которых есть цена (прод, прод-данные, стale-сборка), одно правило, нарушенное при первом же применении, и набор правок 19.08, принятых PQ-01 и не применённых. По 39 файлам из 89 сказать нечего — это названо поимённо в разделе «Что прочитал».

---

## 0. Что прочитал и что не прочитал

**Прочитал целиком, построчно (`cat -n`), все 89 файлов свода** — множество: `git -c core.quotepath=off ls-files`, минус `PE-01-разбор.md`, `PQ-01-разбор-роли.md`, `.gitignore` (итого 89; `.gitignore` прочитан отдельно, 3 строки). Это: `README.md`, `knowledge/README.md`, 43 файла `knowledge/standards/`, 4 `knowledge/guides/`, 3 `knowledge/recipes/`, 7 `playbooks/`, 9 `scenarios/`, 4 `templates/`, `roles/hr-d.md`, 13 `roles/big7/`, 3 `roles/engineers/`.

**Прошлые разборы от 19.08.2026** (`PE-01-разбор.md` — 2457 строк, `PQ-01-разбор-роли.md` — 1846 строк): прочитал **не целиком**, а по оглавлению (`grep -n '^#'`) и затем сплошь следующие разделы: в PE-01 — строки 1–400 (этапы 0–2 целиком), 395–670 (этап 3 — правки 1–11, этап 4 — удаление, этап 5 — возражения, три вывода), 1275–1345 (блок 2, круг 3: Г1, сироты), 1430–1605 (блок 3 целиком), 1605–1675 (блок 4: У1–У3), 2357–2457 (правка 11, числа блока 5, открытые вопросы, «что прочитал»); в PQ-01 — §76–78, §85, §88–93, §54–60, §104–110. **Не читал:** в PE-01 — круги ответов на разбор PQ-01 (строки 670–1035), блок 1 (строки 842–1035, 943–1035) и блок 2 круги 1–2, 4 (1035–1275, 1393–1430), блок 4 У4–У8 и круг 2 (1675–1868), блок 5 правки 4–10 (1868–2357); в PQ-01 — §1–53, §61–75, §79–84, §94–103. Блок 1 и правки по памяти (`memory/`) не читал намеренно: памяти в этом репозитории нет, проверить их существованием нечем, выводов о них ниже нет.

**Статус применения правок 19.08 установлен не по тексту разборов, а по `git show --stat 49610ab` и по содержимому файлов** (раздел 2).

**Не выполнял:** скрипт аудита инварианта веток из `git-branch-discipline.md` (он для репозитория проекта, не для `.profi`); проверку существования файлов памяти (`memory/*`), `CLAUDE.md`, `docs/passports/**`, `console/src/i18n/**` — их в этом репозитории нет, ссылки на них ниже классифицированы как «внешние», а не как «битые».

**Файлы, по которым сказать нечего (39 из 89):** `knowledge/guides/android-developer-verification.md`, `compact-survivable-autonomous-task.md`, `fiber-middleware-group-gotchas.md`, `verify-deploy-actually-shipped.md`; `knowledge/recipes/` — все три; `knowledge/standards/`: `agent-artifact-channel-naming`, `agent-selfupdate-golden-rule`, `backlog-dedup-before-create`, `capture-findings-immediately`, `challenge-suboptimal-owner-decisions`, `data-pipeline-naming-contract`, `delegate-implementation-to-sonnet`, `delegate-search-to-persistent-haiku`, `doc-completeness-fix-on-miss`, `git-dr-mirror-sync` (кроме вопроса 2 в разделе 8), `inventory-legacy-removal`, `multi-tenant-superadmin-isolation`, `no-confirm-reasonable-nondestructive`, `no-guessing-read-docs-ask`, `no-silent-installs`, `probe-before-implement`, `profi-content-security-gate`, `progress-bar-long-actions`, `reuse-before-new-section`, `safe-wording-avoid-false-block`, `ship-freshest-approved`, `status-tracker-discipline`, `tenant-scoped-secrets-not-in-global-config`, `test-account-secret-hygiene`, `transient-artifacts-cleanup`, `treat-owner-as-expert`, `validate-recovery-before-destructive`, `verify-effect-by-data-never-assert-prediction`, `verify-in-ui`; `roles/big7/`: `ar-01`, `be-01`, `do-01`, `qa-01`, `sec-01`, `sec-02`; `roles/engineers/` — все три; `templates/resource_log.md`, `templates/workflow_scheme.md`; `scenarios/communication.md`, `scenarios/file-ownership.md`; `playbooks/phase_a_b_split.md` (судьба решена 19.08 — У1); `.gitignore`. Эти файлы прочитаны, и по ним правок нет — ни меняющих смысл, ни мелких.

---

## 1. Числа

Все числа — из команд ниже, выполненных в корне рабочей копии 24.08.2026. Определение множества — в каждой строке.

| Что | Значение | Команда | Множество |
|---|---|---|---|
| Файлов свода | **89** | `git -c core.quotepath=off ls-files \| grep -v разбор \| grep -v '^.gitignore$' \| wc -l` | все отслеживаемые файлы минус два файла разбора минус `.gitignore` |
| Объём свода | **4 342 строки, 296 585 символов, 473 450 байт** | тот же список → `xargs -0 cat \| wc -l -m -c` при `LC_ALL=C.UTF-8` | то же |
| Набор Шага −1 (`README + standards + scenarios + playbooks`) | **60 файлов, 2 674 строки, 192 842 символа, 308 321 байт** | `cat README.md knowledge/standards/*.md scenarios/*.md playbooks/*.md \| wc -l -m -c`; `ls … \| wc -l` | дословно по строке 21 `session-start.md` |
| Набор Шага 1.5 (то же + `roles/*`) | **77 файлов, 3 754 строки, 265 210 символов, 429 196 байт** | то же + `roles/*.md roles/big7/*.md roles/engineers/*.md` | дословно по строке 63 `session-start.md` |
| Разница двух наборов | **17 файлов, 120 875 байт** | вычитание | `roles/` целиком |
| Ссылок на `*.md` в бэктиках и `](…md)` | **242 сырых, 201 проверяемых, 82 не найдены в `.profi`** | скрипт `.tmp_checklinks.sh` (удалён после прогона; текст — в разделе 1а) | исключены плейсхолдеры `<…>`, `memory/*`, `docs/*`, абсолютные пути |
| Из 82 ненайденных — **битых внутренних** (файл должен быть в `.profi`, его нет) | **1** | та же выдача, разобрана вручную | `playbooks/worktree_deploy_safety.md:102 → knowledge/recipes/git_merge_strategies.md` |
| Из 82 — **внешних** (файлы памяти проекта, `CLAUDE.md`) | **75** | вручную по выдаче | `MEMORY.md`, `project_*`, `feedback_*`, `CLAUDE.md`, `agent-admin-server-contract.md` |
| Из 82 — по короткому имени без пути (файл есть) | **6** | вручную | `prod_escalation.md` ×4, `validate-recovery-before-destructive.md`, `hr-d.md`/`big7/INDEX.md` в README (относительно `roles/`) |
| Вики-ссылок `[[имя]]` | **21 уникальное имя, 20 существуют, 1 нет** | `grep -onE '\[\[[^]]+\]\]'` по своду → проверка `-e` по трём каталогам `knowledge/*` | не существует `[[feedback_compact_reload_profi]]` (файл памяти), плюс `[[...]]` как плейсхолдер ×4 |
| Ссылок на правила по имени, у которых нет файла в `.profi` | **9** | `grep -rnoE 'function-passport…\|ota-always-full-package\|verify-no-break\|vc-stale-builds-rootcause\|finish-before-new\|tests-mandatory\|verify-skill\|persist-streak\|knowledge-storage'` | перечень в разделе 5 |
| Файлов свода, называющих команду числом (6/7/9/10) | **22** | `grep -rlE 'Больш[а-яё]*[ -]?(6\|7\|9\|10)\b\|Больш[а-яё]* (шестёрк\|семёрк\|девятк\|десятк)[а-яё]*\|[Bb]ig[ _-]?(6\|7\|9\|10)\b' --include=*.md knowledge scenarios playbooks roles README.md templates \| wc -l` | файлы свода; пути `roles/big7/` и `big7-consultation.md` как адреса не отделялись — это верхняя оценка |
| Заполненных форм «История правок» | **0** (единственное вхождение — сам шаблон в `no-regression.md`) | `grep -rln 'История правок' --include=*.md . \| grep -v разбор` | свод |
| Файлов свода, изменённых коммитом `49610ab` (правки, меняющие смысл) | **19** | `git show --stat 49610ab` минус два файла разбора минус новый `rule-expiry-and-ownership.md` | — |
| Стандартов по формату `knowledge/README.md` («## Условия применения») | **0 из 43**; guides+recipes — **5 из 7** | `grep -l '^## Условия применения' knowledge/standards/*.md \| wc -l` | — |
| Временных оговорок `⏳` | **4 строки в 3 файлах**; без даты снятия и без срока вопроса — **1** (`big7-consultation.md:19`) | `grep -rn '⏳'` | свод |

Оценка в токенах — **не измерена** (токенизатора в среде нет). Для оценки: русский текст с разметкой ≈ 2,5–3,5 символа на токен → набор Шага −1 ≈ 55–77 тыс. токенов, набор Шага 1.5 ≈ 76–106 тыс. При контексте 1M (Шаг 0) это 6–11 % — исполнимо; при контексте 200k — 28–53 % — неисполнимо. Значит, исполнимость Шага 1.5 целиком зависит от выполнения Шага 0, и это нигде не сказано.

### 1а. Образец проверки ссылок (дословно, чтобы повторить)

```bash
export LC_ALL=C.UTF-8
git -c core.quotepath=off ls-files | grep -v разбор | grep -v '^.gitignore$' > /tmp/corpus.txt
: > /tmp/links_raw.txt
while read -r f; do
  grep -onE '`[^`]*\.md[^`]*`' "$f" | sed "s|^|$f:|" >> /tmp/links_raw.txt
  grep -onE '\]\([^)]*\.md[^)]*\)' "$f" | sed "s|^|$f:|" >> /tmp/links_raw.txt
done < /tmp/corpus.txt
# далее: снять бэктики/скобки, отрезать `:строка` и `#якорь`, пропустить `<…>`, memory/, docs/, абсолютные;
# проверить `-e` от корня и от каталога файла; печатать MISSING.
```

---

## 2. Состояние правок от 19.08.2026 (применено / отклонено / ожидает)

Установлено по `git show --stat 49610ab` (22 файла, из них 19 свода) и по текущему содержимому файлов, а не по тексту разборов.

| Пункт 19.08 | Вердикт PQ-01 | Состояние 24.08 | Чем проверено |
|---|---|---|---|
| Этап 0 — `pe-01.md`, `pq-01.md` | принято после кругов 1–3 | **применено** | коммиты `d80a02e`, `11d87ab`, `49610ab` |
| Правка 4а/4б — база для изолированных копий, четыре `git pull` | принять с правкой | **применено** | `worktree_deploy_safety.md:10–58`, `git-workflow.md:17–23`, `task-template.md:19–21`, `backend-engineer.md:28` |
| Правка 5 → Г2 — идентификаторы модели | отозвана в пользу Г2; Г2 закрыта | **применено** | `session-start.md:27–36`, `big7-consultation.md:19` |
| Правка 6а–6е — активный план | принять с правкой | **применено** | `session-start.md:70–78`, `memory_management.md:75–88`, `workflow_loop.md:9` |
| Правка 8а/8б/8в — имя ветки, исключение, ритуал мержа; §107 PQ-01 | принято, условие «вся сразу» | **применено** | `git-branch-discipline.md` (`grep -c main` → 0), строки 28–39 |
| Правка 9 — место записи формы `no-regression` | принято | **применено** — но см. правку П12: **ни разу не исполнено** | `no-regression.md:39–44`; `grep 'История правок'` → только шаблон |
| Правка 10 — стандарт о сроке жизни правила | принять с правкой (пункт 6) | **применено** | `rule-expiry-and-ownership.md`, 60 строк |
| Правка 11 / У3 — подвал в семи файлах `scenarios/` | принято с переоформлением (§85) | **применено** | `grep -rlF '<подвал>'` → только файлы разбора; `big7-consultation.md:82`, `parallel-agents.md:83` сохранены |
| Г3 — «не использовать простые модели» | применено с разрешения владельца | вне `.profi` (память MDMy) | не проверяемо здесь |
| **Г1 — мера чтения памяти (набор-перечень в `MEMORY.md`, артефакт `N из M`), §55 «признак соблюдения»** | **принять** (§54–55) | **не применено в `.profi`**: `session-start.md:52` по-прежнему «Не ограничиваться — читать всё», строка 21 — «КАЖДЫЙ файл `memory/*.md`» | `grep 'ОБЯЗАТЕЛЬНО ДО ПЕРВОГО'` по своду → пусто |
| **Сироты — «файл вне указателя — потерянный вход»** | принять, «внесено дословно» (§56) | **в `.profi` отсутствует** (`grep 'потерянный вход'` → пусто); возможно, внесено в память MDMy — проверить нечем | — |
| **Д1 — имя «ядро» вместо числа: условие «72 места одним ходом или никак»** | отклонено в исходном виде, условие принято | **ожидает** исполнителя и хода; не начато: 22 файла свода по-прежнему называют число | раздел 1 |
| **Д2 — `INDEX.md`: один дом состава, переименование заголовка, `INDEX.md` в набор гейта** | принять с обязательной правкой (§64) | **не применено**: `INDEX.md:1` «Большая 7», таблица из 7 строк, строки 39–43 — пять строк таблицы вне таблицы (`UX-01`, `UI-01`, `QA-02`, `PE-01`, `PQ-01`) | `cat -n roles/big7/INDEX.md` |
| **Д3 — `@UX-01/@UI-01/@QA-02` в формате созыва с предметным артефактом** | принять с правкой (§65) | **не применено**: `big7-consultation.md:11` «все семь ролей», строки 50–57 — семь адресатов | `cat -n scenarios/big7-consultation.md` |
| **У1 — удалить `playbooks/phase_a_b_split.md` одним ходом с двумя ссылками** | принять | **не применено**: файл есть, `worktree_deploy_safety.md:101` ссылается | `ls playbooks/` |
| **У2 — убрать ссылку на `git_merge_strategies.md`** | принять | **не применено**: `worktree_deploy_safety.md:102` | `grep -rn git_merge_strategies` |
| У4, У7, У8 (архив файлов памяти), Правка 7 (указатель), блок 1 Б1–Б3 | приняты | вне `.profi` | не проверяемо здесь |
| У5 — два блока деплоя в `CLAUDE.md` | снят, заменён правкой §74 | вне `.profi` | — |
| П3 — «не закрывается никогда» против «любой агент имеет предел» | находка без текста правки; вердикта нет | **открыто** — текст даю ниже (П13) | `big7-consultation.md:5`, `agent-question-rendezvous.md:12` |
| Возражение 2 — пять стандартов на одной оси | решение владельца не записано | **открыто** | раздел 8 |
| Открытые вопросы PQ-01 (§93, §107): подтвердить переназначение `origin` задним числом; срок запрета записи в GitLab | — | **открыто** | раздел 8 |

**Итого ожидает с 19.08 (принято PQ-01, не применено, не отклонено): Г1 (+§55), сироты, Д2, Д3, У1, У2; условно — Д1.** Повторно не формулирую — тексты в `PE-01-разбор.md` по указанным строкам и в `PQ-01-разбор-роли.md` §55, §64, §65, §76. Одно замечание к ним от сегодняшнего дня: Д2 писалась до Г1-третьей-редакции и говорит «`INDEX.md` вносится в набор … 79 файлов» — это набор **памяти MDMy**; для `.profi` тот же смысл должен попасть в правку П9 ниже (один перечисленный набор `.profi`, и `roles/big7/INDEX.md` в нём). Я включил его туда.

---

## 3. Правки, меняющие смысл

Порядок — по убыванию цены ошибки. Форма — четыре поля. Слово «Стало» везде означает готовый текст, вставляемый как есть.

### П1 🔴 Прочистка (`docker … prune`) предписана без эскалации в трёх местах при запрете в четвёртом — и это ровно тот случай, ради которого заведён запрет

**Было:**
- `playbooks/prod_escalation.md:13` — «Что считается production-affecting (эскалировать): 1. `docker prune` (любой), volume operations, image deletion.»; `:29` — «Что НЕ требует эскалации: … Build-cache prune без image deletion (если согласовано как routine)»; `:79` — «Реальный кейс: DO-01 при ENOSPC сам выполнил `docker builder prune -af && docker image prune -af`, освободил место и продолжил deploy. … затронуло rollback (старые images снесены). … Self-judgment агента = риск.»
- `playbooks/deploy_workflow.md:10` — «Должно быть **< 70% used**. Если выше → `docker system prune -af --filter "until=72h"`, проверить ещё раз. Только потом — миграции и build.»; `:20` — «Reclaimable < 2 GB. Иначе prune.»; `:92` — «2. `docker system prune -af --filter "until=72h"` (или `--volumes` если согласовано Big 7).»
- `templates/agent_briefing.md:42` — «Pre-flight: `df -h /` must be <70%; if higher → `docker system prune -af --filter "until=72h"` first.»; `:47` — «On ENOSPC: STOP, prune, retry migration».

`docker system prune -a` удаляет **все неиспользуемые образы**, то есть именно «image deletion» из пункта 1 списка эскалации, и именно то, что в «Реальном кейсе» снесло откат. Брифинг вставляется в начало **каждого** промпта исполнителя (`agent_briefing.md:3`), и в том же брифинге строкой 24 стоит «prune → STOP, ESCALATING TO HR-D». Исполнитель получает в одном документе «prune → стоп» и «prune first».

**Стало** (три входа одним ходом):

`playbooks/deploy_workflow.md`, строки 10 и 20 → заменить на:

> - Должно быть **< 70% used**. Если выше — сначала то, что разрешено без эскалации (`playbooks/prod_escalation.md`, «Что НЕ требует эскалации»): `docker builder prune -f --filter "until=72h"` — это кэш сборки, образы не трогаются. Проверить `df -h /` ещё раз. Если всё ещё выше 70 % — **STOP, эскалация в ядро** (`prod_escalation.md`, пункт 1: прочистка образов и томов — production-affecting, потому что сносит образы для отката). Только после этого — миграции и build.
> - Reclaimable < 2 GB. Иначе — тот же порядок: кэш сборки сам, образы — через эскалацию.

`playbooks/deploy_workflow.md`, строка 92 → заменить на:

> 2. `docker builder prune -f --filter "until=72h"` (кэш сборки — без эскалации). Не хватило — **STOP, эскалация в ядро**: `docker system prune -a`/`image prune`/`--volumes` — пункт 1 списка production-affecting, решает ядро, а не исполнитель (случай DO-01 в `prod_escalation.md`, «Почему правило существует»).

`templates/agent_briefing.md`, строки 42 и 47 → заменить на:

> - Pre-flight: `df -h /` must be <70%; if higher → `docker builder prune -f --filter "until=72h"` (build cache only). Still >70% → STOP, escalate (image/volume prune is production-affecting, see PROD-AFFECTING DECISIONS above).
> - On ENOSPC: STOP; build-cache prune only; anything that deletes images or volumes → escalate; then retry migration (build cache is idempotent).

**Почему:** старая редакция неверна не формулировкой, а тем, что **предписывает действие, которое тот же свод называет эскалируемым, и предписывает его первым шагом** — раньше, чем исполнитель дойдёт до запрета. `prod_escalation.md` заведён по случаю самовольной прочистки образов (строка 79); `deploy_workflow.md` и брифинг велят сделать то же самое «first», причём `system prune -a` шире, чем то, что сделал DO-01 в случае. Исполнитель в три часа ночи выполнит ту строку, до которой дошёл первым, — а первой стоит строка «prune first» в брифинге, который читают в начале каждого промпта. Случай: `prod_escalation.md:79`, без даты в файле. Новая редакция не ослабляет запрет, а делает его исполнимым: называет, что можно (кэш сборки — это и есть исключение из строки 29), и где начинается эскалация.

**Проверено существованием:** открыты целиком `prod_escalation.md` (разделы «Что считается», «Что НЕ требует», «Почему правило существует» — прочитаны вместе с соседними пунктами, не по выдаче поиска), `deploy_workflow.md` §1 и §7 целиком, `agent_briefing.md` блоки PROD-AFFECTING и DEPLOY WORKFLOW целиком. Что `docker system prune -a` удаляет неиспользуемые образы — по документации Docker (флаг `-a/--all`: «Remove all unused images not just dangling ones»); на живом сервере не проверял — сервера в предмете нет. `resource_tracking.md:58` («Cron weekly `docker builder prune -af`») новой редакции не противоречит: это кэш сборки.

### П2 🔴 Брифинг велит изолированному агенту «просто `git checkout -b`» — против обязательного предполётного порядка, заведённого после регрессии

**Было:**
- `templates/agent_briefing.md:20` — «If `isolation:"worktree"` — worktree is set up for you; just `git checkout -b <branch>`, work, commit. Don't worry about parent repo.»
- `playbooks/worktree_deploy_safety.md:20` — «`git fetch "<абсолютный путь к главному рабочему каталогу>" master` + `git checkout -b <ветка> FETCH_HEAD` | **Обязательно** до начала работы. Не через ремоут»; `:42` — «Хеш разошёлся или маркера нет — **СТОП**».
- `scenarios/task-template.md:19–21` — ветка создаётся через `git checkout master && git pull` с гейтом по ремоуту, без оговорки для изолированной копии.

**Стало:**

`templates/agent_briefing.md:20` → заменить на:

> - If `isolation:"worktree"` — the worktree is set up for you, but its base is NOT trusted: take it from the main working directory by absolute path, then verify it twice — `git fetch "<absolute path to main working dir>" master && git checkout -b <branch> FETCH_HEAD && git rev-parse --short HEAD` (must equal the hash named in your task) and `ls <marker file from task>` (must exist). Hash differs or marker missing → STOP and report. Do NOT branch from `origin/master` inside a worktree (`playbooks/worktree_deploy_safety.md`, Mandatory pre-flight — written after a worktree agent rebuilt prod from a stale base).

`scenarios/task-template.md` — после строки 21 («См. scenarios/git-workflow.md, п.1») добавить строку в тот же блок:

>                      В изолированной копии (isolation:"worktree") вместо pull — база из главного каталога
>                      по абсолютному пути + хеш + маркер: playbooks/worktree_deploy_safety.md, «Mandatory pre-flight».

**Почему:** старая редакция брифинга неверна по существу — «don't worry about parent repo» велит не делать того, что `worktree_deploy_safety.md` называет обязательным и заведённым после регрессии (шапка файла: агент пересобрал и выложил бандл со stale-базы). Брифинг читается первым и в каждом промпте; плейбук — только если до него дошли. Шаблон задания даёт третий вариант (pull из ремоута), который для изолированной копии плейбук прямо запрещает («Не через ремоут»). Три входа — три разных порядка; исполнитель выберет самый короткий. Случай: шапка `worktree_deploy_safety.md` (дата в файле не названа) и 16.08.2026 по `PE-01-разбор.md:152–166` (два агента подряд встали на проверке stale-`origin`). Правка 4 от 19.08 легла на плейбук и четыре `git pull`, но `templates/` в тот разбор не читались (`PE-01-разбор.md:129`) — это один из входов, пропущенных тогда.

**Проверено существованием:** `worktree_deploy_safety.md` прочитан целиком, включая раздел «Почему не через ремоут» и «Обратная сторона для координатора»; `agent_briefing.md` целиком; `task-template.md` строки 16–23 с окружением. Строка-маркер и хеш в задании — требование уже есть в плейбуке (`:41–42`), новый текст его не добавляет, а повторяет в месте, которое читают.

### П3 🟠 `parallel-agents.md` строит правило на техническом утверждении, которое опровергнуто случаем из соседнего плейбука

**Было:** `scenarios/parallel-agents.md:15–17` — «## Критическое ограничение: worktree блокирует сеть. Subagent в worktree-sandbox **не имеет SSH/scp/curl к внешним хостам**. Поэтому coding и deploy нельзя совмещать в одном worktree-агенте.» И `:76` — «Worktree-агент пытается `ssh root@<host>` — упадёт на sandbox-блокировке.»

Рядом: `playbooks/worktree_deploy_safety.md:3` — «агент, работавший в git worktree, пересобрал и **сам задеплоил** web-bundle на прод-сервер»; `:23` — «SSH на прод для **read-only** диагностики … Разрешено»; `:70–71` — post-flight через `ssh <prod-host>`; `:82` — «SSH на прод-сервер разрешён ТОЛЬКО для read-only diagnostics».

**Стало** (`parallel-agents.md:15–17` заменить, `:76` поправить):

> ## Критическое ограничение: изолированная копия не выкладывает
>
> Coding и deploy не совмещаются в одном worktree-агенте — **по правилу, а не потому, что песочница это не даст.** Сеть из изолированной копии бывает доступна: агент в worktree уже пересобирал и выкладывал бандл на прод со stale-базы (`playbooks/worktree_deploy_safety.md`, шапка). Что можно и чего нельзя из изолированной копии (read-only SSH — можно, build/up/cp/push — нельзя) — там же, разделы «Что НЕЛЬЗЯ» и «Что МОЖНО». Если в конкретной среде SSH действительно заблокирован — это удобно, но на это не рассчитывают.

Строка 76 → «Worktree-агент выполняет `docker compose build`/`up`, `cp` в статику или `git push` — red flag по `worktree_deploy_safety.md`, «Сигналы»; даже если команда прошла, результат откатывается.»

**Почему:** старая редакция неверна фактически: она утверждает блокировку как гарантию («упадёт на sandbox-блокировке»), а соседний плейбук описывает состоявшуюся выкладку из worktree и разрешает read-only SSH. Правило, опирающееся на гарантию, которой нет, исполняется как «раз не упало — значит можно». Случай: шапка `worktree_deploy_safety.md`. Новая редакция переносит запрет с «не сможешь» на «нельзя», что сильнее, а не слабее.

**Проверено существованием:** оба файла прочитаны целиком; в `worktree_deploy_safety.md` прочитаны таблицы «НЕЛЬЗЯ»/«МОЖНО» вместе с брифинг-блоком «Permissions» (строки 78–86), чтобы убедиться, что read-only SSH разрешён в трёх местах одного файла, а не в одном. Проверить блокировку сети в песочнице на практике не могу — в этой сессии нет изолированной копии с SSH-целью; утверждаю не «сеть есть», а «свод сам описывает случай, когда она была».

### П4 🟠 Причина «fetch падает silently» осталась в двух входах после того, как 19.08 её заменили в трёх

**Было:**
- `scenarios/git-workflow.md:56` — «**Критично:** на VPS перед `git fetch` в активную ветку обязательно отстёгивать HEAD, иначе fetch падает silently.»
- `templates/agent_briefing.md:43` — «ALWAYS `git checkout --detach` on remote BEFORE `git fetch ... HEAD:master`, иначе fetch silently fails.»
- Тот же файл `git-workflow.md:72`, шестнадцатью строками ниже: «Без detach git **отказывает и говорит об этом** — команда не молчит. Молчит следующий шаг: сборка запускается независимо от исхода получения кода».

**Стало:**

`git-workflow.md:56` → «**Критично:** на VPS перед `git fetch` в активную ветку обязательно отстёгивать HEAD: без этого git откажет — с сообщением, не молча, — а сборка всё равно запустится и соберёт старый код (подробно ниже, «Почему `--detach` обязателен»).»

`agent_briefing.md:43` → «- Bundle workflow: ALWAYS `git checkout --detach` on remote BEFORE `git fetch ... HEAD:master`. Without it git REFUSES (loudly) — but the build step runs anyway on the old tree. So after fetch ALWAYS check `git log --oneline -3` before build; that check is the gate, not a formality.»

**Почему:** 19.08 установлено (PE-01 блок 4, «Правка причины при шлюзе — ПРИМЕНЕНО, три входа», `PE-01-разбор.md:1826`), что старая причина неверна: git не молчит, молчит отсутствие шлюза между получением кода и сборкой. Правка легла на `git-workflow.md:72`, `deploy_workflow.md:44`, `git-branch-discipline.md:82` и не легла на строку 56 того же `git-workflow.md` и на брифинг. Один файл теперь утверждает противоположное через 16 строк; брифинг, который читают первым, учит неверной причине. Это класс «правка на часть входов», и это не новая правка, а доведение принятой.

**Проверено существованием:** `grep -rn 'silently\|silent' --include=*.md scenarios playbooks templates knowledge roles` — два вхождения с этим смыслом (`git-workflow.md:56`, `agent_briefing.md:43`); остальные два (`agent-selfupdate-golden-rule.md:18`, `no-confirm-reasonable-nondestructive.md:10`) — другой предмет, прочитаны по месту. Разряд: меняет причину правила — значит, меняет смысл; отношу к полной форме.

### П5 🟠 Роль координатора велит читать «релевантные файлы» — протокол старта называет это анти-паттерном

**Было:** `roles/hr-d.md:32` — «8. **Ведение памяти** — в начале сессии читать `memory/MEMORY.md` и релевантные файлы. После сессии — пополнять память уроками».
`scenarios/session-start.md:52` — «Не ограничиваться «ключевыми» или «релевантными» — читать всё»; `:92` — «Anti-patterns: Грузить только «релевантные» файлы → пропускаешь правила, которые не знаешь, что нужны».

**Стало** (`hr-d.md:32`):

> 8. **Ведение памяти** — в начале сессии и после каждого уплотнения контекста — по протоколу `scenarios/session-start.md` (Шаги −1…2), а не «релевантные файлы»: какие именно и в каком объёме, решает протокол, не роль. После сессии — пополнять память уроками (`playbooks/memory_management.md`).

**Почему:** старая редакция неверна тем, что противоречит протоколу старта в той самой точке, по которой за лето записано три рецидива (`session-start.md:15, 17, 21`): роль разрешает то, что протокол называет причиной рецидива. Роль — системный промпт координатора и читается раньше сценария. Случай: рецидивы 17.06, 01.07, 06.07 по `session-start.md`. Окончательную меру чтения (Г1) я здесь не задаю — она ожидает с 19.08; роль должна ссылаться на протокол, каким бы он ни был.

**Проверено существованием:** `hr-d.md` прочитан целиком; `session-start.md` — Шаг 1 и Anti-patterns прочитаны вместе с Шагом −1.

### П6 🟠 У PM-01 свой шаблон задания — короче обязательного, и PM-01 обязан отклонять задания по своему же шаблону

**Было:** `roles/big7/pm-01.md:16–26` — «3. **Онбординг новых агентов** — пишешь задание по шаблону: ``` Ветка: feat/<имя> (создать от master) / НЕ ТРОГАТЬ / ПЕРЕДАТЬ / Проверка в конце: git status, git log / Приложи хэш / Если Bash запрещён — "ЗАДАЧА НЕ ЗАВЕРШЕНА" ```».
`scenarios/task-template.md:13` — «Каждое задание от PM-01 агенту содержит:» — и далее гейт перед ответвлением (`:19–21`), блок «Тесты по этапам (ОБЯЗАТЕЛЬНО)» (`:30–36`); `:54–60` — «PM-01 **отклоняет** работу, если: … Нет раздела «Тесты по этапам»».

**Стало** (`pm-01.md:16–26` заменить целиком):

> 3. **Онбординг новых агентов** — пишешь задание **по `scenarios/task-template.md`, целиком**: ветка с гейтом перед ответвлением, «НЕ ТРОГАТЬ» / «ПЕРЕДАТЬ», раздел «Тесты по этапам» (обязателен — `knowledge/standards/task-testing-all-stages.md`), проверка в конце, условие «ЗАДАЧА НЕ ЗАВЕРШЕНА». Шаблон здесь не повторяется: на 19.08.2026 укороченная копия в этой роли не содержала ни гейта, ни тестов, которые та же роль обязана требовать при приёмке.

**Почему:** старая редакция неверна тем, что это дубль с меньшим объёмом действия (`rule-expiry-and-ownership.md`, п. 4 — «у правила один дом»; `pe-01.md`, п. 2 — «пересказ обязан совпадать по объёму»): PM-01, следуя своей роли, выдаст задание без «Тестов по этапам» и без гейта, а затем по `task-template.md:60` обязан его же отклонить. Неполный список хуже отсутствующего — он выглядит полным. Случая нет: правило выведено из сравнения двух текстов.

**Проверено существованием:** оба файла прочитаны целиком; `task-testing-all-stages.md` существует (48 строк), раздел «Acceptance» прочитан.

### П7 🟠 Кто пишет задание агенту — три ответа в трёх файлах, и новый стандарт о PE-01 не знает ни один из них

**Было:**
- `roles/hr-d.md:27` — «3. **Делегирование** — направить задачу нужному агенту»; `:29` — «5. **Agent briefing** — каждый coding-промпт начинать с embedded брифинга»; `playbooks/workflow_loop.md:27–35` — «HR-D запускает агентов. Каждый промпт coding-агента содержит…».
- `roles/big7/pm-01.md:16` — «Онбординг новых агентов — пишешь задание»; `scenarios/task-template.md:13` — «Каждое задание от PM-01».
- `knowledge/standards/roles-and-briefs-via-pe01.md:9–11` — «Текст новой роли или серьёзного задания агенту пишет **PE-01**. Его проверяет **PQ-01**. … Писать такие тексты самостоятельно, минуя эту пару, больше нельзя.» Ссылается на этот стандарт **только `README.md:53`** (`grep -rln roles-and-briefs-via-pe01`).

**Стало:**

`roles/hr-d.md`, в раздел «Обязанности» после пункта 5 добавить:

> 5а. **Граница с PE-01/PQ-01.** Новая роль и серьёзное задание (по признакам из `knowledge/standards/roles-and-briefs-via-pe01.md`) составляются не тобой и не PM-01, а PE-01, с разбором PQ-01 **до запуска**. Твоя часть в таком случае — поставить задачу PE-01 и вложить готовый текст в брифинг. Признак, что ты нарушил порядок: файл роли или задание изменены, а записи разбора PQ-01 нет. Прямое указание владельца 19.08.2026.

`roles/big7/pm-01.md`, после пункта 3 (в новой редакции П6) добавить одну строку:

>    Серьёзное задание и новая роль — не по этому шаблону, а через PE-01 с проверкой PQ-01 (`knowledge/standards/roles-and-briefs-via-pe01.md`); шаблон выше — для рядовых заданий по готовому образцу.

`scenarios/task-template.md:13` → «Каждое рядовое задание агенту (от PM-01 или HR-D) содержит: … Серьёзные задания и новые роли — `knowledge/standards/roles-and-briefs-via-pe01.md`.»

**Почему:** старая редакция неверна тем, что стандарт, заведённый прямым указанием владельца, не имеет ни одного входа из тех файлов, по которым задания фактически пишутся: у HR-D, PM-01 и шаблона задания нет слова «PE-01». Правило без входа из места срабатывания не срабатывает (`rule-expiry-and-ownership.md`, п. 3: производящая половина отказывает первой). Случай: `roles-and-briefs-via-pe01.md:23–27` (18–19.08.2026). Отдельно — вопрос об исполнимости третьего признака «всё, что агент выполняет без пошагового контроля» вынесен в раздел 8, текст стандарта здесь не трогаю.

**Проверено существованием:** `grep -rln 'roles-and-briefs-via-pe01' --include=*.md .` → `README.md` и сам стандарт; `hr-d.md`, `pm-01.md`, `task-template.md`, `workflow_loop.md` прочитаны целиком.

### П8 🟡 Брифинг перечисляет production-affecting действия неполным списком — при полном списке в плейбуке

**Было:** `templates/agent_briefing.md:24` — «ANY action on shared production infra (prune, restart cascade, migration retry, force-deploy, image deletion, secret rotation, schema DROP) → STOP». В `playbooks/prod_escalation.md:13–21` — девять пунктов; в брифинге нет пунктов 5 (push клиентского обновления на > 1 устройство), 6 (любая команда с `-f`/`--force`/`--volumes`), 8 (отключение security-middleware «временно»), 9 (изменения данных в продовой БД).

**Стало** (`agent_briefing.md:24`):

> - ANY action on shared production infra → STOP, report to HR-D as "ESCALATING TO HR-D" with proposal + reversibility cost. The full list of what counts is `playbooks/prod_escalation.md`, section "Что считается production-affecting" — read it, this line is not the list. If unsure whether it counts — it counts.

**Почему:** старая редакция неверна по правилу о пересказе (`pe-01.md`, п. 2, последний абзац): «Если правило перечисляет объекты, указатель либо перечисляет их все, либо не перечисляет ни одного и отсылает за списком в файл — неполный список хуже отсутствующего, потому что выглядит полным». Исполнитель, сверившись со скобкой в брифинге, не найдёт там `UPDATE` прод-данных и сочтёт его разрешённым. Случая нет: выведено из сравнения списков. Пример остался в предыдущем абзаце брифинга (`:25–27` — что можно без эскалации), его не трогаю.

**Проверено существованием:** оба списка прочитаны рядом и сопоставлены попунктно (9 против 7 названных, 4 отсутствуют).

### П9 🟡 Протокол старта задаёт набор `.profi` тремя разными способами в одном файле, привязан к каталогу одной машины и ссылается на файл памяти, которого в библиотеке нет

**Было:**
- `scenarios/session-start.md:21` (Шаг −1) — «(1) все `.profi` README+standards+scenarios+playbooks» — 60 файлов; `:19` (самопроверка) — то же; `:63` (Шаг 1.5) — «Прочитать актуальные `knowledge/standards/*`, `scenarios/*`, `playbooks/*`, `roles/*`» — 77 файлов, и слово «актуальные» не определено.
- `:59` — «`.profi` (`C:\Users\orlovmy\Projects\.profi`) — единый источник истины» при `README.md:33` «Скопировать `.profi/` в проект или держать как submodule / отдельный clone рядом».
- `:21` — «См. [[feedback_compact_reload_profi]]» — файл памяти MDMy; в `.profi` его нет, и это единственная вики-ссылка свода в никуда (раздел 1).

**Стало:**

`session-start.md:21`, фрагмент «(1) все `.profi` README+standards+scenarios+playbooks» → «(1) **обязательный набор `.profi`** (перечень — Шаг 1.5)»; `:19` — так же. Хвост строки 21 «См. [[feedback_compact_reload_profi]]» → «(запись о рецидиве — в памяти проекта, где он случился).»

Шаг 1.5 (строки 57–66) → заменить на:

> ## Шаг 1.5 — Обязательный набор `.profi` (для ЛЮБОГО проекта)
>
> `.profi` — единый источник истины для ролей, стандартов, сценариев, плейбуков, шаблонов. Где он лежит в этом проекте — в `CLAUDE.md`/`MEMORY.md` проекта (копия, submodule или соседний clone — `README.md`, «Как использовать»); абсолютный путь в этом файле не пишется — он у каждой машины свой.
>
> **Обязательный набор — перечень, а не признак:**
> 1. `README.md`;
> 2. `knowledge/standards/*.md` — все;
> 3. `scenarios/*.md` — все;
> 4. `playbooks/*.md` — все;
> 5. `roles/hr-d.md` и `roles/big7/INDEX.md` — роль координатора и единственный дом состава команды.
>
> Остальные файлы ролей — системные промпты конкретных агентов: читаются при созыве этого агента, не при старте. `knowledge/guides/`, `knowledge/recipes/`, `templates/` — по теме задачи.
>
> Объём набора на 24.08.2026: **62 файла, 2 769 строк, 199 607 символов, 318 821 байт** (`cat README.md knowledge/standards/*.md scenarios/*.md playbooks/*.md roles/hr-d.md roles/big7/INDEX.md | wc -l -m`). Это помещается только в контекст 1M — поэтому Шаг 0 стоит раньше этого шага, а не для порядка.
>
> **Артефакт гейта** — одна строка до первого действия: `ГЕЙТ .profi: N файлов из M` — где M получено командой выше (`ls … | wc -l`), а не названо по памяти. `N < M` — гейт не пройден.
>
> **Механизм авто-подгрузки:** в `CLAUDE.md`/`MEMORY.md` каждого проекта должен быть явный пункт «при старте сессии подгрузить `.profi`» с путём. Нет пункта — добавить в тот же ход. Прямое поручение владельца, 2026-06-08.

**Почему:** старая редакция неверна трижды. (1) Два множества в одном файле (60 и 77 файлов) при недоопределённом «актуальные» — исполнитель выберет меньшее и будет прав; это тот самый класс «признак вместо перечня», который 19.08 стоил двух отклонений (`PE-01-разбор.md:1275–1340`). (2) Абсолютный путь одной машины в файле, который `README.md` объявляет переносимым, — утверждение о состоянии без даты и неверное для любого другого проекта (`rule-expiry-and-ownership.md`, п. 1). (3) Вики-ссылка на файл памяти MDMy из универсального сценария — для любого другого проекта это ссылка в никуда; сам случай (06.07.2026) уже пересказан в той же строке, ссылка смысла не добавляет. Случаи: рецидивы 17.06, 01.07, 06.07 (`session-start.md:15–21`); числа — раздел 1.

**Проверка по правилу о сокращении обязательного.** Новый набор меньше набора Шага 1.5 (62 против 77 файлов: не читаются при старте 15 файлов ролей агентов) и больше набора Шага −1 (60). Три самых дорогих правила `.profi` — те, чьё нарушение необратимо:
1. потеря устройства / данных — `knowledge/standards/validate-recovery-before-destructive.md` → **внутри** (п. 2);
2. простой боевой системы — `playbooks/prod_escalation.md` → **внутри** (п. 4);
3. выкладка старого кода поверх прода — `playbooks/worktree_deploy_safety.md` и `knowledge/standards/git-branch-discipline.md` → **внутри** (п. 4 и п. 2).
Ни одно правило класса HARD не лежит в `roles/` — проверено: `grep -l 'HARD' roles/*/*.md roles/*.md` → пусто. Что теряется: роли `pm-01…sec-02`, `ux-01`, `ui-01`, `qa-02`, `pe-01`, `pq-01`, инженеры — это промпты, а не правила для координатора; они читаются при созыве (и в `big7-consultation.md` созыв идёт через одного постоянного агента, которому роли передаются при запуске).

**Проверено существованием:** `session-start.md` прочитан целиком трижды (Шаг −1, самопроверка, Шаг 1.5 — вместе с соседними абзацами); числа из команд раздела 1; `roles/big7/INDEX.md` существует (43 строки); `grep -l HARD` по ролям выполнен.

### П10 🟡 Временный запрет Fable без срока — по собственному стандарту свода это постоянный запрет, и эта сессия идёт на Fable

**Было:** `scenarios/big7-consultation.md:19` — «[⏳TEMP-OPUS-INSTEAD-OF-FABLE: временный жёсткий запрет Fable (owner 2026-07-12, срок НЕ задан); вернуть Fable по этой метке когда owner снимет]».
`knowledge/standards/git-branch-discipline.md:62` — «Оговорка без даты снятия к следующей ревизии считается просроченной … временная оговорка без даты — это постоянная оговорка»; `rule-expiry-and-ownership.md:8` — датировать утверждения о моделях.

**Стало** (дописать в ту же строку 19 после «срок НЕ задан»):

> ; **срок вопроса:** на каждой ревизии свода (ближайшая — 26.08.2026, `knowledge/standards/rule-expiry-and-ownership.md`) владельцу задаётся вопрос «запрет Fable ещё действует?». Нет ответа — запрет действует дальше как есть; снимается только словами владельца, с датой в этой метке.

И отдельным наблюдением для владельца (не правка): сессия, в которой пишется этот разбор (PE-01, 24.08.2026), по системному контексту работает на модели Fable 5. Либо запрет снят и свод об этом не знает, либо задание выдано вразрез с запретом. Решить может только владелец; до его ответа текст запрета остаётся дословно.

**Почему:** старая редакция неверна не запретом (его смысл — владельца, не трогаю), а отсутствием момента пересмотра: «когда owner снимет» не называет, кто и когда спросит, и через квартал метка будет объяснять любому, почему высший орган суждения работает не на самой свежей модели, — ровно то, против чего написан весь раздел «Модель Big-7». Собственный стандарт свода (`git-branch-discipline.md:62`, внесён 19.08 и принят PQ-01) называет такую оговорку постоянной. Случай: `rule-expiry-and-ownership.md:52` — «правило „работать на свежей модели“ имело четыре входа, и в каждом стояла своя модель».

**Проверено существованием:** `grep -rn '⏳'` — четыре строки, три из них с датой и адресом снятия (`memory/feedback_push_github_only_temporarily.md`), одна без; `rule-expiry-and-ownership.md` раздел «Ревизия» прочитан. Модель этой сессии — по строке системного контекста «You are powered by the model named Fable 5»; проверить через `/model` из агента не могу.

### П11 🟡 Артефакт ревизии свода записывается в `MEMORY.md` — у библиотеки нет `MEMORY.md`

**Было:** `knowledge/standards/rule-expiry-and-ownership.md:40–48` — «Первая ревизия — до 26.08.2026 … к этой дате существует либо строка в `MEMORY.md` вида `Ревизия свода: <дата> · не сработало: N · архивировано: M`, либо запись о том, почему ревизия не проведена».

`.profi` — автономный репозиторий (`README.md:33` — копия/submodule/clone), `MEMORY.md` в нём нет; в какой из проектных памятей искать строку — не сказано. У правила есть срок и форма, но нет места.

**Стало** (строки 42–48 заменить):

> **Артефакт — двусторонний и лежит в самой библиотеке, а не в памяти какого-то из проектов:** к этой дате в `README.md` библиотеки существует раздел «Ревизии свода» со строкой вида
>
> ```
> Ревизия свода: <дата> · проверено файлов: K · не сработало: N · архивировано: M · разбор: <файл>
> ```
>
> либо строка «Ревизия <дата> не проведена: <кем отложена, до какого числа>». Нет ни того ни другого — считать, что вопрос «что в своде лишнее» остался без ответа, и не выдавать за него выборку по виду. Проектная память может дублировать строку указателем, но дом — здесь: `README.md` входит в обязательный набор старта, `MEMORY.md` проекта — нет для соседнего проекта.

**Почему:** старая редакция неверна по пункту 3 того же стандарта — артефакт повешен на место, которого у предмета правила нет; для любого проекта, кроме MDMy, правило неисполнимо, а для MDMy — исполнимо в файле, который читают только в MDMy. Случая нет: выведено из устройства репозитория (`ls` корня — `MEMORY.md` отсутствует). Срок 26.08.2026 не трогаю; этот разбор — кандидат в «разбор: <файл>» для первой строки.

**Проверено существованием:** `ls` корня рабочей копии; `README.md` прочитан целиком — раздела «Ревизии» нет; `git-branch-discipline.md:62` ссылается на ту же дату — после правки адрес артефакта у обоих один.

### П12 🟡 Форма `no-regression` не заполнена ни разу — включая девятнадцать файлов, изменённых коммитом, который её и ввёл

**Было:** `scenarios/no-regression.md:39` — «**Где форма записывается.** В теле самого изменения … для `.profi` и памяти — абзацем в правленом файле под заголовком «История правок» … **Незаписанная форма считается незаполненной**»; `:41` — «**Когда обязательна:** удаление правила, изменение его смысла, изменение роли, изменение порядка эскалации, любые требования безопасности».

Факт: коммит `49610ab` (19.08.2026) изменил смысл в 19 файлах свода — и ни в одном нет абзаца «История правок»; `grep -rln 'История правок'` по своду даёт только сам шаблон. Правило введено и нарушено одним коммитом. При этом у каждой из этих правок **есть** запись с полями «Было / Стало / Почему» и вердиктом PQ-01 — в двух файлах разбора по 360 КБ.

**Стало** (`no-regression.md:39` заменить абзац):

> **Где форма записывается.** В теле самого изменения, а не в переписке. Для `.profi`: если правка прошла разбор PE-01/PQ-01 — записью этого разбора (поля «Было / Стало / Почему / Проверено существованием» у PE-01 и вердикт PQ-01 — это и есть заполненная форма), а в правленом файле и в сообщении коммита — одна строка-адрес: `История правок: <файл разбора>, пункт <N>, <дата>`. Если разбора не было (правка не меняющая смысла — `pq-01.md`, «Когда ты подключаешься») — форма не требуется. Для памяти проекта — абзацем в правленом файле под заголовком «История правок». Для кода — в описании коммита. **Незаписанная форма считается незаполненной**: 19.08.2026 форма была введена и в тот же день не заполнена ни в одном из 19 изменённых файлов — потому что запись требовалась в файле, а работа велась в разборе.

**Почему:** старая редакция неверна тем, что требует второй экземпляр записи, которая уже существует в более полной форме, — и потому не исполняется: 0 из 19 при первом же применении (`git show --stat 49610ab`; `grep`). Требование без исполнения обесценивает соседние (`pe-01.md`, п. 5). Новая редакция **не понижает** требование — форма остаётся обязательной, — а называет место, где она фактически живёт, и добавляет адрес в файл, чтобы от файла можно было дойти до обоснования. Если PQ-01 сочтёт это ослаблением — запасной вариант: дописать «История правок» в 19 файлов задним числом ссылкой на пункты разбора (это та же правка, только в 19 местах вместо одного). Случай: коммит `49610ab`.

**Проверено существованием:** `git show --stat 49610ab`; `grep -rln 'История правок' --include=*.md .`; `no-regression.md` прочитан целиком, включая «Когда не требуется» и «Частые ловушки».

### П13 🟡 «Не закрывается никогда» против «любой агент имеет предел» — находка П3 от 19.08, оставшаяся без текста

**Было:** `scenarios/big7-consultation.md:5` — «созывается в начале сессии **один раз и не закрывается никогда**»; `knowledge/standards/delegate-search-to-persistent-haiku.md:4` — «постоянный, не выключаемый Haiku-агент»; `knowledge/standards/agent-question-rendezvous.md:12` — «**Не плодить безлимитные fire-and-forget агенты.** Любой агент имеет явный scope + предел (макс. время / макс. tool-calls)».

**Стало** (дописать в `agent-question-rendezvous.md:12` после «предел (макс. время / макс. tool-calls)»):

> Исключение — **персистентные агенты по стандартам делегирования** (ядро — `scenarios/big7-consultation.md`; поиск — `delegate-search-to-persistent-haiku`; документация — `delegate-documentation-to-persistent-sonnet`; наблюдение — `delegate-background-ops-to-cheap-subagents`): их предел — сессия, потому что их ценность в накопленном контексте. Это не fire-and-forget: они ждут `SendMessage`, а не крутят цикл.

**Почему:** старая редакция неверна тем, что общее правило о пределе не знает о четырёх стандартах, которые это правило прямо нарушают по указанию владельца (07.07, 08.07.2026), — и потому читается как запрет на них. Противоречие снимается одной фразой о границе применимости (`pe-01.md`, Б3); 19.08 оно было названо (`PE-01-разбор.md:237–242`) и текста не получило.

**Проверено существованием:** четыре названных файла существуют (раздел 1, вики-ссылки); `agent-question-rendezvous.md` прочитан целиком, включая «Экономика» и «Триггеры парковки».

### П14 🟡 Роли UX-01 и UI-01 называют файлы одного проекта как артефакт роли

**Было:** `roles/big7/ux-01.md:113–119` — таблица «Чем закреплено»: `console/src/i18n/__nav_grouping.test.ts`, `__nav_structure.test.ts`, `__routes_reachable.test.ts`, `__tabs_in_url.test.ts`, `docs/passports/ux/information-architecture.md`; `:37` — «**`docs/passports/ux/information-architecture.md`** — карта»; `roles/big7/ui-01.md:42` — «ратчеты в `console/src/i18n/`».

**Стало:**

`ux-01.md:37` → «**карта продукта** — в проекте это один файл (в MDMy на 19.08.2026 — `docs/passports/ux/information-architecture.md`; в другом проекте путь берётся из памяти проекта): разделы → подразделы → вкладки, с назначением каждого узла и обоснованием, почему он там.»

`ux-01.md:113–119` → таблицу оставить, колонку «Чем закреплено» заменить на обобщённые имена с пометкой:

> | Что проверяется | Чем закреплено (в проекте — свой файл; имена ниже — из MDMy на 19.08.2026, в другом проекте взять из памяти проекта) |
> |---|---|
> | родственные разделы собраны, а не рассыпаны | проверка группировки навигации (`__nav_grouping.test.ts`) |
> | нет дублей и подписей-признаний, меню не разрастается | проверка структуры навигации (`__nav_structure.test.ts`) |
> | до каждого раздела можно дойти | проверка достижимости маршрутов (`__routes_reachable.test.ts`) |
> | вкладка живёт в адресе | проверка вкладки в адресе (`__tabs_in_url.test.ts`) |
> | место функции на карте | запись в карте продукта (см. «Главный предмет работы») |

`ui-01.md:42` → «часть работы уже делают автоматические проверки словаря (ратчеты; где они лежат — в памяти проекта).»

**Почему:** старая редакция неверна для библиотеки, объявленной переносимой (`README.md:3, 33`; коммит `5d295c4` «убрать проектную привязку — универсальные формулировки во всей библиотеке»): в другом проекте исполнитель будет искать `console/src/i18n/__nav_grouping.test.ts`, не найдёт и решит, что артефакта нет — а артефакт для этой роли обязателен (`ux-01.md:109`). Утверждение о путях без даты — п. 1 `rule-expiry-and-ownership.md`. Случая нет: выведено из того, что библиотека сейчас используется только в одном проекте, и потому расхождение ещё не стреляло. Остальные проектные следы свода (раздел 8, вопрос 2) — не правлю: часть из них принята PQ-01 19.08 как осознанная.

**Проверено существованием:** `grep -rnoE 'console/src[^ `]*|docs/passports[^ `)]*'` — 8 вхождений, все в `ux-01.md`, `ui-01.md` и в четырёх стандартах, где `docs/passports/**` стоит как пример таксономии (прочитаны по месту — там это образец, не адрес). Файлы `console/src/…` в этом репозитории отсутствуют по определению.

---

## 4. Список на удаление

По форме роли: происхождение — что защищает — куда переезжает.

### Удаление 1. `templates/memory_index.md`, раздел «Doctrine — правила работы» (строки 12–31): двадцать строк, велящие новому проекту завести файлы памяти, дублирующие `.profi`

**Происхождение:** первый коммит библиотеки `a43130e` (2026-06-08, «universal team library»); шаблон индекса памяти перенесён из проекта, где эти `feedback_*` существовали до появления `.profi`.

**Что защищает сегодня:** ничего сверх `.profi`: каждая из 17 строк (`feedback_session_start`, `feedback_response_format`, `feedback_communication_chain`, `feedback_hrd_no_coding`, `feedback_big7_with_sec02`, `feedback_prod_escalation`, `feedback_no_parallel_same_tree`, `feedback_finish_before_new`, `feedback_official_docs_required`, `feedback_build_policy`, `feedback_ui_redesign_on_growth`, `feedback_admin_tooltips`, `feedback_changelog_required`, `feedback_autonomy_policy`, `feedback_autonomous_diagnosis`, `feedback_task_clarification`, `project_workflow_scheme`) — это пересказ сценария или стандарта `.profi`. Шаблон велит новому проекту **создать** эти файлы, а `session-start.md:59` велит, чтобы «память конкретного проекта хранила только проектную специфику и **ссылалась** на `.profi`», и четыре стандарта `delegate-*` — «строка-указатель, не дублировать». Шаблон производит ровно тот класс дефекта, который 19.08 был главной находкой по памяти MDMy: правило в двух домах, расходящихся при первой правке (`rule-expiry-and-ownership.md`, п. 4).

**Куда переезжает смысл:** в одну строку на месте раздела:

> ## Doctrine — правила работы
>
> - [Библиотека `.profi`](<путь к .profi в этом проекте>/README.md) — роли, сценарии, плейбуки, стандарты. **Здесь не дублируются**: правило живёт в `.profi`, в памяти — только проектная частность со ссылкой (`scenarios/session-start.md`, Шаг 1.5).

Строки 33–46 («Infrastructure & deploy», «Team») — оставить: это проектная специфика (пути на VPS, история найма), ей здесь место. `agent_briefing_template.md` (строка 15) → заменить на ссылку `templates/agent_briefing.md` библиотеки.

**Проверено существованием:** `templates/memory_index.md` прочитан целиком; для каждой из 17 строк найден файл `.profi` с тем же правилом (`communication.md`, `hr-d.md`, `big7-consultation.md`, `prod_escalation.md`, `parallel-agents.md`, `workflow_loop.md` п. 0, роли «Официальная документация», `agent_briefing.md` ENVIRONMENT, `ux-01.md`, `no-confirm-*`/`autonomous-self-verify-*`, `confirm-understanding-*`); для `feedback_admin_tooltips` и `feedback_changelog_required` прямого дома в `.profi` **нет** — это проектные требования, их строки оставить, перенеся в раздел проектной специфики.

### Ожидают с 19.08 (не повторяю): У1 `playbooks/phase_a_b_split.md` (с двумя ссылками), У2 ссылка `git_merge_strategies.md`.

Других кандидатов на удаление по цене не нашёл. Признак отбора назову честно: «файл или раздел, смысл которого целиком живёт в другом файле `.profi`» — проверен на трёх примерах до подсчёта (`phase_a_b_split.md` — ловит; раздел Doctrine — ловит; `memory_management.md` «Session start ритуал» — **не ловит**, потому что там краткая версия плюс ссылка, и краткая версия отличается от полной по объёму: см. мелкие правки). Признак ловит дубли, не ловит мёртвые правила — их искал отдельно по дате и артефакту (раздел 3, П10–П12).

---

## 5. Правки, не меняющие смысла (одной строкой каждая; разряд перепроверяет PQ-01)

1. `knowledge/standards/deploy-provenance-and-build-integrity.md:13, 30` — ссылки `ota-always-full-package`, `finish-before-new`, `verify-no-break`, `vc-stale-builds-rootcause` — файлов в `.profi` нет: первую заменить на «(§2 этого файла)», вторую — на `playbooks/workflow_loop.md` п. 0, третью — на `knowledge/guides/verify-deploy-actually-shipped.md`, четвёртую — на «урок в памяти проекта».
2. `knowledge/standards/task-testing-all-stages.md:14, 48`, `documentation-first-golden-rule.md:13, 23`, `delegate-documentation-to-persistent-sonnet.md:28` — «function-passport rule» → `documentation-first-golden-rule` (там «паспорт до кода»); в самом `documentation-first-golden-rule.md:13` — «см. раздел «Гейт» этого файла».
3. `knowledge/standards/regression-safety-on-new-feature.md:6` — «tests-mandatory» → `task-testing-all-stages`; `verify-in-ui.md:19` — «guide по verify-skill» → `knowledge/guides/verify-deploy-actually-shipped.md`; `git-branch-discipline.md:104` — «deploy-bundle pitfall, verify-own-deploys» → `scenarios/git-workflow.md` («Деплой через git bundle»), `knowledge/guides/verify-deploy-actually-shipped.md`.
4. `knowledge/standards/enforced-rotation-for-self-stored-transient-data.md:16` — «(ср. persist-streak: write-on-change)» — такого правила в `.profi` нет: убрать скобку или заменить на «(писать при изменении, не каждый тик)» без ссылки.
5. `knowledge/standards/delegate-background-ops-to-cheap-subagents.md:47` — «(правило knowledge-storage)» → «(`knowledge/README.md`, «Что НЕ сюда»)».
6. `knowledge/standards/confirm-understanding-before-acting.md:19` — имена через подчёркивание `treat_owner_as_expert`, `no_confirm_reasonable_nondestructive`, `task_clarification` → `treat-owner-as-expert`, `no-confirm-reasonable-nondestructive`; `task_clarification` — файла в `.profi` нет, заменить на `playbooks/workflow_loop.md` п. 1.
7. `knowledge/standards/autonomous-self-verify-nondestructive.md:9` — «(см. `feedback_autonomous_no_owner_questions`)» — файл памяти; заменить на «(запись в памяти проекта)»; там же `:3, 15, 20` и `scenarios/session-start.md:15` — `prod_escalation.md` → `playbooks/prod_escalation.md`, `validate-recovery-before-destructive.md` → `knowledge/standards/validate-recovery-before-destructive.md`.
8. `knowledge/standards/delegate-documentation-to-persistent-sonnet.md:29`, `documentation-first-golden-rule.md:23` — `agent-admin-server-contract.md` как пример без пометки — добавить «(пример из MDMy)».
9. `knowledge/README.md:26–42` «Формат файла» — ни один из 43 стандартов ему не следует (0 из 43), guides/recipes — 5 из 7: дописать «Формат ниже — для `guides/` и `recipes/`; стандарты идут по форме «Статус · Применимость · Правило · Зачем · Источник»», чтобы правило перестало быть мёртвым.
10. `README.md:23` — перечень сценариев в скобках неполный (нет `session-start`, `big7-consultation`, `no-regression`, `communication`, `task-template`): заменить на «(см. каталог)».
11. `scenarios/git-workflow.md:38` — «`git rebase -i` интерактивный (--no-edit и т.п.) — не поддерживается» — скобка не про интерактивность: убрать «(--no-edit и т.п.)».
12. `roles/hr-d.md:15–19` — список «Допустимы Edit/Write только для» содержит пункт 2 «Read/Grep для маршрутизации», который не Edit/Write: вынести его из списка отдельной строкой.
13. `playbooks/workflow_loop.md:51` — «Формат — см. `roles/big7/INDEX.md`» → `scenarios/big7-consultation.md` (INDEX отсылает туда же).
14. `playbooks/worktree_deploy_safety.md:100–102` — пути с префиксом `.profi/` при том, что весь остальной свод пишет пути от корня библиотеки: убрать префикс (строка 102 — У2).
15. `playbooks/memory_management.md:75` «Краткая версия» Шага 1 — не называет Шаг −1 и Шаг 1.5: дописать «после уплотнения — Шаг −1; набор `.profi` — Шаг 1.5».
16. `roles/big7/qa-02.md:83` и `ux-01.md:103` — перечни ролей продукта («суперадмин, админ тенанта, оператор, читающая роль, аудитор, сотрудник») различаются между собой (у QA-02 есть «аудитор», у UX-01 нет) — привести к одному и пометить «роли — по проекту; перечень — в памяти проекта».
17. `knowledge/standards/rule-expiry-and-ownership.md:4` и `roles/big7/pe-01.md:7, 129` — «`CLAUDE.md`» без пути: это файл проекта, дописать «проекта».

Числа к этому списку: ссылок без файла в `.profi` — 9 имён правил (раздел 1) + 1 вики-ссылка + 1 битая внутренняя (У2, ожидает) + 6 коротких имён без пути.

---

## 6. Ожидает с 19.08 — сводно

| Пункт | Где текст | Вердикт PQ-01 | Что нужно для применения |
|---|---|---|---|
| Г1 — набор памяти как перечень, артефакт `N из M`; §55 «признак соблюдения» | `PE-01-разбор.md:1304–1340`; `PQ-01-разбор-роли.md` §54–55 | принять | правка `session-start.md` Шаг 1 (строки 48–53, 21) + раздел «ОБЯЗАТЕЛЬНО ДО ПЕРВОГО ДЕЙСТВИЯ» в `MEMORY.md` проекта |
| Сироты — «потерянный вход, не отменённое правило» | `PE-01-разбор.md:1340–1352` | принять | в `.profi` не найдено; место — `session-start.md` Шаг 1 или `memory_management.md` |
| Д1 — имя «ядро» вместо числа | `PE-01-разбор.md:1549–1563` | условие принято | исполнитель и один ход по всем нормативным местам (в `.profi` — до 22 файлов, раздел 1) либо не начинать |
| Д2 — `INDEX.md` единственный дом состава, заголовок, в набор гейта | `PE-01-разбор.md:432–462, 1563–1579`; PQ §64 | принять с правкой | переписать `INDEX.md` (таблица из 10 + PE/PQ вне ядра), 12 мест → ссылка; `INDEX.md` в набор — учтено в П9 |
| Д3 — `@UX-01/@UI-01/@QA-02` в формате созыва, предметный артефакт | `PE-01-разбор.md:1579–1592`; PQ §65 | принять с правкой | `big7-consultation.md:11, 50–57` |
| У1 — удалить `phase_a_b_split.md` с двумя ссылками | `PE-01-разбор.md:1629–1643` | принять | `worktree_deploy_safety.md:101` + `memory/reference_profi.md` проекта |
| У2 — ссылка `git_merge_strategies.md` | `PE-01-разбор.md:1643–1657` | принять | `worktree_deploy_safety.md:102` |
| П3 — персистентные агенты против «предел у любого» | находка без текста | — | текст дан: П13 |
| Возражение 2 — пять стандартов одной оси | `PE-01-разбор.md:648–652` | решение владельца | ответ владельца |

---

## 7. Проверка противоречий — что искал и не нашёл

Чтобы «нечего сказать» было отличимо от «не смотрел»:
- **Состав команды:** расхождение 7/9/10 подтверждено (`README.md:21, 36, 38`; `INDEX.md:1, 7, 21`; `big7-consultation.md:11, 50–57`; `prod_escalation.md:47–52` — кворум из семи, что допустимо как минимум) — всё это предмет Д2/Д3, повторно не формулирую.
- **Модели:** после Г2 версий в своде нет (`grep -rn 'opus-4\|sonnet-4\|4\.[0-9]'` → только `session-start.md:36` как перечень разошедшихся имён-доказательство). Семейства (Haiku/Sonnet/Opus) в пяти стандартах делегирования — роли-уровни, не версии; противоречия с Шагом 0 нет, пока действует П10.
- **Ветки:** `main` в своде — только в `git-worktree-branch-hygiene.md:18` как «подставить имя (`main`/`master`)» — верно.
- **Эскалация:** списки `prod_escalation.md` ↔ `hr-d.md` «Политика автономности» ↔ `no-confirm-reasonable-nondestructive.md` ↔ `do-01.md` «Destructive ops» — не противоречат (разные уровни одной шкалы); противоречие только с `deploy_workflow.md`/брифингом (П1).
- **Git-гейт:** направление `master..<ремоут>/master` во всех четырёх входах одинаково (`git-workflow.md:17`, `git-branch-discipline.md:73`, `task-template.md:20`, `backend-engineer.md:28`); `worktree_deploy_safety.md:49` — обратное направление с явным объяснением, что вопрос другой. Противоречия нет.
- **Тесты в задании:** `task-template.md` ↔ `task-testing-all-stages.md` ↔ `regression-safety-on-new-feature.md` — согласованы; расходится только `pm-01.md` (П6).
- **Роли инженеров** ↔ `file-ownership.md` ↔ `INDEX.md` кворум (`BE-01 / FE-01 / AL-01`) — согласованы.

---

## 8. Возражения по существу и вопросы владельцу (текст свода не трогаю до ответа)

1. **Признак «серьёзного задания» №3 неисполним буквально.** `roles-and-briefs-via-pe01.md:17` — «всё, что агент выполняет без пошагового контроля». Любой `Agent(...)` выполняется без пошагового контроля; значит, каждое делегирование требует цикла PE-01 → PQ-01 до запуска, то есть двух дополнительных агентов на каждое задание. Такое требование будет исполняться наполовину и молча (`pe-01.md`, п. 5), а вместе с ним обесценятся первые два признака, которые исполнимы. Указание владельца («новые роли и серьёзные задачи») я не трогаю; прошу владельца уточнить третий признак — например: «задание, затрагивающее прод, устройства или данные, либо рассчитанное дольше чем на одну сессию». До ответа — как есть.
2. **Библиотека универсальная или свод одного проекта?** `README.md:3, 33` и коммит `5d295c4` говорят «универсальная»; при этом 19.08 в три стандарта внесены и приняты PQ-01 проектные указания с датами и адресами (`git-dr-mirror-sync.md:19` «Действует сейчас (MDMy, с 18.08.2026): писать только в GitHub», `git-branch-discipline.md:5, 28–39`, `git-workflow.md:21` — `memory/feedback_push_github_only_temporarily.md`, `gitlab-old`; `worktree_deploy_safety.md:44` — «Ремоутов в этом проекте четыре»). `knowledge/README.md:24` при этом велит: «Специфика конкретного проекта → `memory/` того проекта», а `rule-expiry-and-ownership.md:36` — «в `.profi` остаётся общее правило, в памяти — проектная частность». Это не ошибка одной правки, а вопрос об устройстве: либо (а) библиотека объявляется сводом команды одного проекта с пометкой в `README.md` — и тогда П14 и часть мелких правок не нужны, либо (б) проектные указания переезжают в память с одной строкой-указателем в своде — по уже существующему правилу. Решение за владельцем; предлагать переписывать принятые 19.08 тексты без этого решения не буду.
3. **Запрет Fable** (П10): действует ли? Эта сессия идёт на Fable 5.
4. **Два открытых вопроса PQ-01 от 19.08** (§93, §107), ответа в своде нет: подтвердить переназначение `origin` на GitHub задним числом; до каких пор действует запрет записи в GitLab (от него зависит срок жизни четырёх правок блока 5).
5. **Возражение 2 от 19.08** (пять стандартов на оси «спрашивать или делать») — без ответа; повторяю одной строкой, не переформулирую.

---

## 9. Итог

**Улучшать есть что, и это не вкусовщина.** Четырнадцать правок, меняющих смысл (П1–П14), из них четыре — противоречия с ценой (П1 прод-образы, П2/П3 stale-база и выкладка из изолированной копии, П4 неверная причина в месте, которое читают первым); одна — правило, нарушенное при первом применении (П12); три — класс «правка легла не на все входы» от 19.08 (П4, П5, П7); остальное — мёртвые места и привязка к одной машине. Одно удаление (раздел Doctrine шаблона памяти), семнадцать мелких, семь ожидающих с 19.08, пять вопросов владельцу. По 39 файлам из 89 — нечего править, и это сказано поимённо.

Три самых ценных, по одной строке:
1. **П1** — три места свода велят исполнителю первым шагом сделать то, что четвёртое место называет эскалируемым и ради чего оно написано (`docker system prune -a` сносит образы отката).
2. **П2** — брифинг, который вставляется в каждый промпт, отменяет обязательный предполётный порядок изолированной копии, заведённый после выкладки со stale-базы.
3. **П9** — протокол старта задаёт обязательный набор `.profi` тремя разными множествами в одном файле, с путём одной машины и ссылкой в память одного проекта; дан один перечень с числами и артефактом.

Что после этого: разбор PQ-01 по пунктам; применение — отдельным шагом, одним ходом на все входы каждой правки; строка «Ревизия свода» (П11) — кандидат на первую запись к 26.08.2026.
