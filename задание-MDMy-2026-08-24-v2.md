> **ОТВЕТ ИСПОЛНИТЕЛЯ — `ответ-MDMy-2026-08-24-v2.md`.** Задание выполнено (шаги 0–11), но **центральная посылка опровергнута**: память версионировалась в приватном репозитории MDMy с первого коммита, перенос остановился 19.06.2026. Там же — четвёртый и пятый признаки для правила приёмки, находка о памяти чужих проектов в спасательном фонде, внутреннее расхождение шага 3 и обоснование, почему шаг 9 выполнен иначе.

# Задание ИИ-команде проекта MDMy (v2) — восстановление памяти после утраты 22.08 + хвосты циклов `.profi`

**От:** владелец, через HR-D и PE-01 (`.profi`-сессия 24.08.2026). **Составил:** PE-01. **Разбор PQ-01:** текст этого задания проходит его разбор до отправки исполнителю; отдельно — согласие PQ-01 на снятие переходных скобок (шаг 8) записано заранее: `.profi/PQ-01-разбор-2026-08-24.md`, «Блок 2», вердикты Б3/Б4 и П9 п. 1 («применяются после предъявления записи»).

**Что случилось и почему v2.** 22.08.2026 деструктивный тест `wipe.go` (`filepath.Clean("/")` на Windows разрешился в корень диска) снёс `~/.claude` целиком — погибла вся память проекта, ~343 файла. Бэкапов не было (File History не настроена, OneDrive пуст, корзина пуста, теневые копии недоступны). Задание v1 (`задание-MDMy-2026-08-24.md`, оставлено как история) исполнитель **правильно остановил на Шаге 0**: гейт «память не та» сработал ровно так, как задуман, — это и есть подтверждение, что проверка умеет отказывать. Уроки катастрофы у тебя уже записаны — `feedback_no_destructive_tests_on_prod_infra_devmachine.md` и `feedback_prove_the_check_can_fail.md`; **это задание их не дублирует и не пересказывает — они действуют.**

**Решение владельца (путь 1):** 17 файлов, отстроенных 22–24.08, признаются **новой памятью**; именные правила и обязательный набор **реконструируются**; каждая реконструированная запись объявляет себя реконструкцией с датой, источниками и сроком подтверждения владельцем — по правилу о восстановленных текстах (`.profi/roles/big7/pe-01.md`, «Перед удалением, сокращением и восстановлением правила», последний абзац).

**Спасательный фонд:** два undelete-прогона, **только чтение, ничего в них не менять и не удалять**:
- **R1** = `D:\_recovery\undelete_regular\Recovery_20260822_114204\Misc\md` (10 898 md-файлов);
- **R2** = `D:\_recovery\undelete_regular\Recovery_20260823_202312\Misc\md` (12 807 md-файлов) и дерево `...\Recovery_20260823_202312\Users\...`.

**⚠ ЛОВУШКА undelete — имя ≠ содержимое.** Undelete подставил чужие кластеры под знакомые имена: `project_backlog_cpe_locate.md` (7534 Б) — бинарный мусор под правильным именем; из семи 4616-байтных копий `MEMORY*` живы две; в R2 из 145 файлов с целевыми префиксами живых — 6–7. **Правило приёмки каждого файла (обязательное, без исключений):** файл считается живым, только если (а) frontmatter (`name`/`type`/`description`) согласуется с именем и назначением, (б) текст связный от начала до конца и завершается законченным предложением или секцией; обрыв на полуслове/полусписке — не прошёл (либо переносится с пометкой «хвост утрачен», как шаг 2.1), (в) нулевых байтов — 0 (`grep -c $'\x00'` или эквивалент). Одинаковый размер копий ≠ одинаковое содержимое. Не прошёл любую из трёх проверок — файл не переносится, имя заносится в отчёт как «погиб».

**Границы.** Правишь: память проекта (`memory/`, `MEMORY.md`, `CLAUDE.md` проекта), `docs/trackers/` репозитория MDMy (шаг 2.4) и — в шаге 8 — четыре named-строки `.profi`. Ничего сверх: ни код продукта, ни трекер задач, ни R1/R2 (read-only), ни прочие файлы `.profi`. Деструктивных действий в задании нет; деструктивные тесты — под запретом твоего же `feedback_no_destructive_tests_on_prod_infra_devmachine.md`. Обнаружил, что нужно больше, — стоп и вопрос, а не расширение объёма (`.profi/knowledge/standards/roles-and-briefs-via-pe01.md`; шкала — `ask-or-act.md`, ступень 3).

---

## Шаг 0 — ГЕЙТ: работаем с НОВОЙ памятью

1. Назови путь памяти твоей сессии. Ожидается: `C:\Users\orlovmy\.claude\projects\C--Users-orlovmy-Projects--local-other-Claude-Code-MDMy\memory`.
2. Предъяви: число файлов (`ls *.md | wc -l` ≥ **17**) и наличие **`memory-loss-2026-08-22.md`**.
3. Путь другой, файлов меньше, либо `memory-loss-2026-08-22.md` отсутствует — **СТОП**, одна строка владельцу: «память не соответствует вводной v2» + путь и число.

**Артефакт:** первая строка отчёта — `ПАМЯТЬ v2: <путь> · файлов: <N> · memory-loss-2026-08-22.md: есть`.

## Шаг 1 — Обновить `.profi`

`git -C <путь к .profi> pull` → `git rev-parse origin/main` = **`8252662`** (или потомок — назвать). **Артефакт:** хэш.

## Шаг 2 — Спасение подлинников из R1/R2 (каждый — через правило приёмки)

**Проверь доступность обоих прогонов: `ls R1`, `ls R2` (≈10 898 и 12 807 md-файлов). Недоступны — STOP, одна строка владельцу: „спасательный фонд недоступен: <путь>". Шаги 2–3 без фонда невыполнимы (источники И и В лежат в R1); о шагах 5–7, от фонда не зависящих, спроси владельца отдельно.**

Перенести в память (суффиксы `_N` убрать; в каждый файл — пометка первой строкой после frontmatter: «Восстановлен undelete из `<полный путь источника>`, 24.08.2026; приёмка: frontmatter согласован, текст связный, нулевых байтов 0»):

1. **`R1\prod_escalation.md`** (4175 Б) → `memory/feedback_prod_escalation.md`. Единственный полный подлинник именного правила («Production escalation — STOP → Big 7 → user-approve»). Последние ~2 строки затёрты hex-мусором — мусорный хвост отрезать, сверить оставшийся текст с `.profi/playbooks/prod_escalation.md` и написать в пометке: «хвост (~2 строки) утрачен при восстановлении; соответствует ли утраченный хвост финальным строкам эталона — утрата восполнима по `.profi` при подтверждении владельцем».
2. **`R2\feedback_week_broad_authorization_2026-08-16.md`** (4292 Б, цел полностью, «ПРОДЛЕНО владельцем до 2026-10-01») → как есть. Это действующее разрешение со сроком — обязательный набор без него слеп.
3. **`R1\checkin-update_13.md`** (13 268 Б) → `memory/recovered_index_fragment_2026-08.md`. Это обрезанный подлинник индекса памяти, восстановленный под чужим именем: перечисляет **все девять именных правил** с точными именами файлов и краткими формулировками (стр. 13, 24–25, 30). Пометка: «фрагмент погибшего MEMORY.md, начало утрачено; источник реконструкции шага 3, сам по себе не действующий указатель».
4. **Трекер паритета** — `R1\endpoint-central-parity-program_5.md` (525 426 Б, 4043 строки, записи до 19.08, md5 идентичен в R1 и R2) → **`docs/trackers/endpoint-central-parity-program.md` в git-репозиторий MDMy, НЕ в память.** Почему не в память: (а) это живой трекер программы, а не правило — по `.profi/knowledge/guides/compact-survivable-autonomous-task.md` живой трекер лежит в репо/доке проекта, в памяти — якорь; (б) 525 КБ в каталоге, который читают при старте, — треть контекста, та самая неисполнимость И1 из разбора 19.08; (в) в git он переживёт следующую катастрофу (урок шага 9). В `MEMORY.md`, раздел «RESUME / активные программы», — строка-указатель на него с пометкой «записи после 19.08 утрачены; актуальность сверить». Рядом: `R1\parity-assessment-2026-08-16_5.md` (12 768 Б, цел) → `docs/trackers/parity-assessment-2026-08-16.md`; `R1\dc2-audit-tracker_5.md` (53 860 Б, живой трекер DC-2) → `docs/trackers/dc2-audit-tracker.md`.
5. **Уникальные живые из R2:** `project_service_groups_hidden_2026_08_18.md` (2253 Б), `project_session_2026-05-23_extended.md` (2190 Б) → в память как есть.
6. **Июньский набор из R1** (8 `feedback_*`, 13 `project_*`, 3 `todo_*`, 2 `ai_hr_*`, `hr_director_profile`) — переносить **только прошедшие приёмку**; известно заранее: `project_backlog_cpe_locate.md` — мусор (в отчёт как «погиб»); листинг R1 показывает эти файлы целыми по размеру, но R2-проверка тех же имён дала мусор — размер не аргумент, решает только приёмка по содержимому. Живые копии могут лежать под суффиксами (`feedback_naming_1.md` жив при мусорной `feedback_naming.md` — прецедент R2): проверяй все копии имени, бери живую.
7. **`MEMORY_1.md`** (4616 Б, июньский индекс, md5 `94081e67…`, три чистые копии) → `memory/archive_memory_index_2026-06.md` с пометкой «архив: индекс июньской памяти; НЕ действующий указатель — действующий строится в шаге 4». В сам `MEMORY.md` его не вливать: он описывает состав, которого больше нет.

**Артефакт шага:** таблица в отчёте — `имя → источник (полный путь) → байт → вердикт приёмки (жив/погиб + чем доказано)`; плюс контрольный прогон по всем перенесённым: нулевых байтов — 0.

## Шаг 3 — Реконструкция восьми именных правил

Тексты восьми из девяти именных правил погибли (поиск по обоим прогонам — и по именам, и по содержимому: не найдены). Реконструировать каждое заново, по три источника на правило:

**(И)** — формулировка из спасённого индекса (`recovered_index_fragment_2026-08.md`, шаг 2.3);
**(Р)** — цитаты из разборов 19.08 в `.profi` (точные строки ниже);
**(В)** — выжимки из рабочих файлов памяти в R1 (файлы и строки ниже; каждый файл-источник — тоже через правило приёмки).

Общие для всех восьми строки разборов: `PE-01-разбор.md:1275–1294` — таблица десяти именных (имя, объём в строках, суть и цена нарушения), `:1310` — стартовый состав, `:1330–1332` — тройка необратимых с ценой; `PQ-01-разбор-роли.md:783–788, 798, 816–819, 924–926` — независимые краткие формулировки PQ-01.

| Правило | (И) формулировка индекса | (Р) строки разборов | (В) выжимки в R1 |
|---|---|---|---|
| `feedback_cpe_device_hard_rules` | «CPE HARD RULES» | PE:1285, 1330; PQ:783, 924 | `device-side-stack_10.md:34–35` (полный перечень: no pipes → SIGPIPE→ребут; `.sh` только LF; EDL-only flash, не fastboot; reboot/flash только на модеме, не на хосте; анти-кирпич в root_daemon; красная зона IMEI/IMSI; термо-потолок ~100°C); `edl-wipe-reset-failure_14.md:58–59` (+ «тест только на железе с owner»); `cpe-rearchitecture-program_11.md:10` |
| `feedback_no_restart_without_approval` | «нет рестарта без approval» | PE:345 (цитата: «перед любым `docker compose up --force-recreate` … дождаться явного „да“»), 880 (28.05, «HARD RULE: перезапуск прод-сервисов»), 883–891 (лестница разрешений); PQ:784, 926 | `docker-build-egress-dns_15.md:41`; `dc2-audit-tracker_17.md:116, 145`; `exe-provisioner-autoupdate_15.md:54` |
| `feedback_production_no_disruption` | «прод без disruption» | PE:384, 879 (19.05, цитата), 903 (строка 18: «Любое устройство в БД = production. Тестовых машин больше нет» — **перекрыта с июня**, переносить как историю с пометкой, см. PE:903–920); PQ:785, 356–364 | `apk-flavor-split_16.md:185–189` (гейт APK); `ROADMAP_13.md:203` (BSOD/reboot ban); `services-processes-control_5.md:85`; `TRACKER_10.md:221, 433, 742` |
| `feedback_destructive_commands` | «разрушительное — с согласия» | **Особый случай: правило уже реконструировалось 19.08** — `PQ-01-разбор-роли.md:559–627` (§35–38: вердикт по содержанию восстановленного текста, допустимость восстановления по косвенным данным), `PE-01-разбор.md:1288, 2140–2141` (5173 Б на 19.08). Реконструировать по этим вердиктам — это самый документированный из восьми | `git-workflow.md:36–45` и `do-01.md:34–42` (слабые — это копии текстов `.profi`, не памяти; использовать только как перечень операций) |
| `feedback_test_device_identity` | «тест vs рабочее устройство» | PE:1289; PQ:788, 382 (признак: `is_test_lab`, «-Test-» в имени) | `remote-screen-session-security_15.md:47` («TLab-PC001 (ef7e3652) — РЕАЛЬНОЕ, НЕ трогать»); `binary-rename-mdmd-to-mdmy_15.md:9, 57` («TLab-Test-01 — тест, можно всё; TLab-PC001 — реальный, read-only без согласия») |
| `feedback_never_touch_airplane_mode` | «не трогать airplane mode» | PE:1290; PQ:816 | `reboot-logs-and-watchdog_13.md:49, 68`; `checkin-reliability_11.md:54` (штатная альтернатива: «svc data disable → 5с → svc data enable, БЕЗ авиарежима») |
| `feedback_no_auto_update_prod_group` | «прод без авто-обновления» | PE:1291; PQ:817 | `test-lab-autorollout_21.md:3, 23` («вне тест-групп — только finish; гейтинг по `is_test_lab`») |
| `feedback_inaccessible_only_bootproven_builds` | «недоступным — boot-proven» | PE:1292; PQ:818 | `agent-admin-server-contract_15.md:163` («build128 = boot-proven .good → тест-юниты → тест-флот по owner-go») |

**Форма каждого реконструированного файла** (по `.profi/roles/big7/pe-01.md`, обязательна):
- первая строка тела и `description` frontmatter: «**РЕКОНСТРУКЦИЯ после утраты памяти 22.08.2026** (собрано 24.08.2026). Источники: <перечень — файлы и строки из таблицы>. Добавлено по смыслу, в источниках не было: <перечень или „ничего“>. **Подтверждение владельцем — до 07.09.2026**; нет ответа — правило продолжает действовать как реконструкция, а не превращается в оригинал молчанием.»;
- ничего не смягчать против источников: где источники расходятся — брать строгий вариант и помечать расхождение;
- устаревшее внутри источников (пример: «тестовых машин больше нет») — переносить как датированную историю с пометкой «перекрыто», не как действующее.

**Артефакт:** 8 файлов; в каждом `grep -c 'РЕКОНСТРУКЦИЯ после утраты'` → 1; в отчёте — по каждому правилу список использованных источников и что «добавлено по смыслу».

[Пометка PE-01, 24.08.2026: артефакт шага задан как grep-счёт 1, а форма шага даёт 2 — декларация и в `description`, и первой строкой тела; исполнитель держал форму, артефакт не сошёлся — расхождение артефакта, не исполнения (`ответ-MDMy-2026-08-24-v2.md`, раздел 7).]

Тем же порядком — **девятый файл:** `feedback_compact_reload_profi.md` (погиб, в undelete не найден) — реконструкция из дословного пересказа случая в `.profi/scenarios/session-start.md:21` (рецидив 06.07.2026) и `PE-01-разбор.md:1055` (прямая цитата строки 10 погибшего файла в таблице пяти входов гейта; контекст — `:1055–1084`); та же форма объявления и срок.

## Шаг 4 — Новый раздел «ОБЯЗАТЕЛЬНО ДО ПЕРВОГО ДЕЙСТВИЯ»

По каркасу `.profi/templates/memory_index.md`. **Честно назвать НОВЫМ перечнем** — это не восстановление старого набора из 78 файлов (тот погиб вместе с памятью; его определение — история, `PE-01-разбор.md:1304–1340`). Состав нового:
- 8 реконструированных (шаг 3) + `feedback_prod_escalation.md` (подлинник, шаг 2.1);
- `feedback_week_broad_authorization_2026-08-16.md` (действующее разрешение, срок 01.10.2026);
- `memory-loss-2026-08-22.md`, `feedback_no_destructive_tests_on_prod_infra_devmachine.md`, `feedback_prove_the_check_can_fail.md` — правила, чьё нарушение уже стоило всей памяти;
- прочие действующие разрешения/запреты со сроком, если есть в 17 файлах.

Дата раздела; порядок ведения («правило, чьё нарушение стоило инцидента, вносится тем же ходом, что запись об инциденте») и признак соблюдения (дата раздела не старше свежайшей записи об инциденте) — из каркаса. **Артефакт:** строка `ГЕЙТ память: N из M` следующего старта, `N == M`; в шапке раздела — «перечень НОВЫЙ, после утраты 22.08; прежний набор (78) — история».

## Шаг 5 — Датированные записи решений владельца 24.08.2026

Источник каждой: «ответ владельца через HR-D, `.profi`-сессия 24.08.2026; записи — `.profi/PE-01-разбор-2026-08-24.md`, раздел 8 и блок 2». Записать (файлы погибли — заводятся заново, дублей не будет; `backlog-dedup` всё равно проверить по 17 живым):
1. **GitLab — запрет постоянный:** писать только в GitHub; `gitlab-old` — только чтение/архив (18.08 приостановка → 24.08 навсегда). Прежний `feedback_push_github_only_temporarily.md` погиб — новую запись назвать без «temporarily» (например `feedback_github_only_permanent.md`) и в ней упомянуть имя погибшего файла: на него ссылаются старые тексты.
2. **`origin` = GitHub** — переназначение 19.08 подтверждено владельцем задним числом.
3. **Запрет Fable снят** (12.07 → снят 24.08); модели — ярусами (`.profi/scenarios/session-start.md`, Шаг 0).
4. **Признак №3 серьёзного задания** уточнён (`.profi/knowledge/standards/roles-and-briefs-via-pe01.md`).
5. **«Проектное — в проекте»** — статус прямого указания (`.profi/knowledge/README.md`).
6. **Пять стандартов оси сведены** в `.profi/knowledge/standards/ask-or-act.md`; старые имена — ссылки.

**Артефакт:** перечень файлов с датами; grep по памяти: предписаний «вернуть двойной push» — 0.

## Шаг 6 — Долг уборки веток: запись со сроком

Замер 19.08: **226 влитых при 239 локальных** (норма ≤1–2). Репозиторий MDMy пережил катастрофу (снесён был `~/.claude`, не `Projects`) — **пересчитай на сегодня** скриптом аудита из `.profi/knowledge/standards/git-branch-discipline.md` (локальная половина) и запиши: число, дата замера, срок уборки либо «кем и до какого числа отложена». **Артефакт:** файл:строка записи + свежее число из команды.

## Шаг 7 — `reference_profi.md` заново

Погиб, в undelete не найден. Завести заново: строка-указатель на библиотеку `.profi` (путь из `CLAUDE.md`), её структура одним абзацем по `README.md` библиотеки, пометка «заведён заново 24.08.2026 после утраты; прежний текст не восстановлен». Ссылки на `phase_a_b_split.md` не включать — файл удалён из `.profi` 24.08 (`scenarios/parallel-agents.md` — действующий адрес). **Артефакт:** файл + строка в `MEMORY.md`.

## Шаг 8 — Снятие переходных скобок в `.profi` (гейт!)

**Гейт:** только после шагов 4–6, и в сообщении коммита привести файл:строку записей шага 5 п. 1 и шага 6. Согласие PQ-01 записано заранее (`.profi/PQ-01-разбор-2026-08-24.md`, Б3/Б4, П9 п. 1). Точные правки — только эти четыре строки:

1. `.profi/knowledge/standards/git-dr-mirror-sync.md:27` — **удалить строку целиком**: «(Переходно: в MDMy на 24.08.2026 действует — писать только в GitHub, GitLab — только чтение/архив; решение владельца 18.08 + 24.08. Скобка снимается ходом, которым запись подтверждена в памяти проекта.)» Остальной раздел не трогать.
2. `.profi/knowledge/standards/git-branch-discipline.md:37` — **удалить строку целиком**: «(Переходно: в MDMy на 24.08.2026 адрес записи — GitHub; GitLab — только чтение/архив, решение владельца 18.08 + 24.08. Скобка снимается ходом, которым запись подтверждена в памяти проекта.)»
3. `.profi/knowledge/standards/git-branch-discipline.md:57` — **удалить только хвост строки**: « (Переходно: до подтверждённой записи долга в памяти MDMy этот пример — единственная его запись; скобка снимается ходом, которым запись подтверждена.)» Всё до него — дословно как есть.
4. `.profi/scenarios/session-start.md:76` — **удалить только фрагмент**: «(Переходная скобка: в MDMy на 24.08.2026 это `C:\Users\orlovmy\Projects\.profi`; скобка снимается тем же ходом, которым путь проверен или добавлен в `CLAUDE.md`/`MEMORY.md` проекта.)» — **сначала** проверь/добавь в `CLAUDE.md`/`MEMORY.md` проекта пункт «при старте подгрузить `.profi` из <путь>» (поручение владельца 08.06.2026), потом снимай.

Коммит в `.profi` (main): «MDMy: сняты переходные скобки — записи в памяти проекта <файл:строка>, <файл:строка>» + push в GitHub; в три правленых файла — строка «История правок: задание MDMy v2, шаг 8; записи — <файл:строка>». **Артефакт:** хэш; `grep -rn 'Переходн'` по трём файлам → 0.

## Шаг 9 — Урок катастрофы: память — под версионирование

> **Пометка PE-01, 24.08.2026 — после исполнения; текст шага ниже оставлен как история и не правится.** Посылка «память лежала вне git» **опровергнута исполнителем 24.08.2026**: приватный git-репозиторий памяти существовал с первого коммита проекта — встал не репозиторий, а **ручной перенос** в него (последняя фиксация `d85e2c84`, 19.06.2026, за два месяца до катастрофы 22.08). Шаг исполнен иначе и лучше предложенного здесь «push в конце сессии» — тот был бы ещё одним ручным шагом того же класса, что умерший: построены `infra/scripts/sync-memory.sh` (секреты → копирование → фиксация → push) и шлюз памяти в `deploy.sh` — выкат отказывает кодом 4, если файл живой памяти недостижим из удалённых веток. Механизм привязан к действию, которое человек и так обязан совершить (выкат — единственный путь на прод), а не к памяти человека. Разбор ошибки посылки — `.profi/PE-01-разбор-2026-08-24.md`, «Поправка к блоку заданий MDMy, 24.08».

**Причина.** Живая память (~343 файла) лежала вне git — единственной копией на диске, который один тест стёр целиком; двух undelete-прогонов хватило на **2 подлинника из 9 именных правил**. Правило `.profi` о DR-зеркале (`git-dr-mirror-sync.md`) защищало код и не защищало память — самое дорогое из погибшего. (Уроки о причине катастрофы уже записаны — `feedback_no_destructive_tests_on_prod_infra_devmachine.md`, `feedback_prove_the_check_can_fail.md`; этот пункт — о **выживаемости**, не о причине.)

**Предложение (развилка — решает владелец, `ask-or-act` ступень 3):** каталог памяти — git-репозиторий с приватным remote на GitHub; push — в конце каждой сессии тем же ходом, что обновление `MEMORY.md` (правило-производитель, а не «когда вспомнили»). Запасной вариант — регулярная копия на второй диск, хуже: копию не по расписанию забывают, у git есть история и уже описанный в `.profi` механизм зеркал.

**До ответа владельца — никакого remote и никакого push: `git init` локален и обратим. В `.gitignore` сразу — паттерны секретов; перед первым push — прогнать по памяти grep форматных паттернов секретов (классы — как в приёмке SEC-02, `.profi`-сессия 24.08) и исход назвать владельцу вместе с вопросом.**

**Что сделать:** подготовить `git init` + `.gitignore` для каталога памяти, задать владельцу один вопрос: «приватный GitHub-репозиторий для памяти MDMy — да/нет?» После «да» — создать remote, первый push, в `MEMORY.md` — строка «память версионируется: <remote>; push в конце каждой сессии». **Артефакт:** хэш первого push + строка в `MEMORY.md`; признак несрабатывания на будущее: грязный `git -C <память> status` в конце сессии.

## Шаг 10 — Интерфейсная ветка приёмки С7 (при первой интерфейсной задаче)

Как в v1: агент ядра выдаёт `Проверено: интерфейс — да; …`, `Роль <X> загружена: <файл>, <байт>`, `@UX-01/@UI-01/@QA-02: …` с предметом (`.profi/scenarios/big7-consultation.md`). Результат — **в память проекта** (датированная запись + строка-указатель в `MEMORY.md`); в `.profi/PQ-01-разбор-2026-08-24.md` не дописывать (чужой разбор — не журнал проекта; PQ-01 заберёт запись на ревизии). **Артефакт:** файл записи + строки агента дословно.

## Шаг 11 — Д1 — НЕ обязательное

Как в v1: только с условием 19.08 (`.profi/PE-01-разбор.md:1549–1563`) — «одним ходом по всем нормативным местам (пересчитать своим grep'ом на день хода) либо не выполняется вовсе»; отдельное серьёзное задание через PE-01/PQ-01, не рутина.

---

## Финальный отчёт (форма)

```
ПАМЯТЬ v2: <путь> · файлов: <N> · memory-loss-2026-08-22.md: есть
Шаг 1: .profi = <хэш>
Шаг 2: спасено <N> файлов (таблица: имя → источник → байт → вердикт приёмки); погибло под своим именем: <перечень>; в docs/trackers: <перечень>
Шаг 3: реконструировано 8+1; по каждому — источники и «добавлено по смыслу»; срок подтверждения владельцем: 07.09.2026
Шаг 4: раздел заведён (НОВЫЙ перечень), M = <число>; следующий старт: ГЕЙТ память: N из M
Шаг 5: записи — <файл × 6>
Шаг 6: долг веток — <файл:строка>, свежий замер: <число> влитых из <число> локальных
Шаг 7: reference_profi.md заведён заново
Шаг 8: коммит .profi <хэш>; grep 'Переходн' → 0   (либо: не выполнялся — гейт не пройден, причина)
Шаг 9: вопрос владельцу задан: приватный репозиторий памяти — да/нет; после «да»: push <хэш>
Шаг 10: отложен до первой интерфейсной задачи (или: выполнен, запись — <файл>)
Шаг 11: не выполнялся (условие Д1)
НЕ ТРОНУТО ничего сверх перечисленного: git status по .profi, по репо MDMy и по памяти приложены; R1/R2 не изменялись.
```

Не готово — «ЗАДАЧА НЕ ЗАВЕРШЕНА» и что осталось; «готово» без артефактов — ложный отчёт (`.profi/scenarios/task-template.md`).

**На владельце после выполнения:** подтвердить (или поправить) 8+1 реконструкций — до 07.09.2026; ответить «да/нет» на репозиторий памяти (шаг 9); при желании — заказать Д1 отдельным заданием.
