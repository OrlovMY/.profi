# Сценарий: Консультация Большой семёрки

## Форма созыва — один постоянный агент

Большая 7 — это **один агент**, который созывается в начале сессии **один раз и не закрывается никогда**. HR-D запускает его при первой необходимости и дальше все обращения шлёт этому же агенту (SendMessage), сохраняя его контекст:

- все консультации по нетривиальным задачам;
- **аудит кода** — код на ревью идёт через этого агента;
- **все идеи новых фич** — оценка и приоритизация через него.

Агент отвечает за все роли из `roles/big7/INDEX.md` (состав там, здесь не дублируется), каждая — своим префиксом (`PM-01: ...`, `SEC-01: ...`) и строго в своей компетенции. Накопленный контекст прошлых консультаций — преимущество: агент помнит принятые решения и не противоречит им.

Не плодить отдельных агентов на каждую роль и не создавать нового агента Big 7, пока жив текущий.

## 🔴 Модель Big-7 — САМАЯ СВЕЖАЯ/МОЩНАЯ, пиннить ЯВНО (owner 2026-07-08)

Big-7 = высший орган суждения/аудита/решений → должен работать на **самой свежей и мощной доступной модели**, а НЕ наследовать модель главного цикла по умолчанию. При спавне Big-7 координатор ОБЯЗАН **явно указать модель** (`Agent(..., model: '<freshest>')`), иначе subagent наследует модель координатора (напр. Opus), что может быть НЕ самой свежей.

- 🔴 [⏳TEMP-OPUS-INSTEAD-OF-FABLE: временный жёсткий запрет Fable (owner 2026-07-12, срок НЕ задан); вернуть Fable по этой метке когда owner снимет] Спавнить Big-7 с `model: 'opus'` — **по алиасу, а не по версии**. Алиас всегда указывает на текущий Opus; вписанная версия через месяц пиннит устаревшую модель, и высший орган суждения тихо начинает работать на прошлом поколении. (Формально «самая свежая» могла бы быть Fable, но она временно запрещена owner-директивой — см. метку выше.)
- Правило самообновляемое: «самая свежая доступная», а не хардкод — при появлении новее пиннить её, НО с учётом временного запрета Fable выше (пока не снят — Opus). Сверяться со списком актуальных моделей в системном контексте сессии.
- Отличается от исполнителей: реализация = Sonnet ([[delegate-implementation-to-sonnet]]), retrieval = Haiku ([[delegate-search-to-persistent-haiku]]), а **суждение/аудит/решение (Big-7) = свежайшая флагманская доступная модель (сейчас Opus, см. TEMP-запрет Fable выше)**.
- Грабля (owner 2026-07-08): Big-7 спавнился как subagent_type `claude` → наследовал Opus главного цикла → owner заметил, что он «всегда в Opus», хотя свежее была Fable 5. Впредь — явный `model`-пиннинг при КАЖДОМ спавне/реюзе Big-7 (сейчас пиннить Opus — см. TEMP-запрет Fable выше).

## Когда вызывать

Перед началом любой **нетривиальной** задачи — обсудить с Большой семёркой (состав — `roles/big7/INDEX.md`).

## Что считается «нетривиальным»

- Новая фича, затрагивающая более одного пакета.
- Архитектурное решение (выбор библиотеки, паттерна, схемы данных).
- Изменение safety-критичного кода (auth, crypto, billing, destructive actions).
- Изменение инфраструктуры (compose, nginx, CI/CD, миграции БД на продакшне).
- Изменение публичного API.
- Миграция данных / переезд серверов.

## Что **не** требует Большой семёрки

- Правка опечатки.
- Минорный фикс в своём пакете.
- Добавление логов.
- Переименование локальной переменной.
- Исправление flaky теста.

## Формат консультации

HR-D формулирует задачу и собирает мнения:

```
@PM-01: приоритет задачи, сроки, зависимости от других фич.
@AR-01: архитектурная уместность, альтернативы, trade-offs.
@BE-01: влияние на backend-код, нужны ли новые endpoints.
@SEC-01: security-импликации, authz, secrets, PII.
@DO-01: инфра-требования, миграции, backup, rollback.
@QA-01: как это тестировать, smoke-сценарий.
@SEC-02: что проверить извне после деплоя (post-change regression scope).
@UX-01: где функция живёт — вкладка существующего раздела или новый пункт; не плодим ли параллельный раздел.
@UI-01: все ли надписи из словаря (в обе стороны), пополнены ли оба словаря, различимы ли «нет данных / ноль / ошибка».
@QA-02: что открыть и потрогать руками после выкладки — под какими ролями, какие вкладки и ссылки.
```

Три последних — **обязательны, если изменение видно в интерфейсе**, и пропускаются, только если не видно.

**Артефакт созыва:** в отчёте по изменению интерфейса стоят три строки с этими префиксами, и каждая **называет предмет**, а не «ок»:

```
@UX-01: функция легла в <раздел/вкладку>; параллельного раздела не заведено, потому что <…>
@UI-01: проверены надписи <перечень>; пополнены словари <ru/en>; «нет данных / ноль / ошибка» различимы на <экране>
@QA-02: открыто руками <страницы>, под ролями <перечень>; исходы <…>
```

Нет строки — роль не созывалась, и это видно без расследования. Состав консультации целиком — `roles/big7/INDEX.md`.

Каждый отвечает в пределах своей компетенции **коротко**: блокеры, требования, риски.

## Постдеплой — обязательный шаг

После merge в `master` + деплой:
1. **QA-01** прогоняет функциональный smoke.
2. **SEC-02** — security-regression на затронутые области.
3. Если SEC-02 нашёл **Critical** — roll back или hotfix немедленно.
4. **High** — ближайший sprint; **Medium/Low** — backlog.
5. Решения по всем finding'ам SEC-02 принимает Большая семёрка, не отдельный агент.

## Результат

HR-D сводит мнения и возвращает пользователю:
- Рекомендацию (делать / не делать / делать иначе).
- Риски и план их митигации.
- Кто из команды берёт задачу.
- Ожидаемый план работ.

Если мнения расходятся радикально — HR-D эскалирует пользователю, а не выбирает сам.

---

> **Официальная документация.** Каждый участник консультации по техническому вопросу сверяется с актуальными источниками (Context7 MCP / WebFetch) перед ответом — не с памятью модели.

История правок: `PE-01-разбор-2026-08-24.md`, раздел «Совместная редакция и применение», Д2, Д3 (ожидало с 19.08); вердикты — `PQ-01-разбор-2026-08-24.md`; 2026-08-24.
