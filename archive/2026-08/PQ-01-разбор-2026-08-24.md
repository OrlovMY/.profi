# PQ-01 — разбор предложений PE-01 по библиотеке `.profi`, 2026-08-24

PQ-01 [2026-08-24 01:18]: независимый разбор файла `PE-01-разбор-2026-08-24.md` (HEAD `49610ab`, рабочая копия `profi-review-2026-08-24`). Разбор написан **до** какого-либо обсуждения с PE-01. Ни один файл свода не правлен; результат — только этот файл.

**Короткий вердикт для владельца.** Улучшать есть что, и PE-01 прав, что это не вкусовщина: из четырнадцати правок, меняющих смысл, ни одна не отклонена как вкусовая — по каждой названо, чем старая редакция неверна, и я это подтвердил своими руками. Но **применять как есть нельзя ни одну из «принятых с правкой» (их девять)**: в двух местах PE-01 сам наступил на правило, которое чинит (вводит имя «ядро» по частям, хотя 19.08 условие было «72 места одним ходом или никак»; ссылается на стандарт, не прочитав в нём соседнюю строку с «cap»), в одном — новая редакция брифинга чинит базу, но по-прежнему не запрещает выкладку из изолированной копии, то есть случай, ради которого плейбук написан, ею не ловится. Из семнадцати «мелких» правок **пять переквалифицированы** в меняющие смысл. В числах найдено **семь расхождений**, одно из них существенное (22 против 28 файлов, называющих команду числом, — из-за локали в команде).

---

## 0. Что проверял сам и где проверка кончается

**Выполнял своими руками** (в корне рабочей копии, `LC_ALL=C.UTF-8`): все команды раздела 1 разбора PE-01 — `ls-files | wc`, `wc -l -m -c` по трём наборам, `git show --stat 49610ab`, `grep` по `История правок`, `⏳`, `silently|silent`, `roles-and-briefs-via-pe01`, `HARD` по ролям, `[[…]]`, девяти именам правил, `console/src|docs/passports`, `Больш…|Big…(6|7|9|10)` в двух локалях; `git log` по `a43130e`, `5d295c4`, `templates/memory_index.md`; `git show` по четырём файлам коммита `49610ab`; `curl -sI https://example.com` из этой изолированной копии (для П3).

**Открывал целиком (`cat -n`)**: `prod_escalation.md`, `deploy_workflow.md`, `agent_briefing.md`, `worktree_deploy_safety.md`, `parallel-agents.md`, `git-workflow.md`, `session-start.md`, `hr-d.md`, `pm-01.md`, `task-template.md`, `no-regression.md`, `rule-expiry-and-ownership.md`, `big7-consultation.md`, `agent-question-rendezvous.md`, `roles/big7/INDEX.md`, `templates/memory_index.md`, `phase_a_b_split.md`, `roles-and-briefs-via-pe01.md`, `pe-01.md`, `pq-01.md`. **По строкам, не целиком**: `ux-01.md` (37, 100–120), `ui-01.md` (42), `qa-02.md` (84), `resource_tracking.md` (50–62), `git-branch-discipline.md` (5, 62, 73, 82, 104), `README.md` (3, 21, 23, 33, 36, 38, 53), `knowledge/README.md` (24–42), `memory_management.md` (75), `workflow_loop.md` (1–35, 51), четыре стандарта `delegate-*` (шапки и строки с «cap»), и по одной строке — одиннадцать файлов из списка мелких правок.

**Не читал**: 39 файлов, по которым PE-01 «сказать нечего», — **не перепроверял**, что там действительно нечего сказать; моя проверка распространяется только на то, что PE-01 предложил, а не на полноту его обхода. Прошлые разборы 19.08 — только вердиктную таблицу `PQ-01-разбор-роли.md` (строки 883–886, 1124–1126, 1261–1263, 1495–1508) и строки `PE-01-разбор.md`, на которые ссылается сегодняшний разбор (129, 152–166, 237–242, 1826–1845, заголовки 1275–1643).

**Где кончается проверка и начинается доверие к записи:**
- Случай DO-01 (`prod_escalation.md:79`) и случай stale-выкладки (`worktree_deploy_safety.md:3`) — записи без даты; что именно произошло на сервере, проверить нечем. Вердикты по П1–П3 опираются на запись.
- Память проекта MDMy (`MEMORY.md`, `feedback_*`, `CLAUDE.md`, `reference_profi.md`) в этом репозитории отсутствует; всё, что PE-01 и я говорим о ней, — **по записи 19.08**, не по файлам. Это важно для П9 (куда переезжает путь к `.profi`) и для удаления 1.
- Модель сессии — по строке системного контекста; `/model` из агента недоступен. Я, как и PE-01, работаю на Fable 5 — по той же строке.
- Вердикты PQ-01 от 19.08 (Г1 «принять», Д2/Д3 «принять с правкой») — по вердиктной таблице прошлого файла, не по полному тексту §54–65.

---

## 1. Числа и ссылки — что сошлось и что нет

Повторены все 14 строк таблицы раздела 1 PE-01. **Сошлись дословно:** 89 файлов; 4 342 / 296 585 / 473 450; набор Шага −1 — 60 / 2 674 / 192 842 / 308 321; набор Шага 1.5 — 77 / 3 754 / 265 210 / 429 196; набор П9 — 62 / 2 769 / 199 607 / 318 821; 43+4+3+7+9+4+13+3 по каталогам; `История правок` — только шаблон; `49610ab` — 22 файла; формат `## Условия применения` — 0 из 43 и 5 из 7; `⏳` — 4 строки в 3 файлах, без срока одна; `HARD` в `roles/` — пусто; `silently` — ровно два вхождения с этим смыслом; `git_merge_strategies.md` — нет, `phase_a_b_split.md` — есть; `roles-and-briefs-via-pe01` — вход только `README.md:53`.

**Расхождения (семь):**

| № | Где у PE-01 | У PE-01 | У меня | Чем объясняется | Вес |
|---|---|---|---|---|---|
| 1 | раздел 1, «файлов, называющих команду числом»; Д1 «до 22 файлов» | **22** | **28** | Та же строка `grep -rlE 'Больш[а-яё]*…'` даёт 22 **в локали `C`** и 28 в `C.UTF-8` — в `C` классы `[а-яё]` по многобайтным символам не работают, и кириллическая половина образца ловит не всё. PE-01 объявил `LC_ALL=C.UTF-8` для `wc`, но для этой строки, судя по числу, запускал без неё. Это ровно пункт его же инструкции («число от команды и число от помощника — два разных числа, пока не совпали»). | Существенно для Д1: объём хода по переименованию — не «до 22», а **не меньше 28** файлов `.profi`. |
| 2 | раздел 1, вики-ссылки | 21 имя, 20 есть, 1 нет | **20 имён, 19 есть, 1 нет**, плюс `[[...]]` ×4 | В «21» засчитан плейсхолдер `[[...]]`, хотя он же вынесен отдельно. | Мелко. |
| 3 | П7, «Проверено существованием» | «→ `README.md` и сам стандарт» | Сам стандарт **не содержит** своего имени (`grep -c` → 0) | Неточность описания; вывод «один вход» верен. | Мелко. |
| 4 | Удаление 1, происхождение | `a43130e` — **2026-06-08** | `a43130e` — **2026-06-04** (`git log --date=short`) | Дата 08.06 — это дата поручения владельца об авто-подгрузке (`session-start.md:66`), а не коммита. | Мелко, но в поле «происхождение» дата — единственный проверяемый факт. |
| 5 | П14, «Проверено существованием» | 8 вхождений `console/src|docs/passports` | **14** по той же строке | Пропущены `doc-completeness-fix-on-miss.md:17, 51`, `no-guessing-read-docs-ask.md:7, 16`, `delegate-documentation…:14`. Вывод не меняется (там образцы, не адреса), но число неверно. | Мелко. |
| 6 | П12, «изменил смысл в 19 файлах» | 19 | **≤ 16**: в `communication.md`, `file-ownership.md`, `post-incident-memory-update.md` коммит только снял подвал (У3) | «0 из 19» → «0 из ≤16». Находка стоит. | Мелко. |
| 7 | П8, «4 отсутствуют» (из 9) | 4 (пп. 5, 6, 8, 9) | **5**: в брифинге нет и п. 4 (`nginx config swap без rollback`), а из п. 1 нет «volume operations» | Сопоставление не попунктное. Усиливает П8, не ослабляет. | Мелко. |

Ещё две неточности не в числах: мелкая правка 16 указывает `qa-02.md:83` — перечень ролей на **строке 84**; мелкая правка 1 называет `verify-no-break` только на строках 13 и 30 — оно стоит ещё на **21 и 27** того же файла (правка на два входа из четырёх — класс, который PE-01 сам называет худшим, чем отсутствие правки). Раздел 7: «`main` в своде только в `git-worktree-branch-hygiene.md:18`» — файл лежит в `knowledge/recipes/`, не в стандартах, и `main` есть ещё в `rule-expiry-and-ownership.md:52` и `file-ownership.md:17` (в другом смысле); на вывод не влияет.

Оценка в токенах (55–106 тыс.) — не проверял: токенизатора нет у меня так же, как у него; PE-01 честно пометил как оценку.

---

## 2. Правки П1–П14 — вердикты

Форма: вердикт · названо ли, чем **старое** неверно (требование владельца — не «ради видимости») · ослабление безопасности · текст правки, где нужен.

### П1 — прочистка образов предписана без эскалации в трёх местах. **Принять с правкой.**

**Чем старое неверно — названо и подтверждено:** `deploy_workflow.md:10, 20, 92` и `agent_briefing.md:42, 47` велят `docker system prune -af` первым шагом; `prod_escalation.md:13` называет любой `docker prune` и удаление образов эскалируемым, `:79` — случай, когда именно это снесло откат. Брифинг читают первым (`agent_briefing.md:3`). Противоречие с ценой — подтверждаю.

**Безопасность:** новая редакция усиливает, не ослабляет. Но две вещи PE-01 не увидел, и обе — в том самом файле, который он «прочитал целиком»:

1. **`prod_escalation.md:18`: «Любая команда с `-f` / `--force` / `--volumes`» — production-affecting.** Новый текст предписывает `docker builder prune -f` без эскалации. Исполнитель, читающий список буквально, получает две команды свода в лоб. Без `-f` команда спрашивает подтверждение и агент виснет, так что флаг нужен, — значит, нужна оговорка.
2. **`prod_escalation.md:29` разрешает build-cache prune «если согласовано как routine»** — оговорка выпала. Согласование есть: `resource_tracking.md:58–59` (cron weekly builder prune и «pre-deploy prune обязательным шагом» — оба за DO-01). На неё надо сослаться, а не опустить.
3. **Слово «ядро».** 19.08 Д1 («ядро» вместо числа) отклонена с условием «одним ходом по всем нормативным местам или никак» (`PQ-01-разбор-роли.md:1124`). Новые тексты П1 вводят «эскалация в ядро» в двух файлах — это и есть полумера, дающая два имени одной команды. До хода Д1 — «Big 7».

**Текст (заменяет «Стало» PE-01 в трёх местах):**

`deploy_workflow.md:10, 20`:
> - Должно быть **< 70% used**. Если выше — сначала то, что разрешено без эскалации: `docker builder prune -f --filter "until=72h"` — кэш сборки, образы не трогаются (`playbooks/prod_escalation.md`, «Что НЕ требует эскалации»; согласовано как routine — `playbooks/resource_tracking.md`, «Триггеры оптимизации». Флаг `-f` здесь — отказ от интерактивного подтверждения, а не force поверх данных; пункт 6 списка production-affecting про него не говорит). Проверить `df -h /` ещё раз. Если всё ещё выше 70 % — **STOP, эскалация в Big 7 через HR-D** (`prod_escalation.md`, пункт 1: прочистка образов и томов — production-affecting, потому что сносит образы для отката). Только после этого — миграции и build.
> - Reclaimable < 2 GB. Иначе — тот же порядок: кэш сборки сам, образы — через эскалацию.

`deploy_workflow.md:92`:
> 2. `docker builder prune -f --filter "until=72h"` (кэш сборки — без эскалации, см. §1). Не хватило — **STOP, эскалация в Big 7**: `docker system prune -a` / `image prune` / `--volumes` — пункт 1 списка production-affecting, решает Big 7, а не исполнитель (случай DO-01 — `prod_escalation.md`, «Почему правило существует»).

`agent_briefing.md:42, 47`:
> - Pre-flight: `df -h /` must be <70%; if higher → `docker builder prune -f --filter "until=72h"` (build cache only; `-f` = no interactive prompt, this is the routine exception in `prod_escalation.md`). Still >70% → STOP, escalate (image/volume prune is production-affecting, see PROD-AFFECTING DECISIONS above).
> - On ENOSPC: STOP; build-cache prune only; anything that deletes images or volumes → escalate; then retry migration (build cache is idempotent).

### П2 — брифинг велит «просто `git checkout -b`» в изолированной копии. **Принять с правкой.**

**Чем старое неверно — названо и подтверждено:** `agent_briefing.md:20` «don't worry about parent repo» против `worktree_deploy_safety.md:20, 42` (обязательно, СТОП при расхождении); `task-template.md:18–21` даёт третий порядок через ремоут, который плейбук для копии запрещает. Что `templates/` не читались 19.08 — подтверждено (`PE-01-разбор.md:129`).

**Чего не хватает — и это самое дорогое место разбора.** `worktree_deploy_safety.md:77–86` требует: «В брифинге worktree-агенту ОБЯЗАТЕЛЬНО» блок Permissions с `⛔ ЗАПРЕЩЕНО: docker compose build/up, cp в статику, git push origin…`. **В `templates/agent_briefing.md` этого блока нет** — ни в старой редакции, ни в новой редакции строки 20. Новая строка чинит базу и не запрещает выкладку. Случай из шапки плейбука — агент **выложил** со stale-базы; по новой редакции агент со свежей базой по-прежнему может выложить, и брифинг ему этого не запрещает. Это не довесок, а половина правила: плейбук написан после выкладки, а не после неверной базы.

**Текст (`agent_briefing.md:20`, заменяет «Стало» PE-01):**
> - If `isolation:"worktree"` — the worktree is set up for you, but its base is NOT trusted: take it from the main working directory by the absolute path named in your task, then verify it twice — `git fetch "<absolute path to main working dir>" master && git checkout -b <branch> FETCH_HEAD && git rev-parse --short HEAD` (must equal the hash named in your task) and `ls <marker file from task>` (must exist). Hash differs or marker missing → STOP and report. Do NOT branch from `origin/master` inside a worktree. **From a worktree you NEVER deploy:** no `docker compose build/up` on the server, no `cp` into static/staging dirs, no `git push origin`, no cherry-pick «for sync», no `git reset --hard`; SSH to prod — read-only diagnostics only. Deploy = coordinator after merge. (`playbooks/worktree_deploy_safety.md` — written after a worktree agent rebuilt and deployed prod from a stale base.)

Вставка в `task-template.md` после строки 21 — принять как есть.

**Безопасность:** усиление. Оговорка для координатора (`:63` — ветку забирать из главного каталога самому) в брифинг исполнителя не нужна, она адресована HR-D и лежит в плейбуке.

### П3 — `parallel-agents.md` строит правило на «песочница блокирует сеть». **Принять с правкой.**

**Чем старое неверно — названо и подтверждено, причём делом:** из **этой** изолированной копии `curl -sI https://example.com` вернул `HTTP/1.1 200 OK`. Сеть есть. Правило «не сможешь» — ложная гарантия; PE-01 прав, что «нельзя» сильнее «не сможешь».

**Правка обязательна по двум причинам:**
1. **Правка легла не на все входы.** `parallel-agents.md:78` — «Один deploy-агент **С** worktree → не сможет scp/ssh» — та же ложная гарантия, PE-01 её не тронул (правил 15–17 и 76). Три входа в одном файле, правка на два.
2. **Новый текст ослабляет Phase A.** Строки 27, 30, 59 того же файла велят промпту Phase A запрещать **любой** SSH («Не выполнять SSH/scp/прод-команды»). Новый раздел говорит «read-only SSH — можно» со ссылкой на плейбук. Плейбук действительно разрешает read-only SSH (`worktree_deploy_safety.md:23, 82`), но промпт Phase A — более строгое требование того же файла, и заменять его ссылкой на менее строгое — то самое «исполнитель выберет удобное». Строгое остаётся.

**Текст (`parallel-agents.md:15–17`, заменяет «Стало» PE-01):**
> ## Критическое ограничение: изолированная копия не выкладывает
>
> Coding и deploy не совмещаются в одном worktree-агенте — **по правилу, а не потому, что песочница это не даст.** Сеть из изолированной копии бывает доступна: агент в worktree уже пересобирал и выкладывал бандл на прод со stale-базы (`playbooks/worktree_deploy_safety.md`, шапка); 24.08.2026 `curl` к внешнему хосту из изолированной копии прошёл. Поэтому промпт Phase A **запрещает SSH/scp/прод-команды словами** (ниже, «Phase A only») и на блокировку не рассчитывает. Что можно и чего нельзя из изолированной копии, когда SSH всё же нужен для read-only диагностики, — `worktree_deploy_safety.md`, «Что НЕЛЬЗЯ» / «Что МОЖНО»; но это исключение координатор выдаёт явно в брифинге, по умолчанию его нет.

Строка 76 — принять текст PE-01. **Строка 78** → «Один deploy-агент **С** worktree — запрещён правилом (`worktree_deploy_safety.md`), а не песочницей: даже если SSH прошёл, выкладка из копии откатывается. Phase B — всегда без isolation.»

### П4 — причина «fetch падает silently» осталась в двух входах. **Принять.**

Подтверждено: `git-workflow.md:56` против `:72` того же файла; `agent_briefing.md:43`. Образец 19.08 (`PE-01-разбор.md:1828`) включал `silently` и `templates` — и всё же два входа пропущены; это довод не только за П4, но и за то, что запись «ПРИМЕНЕНО, три входа» 19.08 была неверна (входов было пять). Чем старое неверно — названо (неверная причина в месте, которое читают первым). Безопасность — усиление (шлюз `git log -3` назван явно в брифинге). Тексты — как у PE-01.

### П5 — роль координатора велит читать «релевантные файлы». **Принять.**

Подтверждено: `hr-d.md:32` против `session-start.md:52, 92`; три рецидива на строках 15, 17, 21 с датами 17.06, 01.07, 06.07. Чем старое неверно — названо. Текст — как у PE-01; он не задаёт меру (правильно — она ожидает по Г1), а отсылает к протоколу.

### П6 — у PM-01 свой шаблон задания короче обязательного. **Принять с правкой** (одна дата).

Подтверждено: `pm-01.md:17–26` без гейта и без «Тестов по этапам»; `task-template.md:60` обязывает PM-01 отклонять такое. Чем старое неверно — названо (дубль с меньшим объёмом, `rule-expiry…` п. 4). Правка: в тексте «на 19.08.2026 укороченная копия…» — дата наблюдения **24.08.2026** (установлено сегодня; 19.08 `pm-01.md` не открывали). Случай честно назван отсутствующим.

### П7 — кто пишет задание: три ответа, стандарт о PE-01 без входа. **Принять.**

Подтверждено: единственный вход — `README.md:53`; `hr-d.md`, `pm-01.md`, `task-template.md`, `workflow_loop.md:27–35` слова «PE-01» не содержат. Чем старое неверно — названо (правило без входа из места срабатывания; `rule-expiry…` п. 3). Тексты — как у PE-01. Вопрос об исполнимости третьего признака правильно вынесен владельцу (раздел 8), текст стандарта не тронут.

### П8 — брифинг перечисляет production-affecting неполным списком. **Принять с правкой.**

Подтверждено, и недосчитано: отсутствуют не 4 пункта, а 5 (ещё п. 4 — nginx swap без rollback) плюс «volume operations» из п. 1. Чем старое неверно — названо (`pe-01.md`, п. 2: неполный список хуже отсутствующего).

**Почему с правкой.** PE-01 выбрал вариант «ни одного, и отсылка к файлу». Но `pe-01.md:99`: «сокращением считается и переезд обязательного за пределы того, что читают». Брифинг — то, что исполнитель читает наверняка (он в промпте); `playbooks/prod_escalation.md` — то, что читает, если дошёл и если `.profi` у него вообще доступен. Девять пунктов — короткий список, и правило «либо все, либо ни одного» выполняется первым способом без потери.

**Текст (`agent_briefing.md:24`):**
> - ANY action on shared production infra → STOP, report to HR-D as "ESCALATING TO HR-D" with proposal + reversibility cost. Production-affecting is (full list = `playbooks/prod_escalation.md`, «Что считается production-affecting»): (1) any `docker prune`, volume ops, image deletion; (2) migration retry after error, manual DB fix, `ALTER`/`DROP`; (3) restart cascade (≥2 containers); (4) nginx config swap without `nginx -t`/rollback; (5) pushing a client/agent update to >1 device; (6) any command with `-f`/`--force`/`--volumes` (except the build-cache prune named in DEPLOY WORKFLOW); (7) touching credentials, secrets, certs; (8) disabling/relaxing security middleware «temporarily»; (9) changing data in the prod DB. If unsure whether it counts — it counts.

### П9 — протокол старта задаёт набор `.profi` тремя способами, с путём одной машины и ссылкой в память одного проекта. **Принять с правкой.**

**Чем старое неверно — названо и подтверждено:** 60 (строки 19, 21) против 77 (строка 63), «актуальные» не определено; абсолютный путь `C:\Users\orlovmy\…` в файле, который `README.md:33` объявляет переносимым; `[[feedback_compact_reload_profi]]` — единственная вики-ссылка в никуда. Числа 62 / 2 769 / 199 607 / 318 821 — воспроизвёл дословно.

**Проверка на сокращение обязательного** — PE-01 сделал по форме (три дорогих правила, все внутри; `HARD` в `roles/` — пусто, подтверждаю). Я дополнительно проверил, не лежат ли в `roles/` прямые указания владельца, которые выпадут из чтения при старте: два (`ui-01.md:33` — 18.08, `pe-01.md:135` — 11.08), оба адресованы своей роли и читаются при её созыве (`README.md:36`: «промпт роли = содержимое `roles/<role>.md`»). Сокращение с 77 до 62 — допустимо.

**Четыре правки к тексту:**

1. **Порядок применения — сначала проверить, что путь есть у проекта.** Строка 59 — единственное место в `.profi`, где записан путь к библиотеке. Новая редакция велит брать его из `CLAUDE.md`/`MEMORY.md` проекта, а есть ли он там — проверить в этом репозитории нечем (граница, раздел 0). Условие: строка с абсолютным путём снимается **тем же ходом**, которым в `MEMORY.md`/`CLAUDE.md` проекта MDMy проверено или добавлено «при старте подгрузить `.profi` из <путь>»; до этого — не снимать.
2. **Имя файла памяти не терять.** «(запись о рецидиве — в памяти проекта, где он случился)» → «(запись о рецидиве — в памяти проекта; в MDMy это `feedback_compact_reload_profi`)». Вики-ссылка в никуда — брак; имя в тексте — нет, и по нему файл находят.
3. **Не вписывать байты и символы в правило.** `worktree_deploy_safety.md:57` и `rule-expiry…` п. 6, последний абзац: «величину состояния в тексте правила не фиксируют — в правиле стоит команда». Оставить: «Объём набора на 24.08.2026 — 62 файла; пересчитывать командой `ls README.md knowledge/standards/*.md scenarios/*.md playbooks/*.md roles/hr-d.md roles/big7/INDEX.md | wc -l`». Строки, символы, байты — убрать: они протухнут с первой правкой и превратят верное правило в неверный факт.
4. **«Помещается только в контекст 1M»** — по оценке самого PE-01 это 28–53 % контекста 200k; «только» — преувеличение, и исполнитель на 200k прочтёт его как «значит, можно не читать». Заменить на: «Это четверть-половина контекста 200k и единицы процентов от 1M — поэтому Шаг 0 стоит раньше этого шага, а не для порядка.»

Остальное — перечень, артефакт `ГЕЙТ .profi: N из M`, `roles/hr-d.md` и `INDEX.md` в наборе — принять. Артефакт согласуется с Г1/§55 от 19.08.

### П10 — временный запрет Fable без срока. **Принять.**

Подтверждено: `big7-consultation.md:19` без даты снятия; `git-branch-discipline.md:62` (внесено 19.08) называет такую оговорку постоянной. Чем старое неверно — названо (нет момента пересмотра). **Безопасность и указание владельца:** текст запрета не тронут, дописан только срок вопроса; «нет ответа — действует дальше» — не ослабление. Наблюдение о Fable 5 в этой сессии подтверждаю по своей строке системного контекста — с той же оговоркой: это субагент PE-01/PQ-01, а запрет — о модели Big-7; решать владельцу.

### П11 — артефакт ревизии в `MEMORY.md`, которого у библиотеки нет. **Принять.**

Подтверждено: `ls` корня — `MEMORY.md` нет; `rule-expiry…:42` вешает артефакт на него; `README.md` раздела «Ревизии» нет. Чем старое неверно — названо (п. 3 того же стандарта). Срок 26.08.2026 не тронут. Текст — как у PE-01. Замечание без правки: «проверено файлов: K» — K считать той же командой, что M в П9, иначе это два разных числа.

### П12 — форма `no-regression` не заполнена ни разу. **Принять с правкой.**

Подтверждено: строка 39 введена коммитом `49610ab` (`git show`), и ни в одном файле того же коммита абзаца «История правок» нет. Чем старое неверно — названо (второй экземпляр записи, которая уже есть; 0 исполнений при первом применении).

**Два места, где новая редакция теряет покрытие:**

1. **Поле «Регрессы».** Форма `no-regression` (строки 31–34) имеет поле «Регрессы: перечислить всё, что может стать хуже». У формы PE-01 (Было/Стало/Почему/Проверено) такого поля нет; «Почему» отвечает, чем старое неверно, а не что станет хуже. Приравнивать одну форму к другой можно только с этим полем. Правка: в «Стало» после «(поля … у PE-01 и вердикт PQ-01 — это и есть заполненная форма)» добавить: «**при условии, что в записи разбора по этой правке есть строка «Регрессы: …» — что может стать хуже, — либо у PE-01, либо в вердикте PQ-01; нет строки — форма не заполнена**».
2. **Правка, меняющая смысл, без разбора.** Новый текст: «Если разбора не было (правка не меняющая смысла…) — форма не требуется» — приравнивает «без разбора» к «не меняющая смысла». Это неверно для правок координатора по `post-incident-memory-update.md` или по указанию владельца (`rule-expiry…` п. 6: «правка тем же ходом» — без PE-01/PQ-01). Старая редакция такие случаи ловила абзацем в файле. Правка: «Если правка меняет смысл, а разбора PE-01/PQ-01 не было (указание владельца, правка после инцидента) — форма абзацем «История правок» в правленом файле, как прежде. Если правка смысла не меняет (`pq-01.md`, «Когда ты подключаешься») — форма не требуется.»

Запасной вариант PE-01 (19 абзацев задним числом) — не нужен: строка-адрес в файле + запись разбора с «Регрессы» покрывает то же.

### П13 — «не закрывается никогда» против «любой агент имеет предел». **Принять с правкой.**

Подтверждено: `big7-consultation.md:5`, `agent-question-rendezvous.md:12`, четыре стандарта существуют, указания владельца 07.07 и 08.07 — в шапках. Чем старое неверно — названо (общее правило не знает об исключениях, заведённых владельцем).

**Правка обязательна:** PE-01 пишет «их предел — сессия» для всех четырёх, **не прочитав соседнюю строку** в одном из них. `delegate-background-ops-to-cheap-subagents.md:38`: «**Cap:** не плодить вечные watch-агенты (ср. [[agent-question-rendezvous]] §2) — … закрывать по завершении программы наблюдения»; строка 50 — вердикт Big 7 «cap+reuse». Для watch-агентов предел **уже** задан, и он не сессия. Это ровно случай из `pe-01.md:67` («оговорка, ограничивающая находку, стоит рядом с ней и в выдачу поиска не попадает») — в поле «Проверено существованием» стоит «четыре файла существуют», а не «что прочитал вокруг». Второе: «ядро» — см. П1, п. 3.

**Текст (дописать в `agent-question-rendezvous.md:12`):**
> Исключение — **персистентные агенты по стандартам делегирования**: Big 7 (`scenarios/big7-consultation.md`), поиск (`delegate-search-to-persistent-haiku`), документация (`delegate-documentation-to-persistent-sonnet`) — их предел — сессия, потому что их ценность в накопленном контексте; они не fire-and-forget: ждут `SendMessage`, а не крутят цикл. Watch-агенты (`delegate-background-ops-to-cheap-subagents`, «Cap») — тоже персистентные и реюзятся, но закрываются по завершении программы наблюдения, как сказано там.

### П14 — роли UX-01 и UI-01 называют файлы одного проекта. **Принять с правкой** (условие применения).

Подтверждено: `ux-01.md:37, 115–119`, `ui-01.md:42` — пути MDMy; `README.md:3, 33` и коммит `5d295c4` (04.06.2026) — библиотека объявлена универсальной. Чем старое неверно — названо (артефакт обязателен, `ux-01.md:109`, а в другом проекте файла нет). Тексты PE-01 — принять: имена файлов сохранены в скобках, дата поставлена.

**Условие:** PE-01 сам ставит владельцу вопрос 2 (раздел 8) и сам говорит, что при ответе (а) «свод одного проекта» П14 не нужна. Значит, П14 применяется **только после ответа (б)**; до ответа — не применять, чтобы не переписывать дважды. Это не вкусовая правка, но её необходимость зависит от решения, которого ещё нет.

---

## 3. Удаление и «ожидает с 19.08»

### Удаление 1 — `templates/memory_index.md`, раздел «Doctrine» (строки 12–31). **Принять с правкой.**

Три обязательных поля есть. **Происхождение** — проверено, с поправкой даты (04.06, не 08.06). **Что защищает** — проверено выборочно: `feedback_prod_escalation` → `prod_escalation.md`, `feedback_no_parallel_same_tree` → `parallel-agents.md`, `feedback_finish_before_new` → `workflow_loop.md` п. 0, `feedback_task_clarification` → `workflow_loop.md` п. 1 / `confirm-understanding-before-acting.md` — дома есть. Что `feedback_admin_tooltips` и `feedback_changelog_required` — проектные и остаются, PE-01 назвал сам; подтверждаю, что в `.profi` им дома нет (`grep -ril 'tooltip\|changelog' knowledge scenarios playbooks` — по смыслу правила не нашёл; проверял по этим двум словам).

**Правка — производящая половина.** Шаблон индекса — единственное место, которое **производит** память нового проекта (`rule-expiry…` п. 3: артефакт вешается на производящую половину). Правка Г1 от 19.08 (принята) требует в `MEMORY.md` раздел «ОБЯЗАТЕЛЬНО ДО ПЕРВОГО ДЕЙСТВИЯ» с перечнем-набором и артефактом `N из M`. Если шаблон его не содержит, новый проект его не получит, и Г1 останется правкой одного проекта. Строка-замена PE-01 — принять, и **в тот же ход** добавить в шаблон каркас этого раздела (текст — по `PE-01-разбор.md:1304–1340`, который принят; повторять здесь не буду, чтобы не плодить третий дом). Без этого удаление — принять, но Г1 обязана прийти в шаблон отдельной правкой, и её надо назвать в списке ожидающих.

### У1 — `playbooks/phase_a_b_split.md`. **Принять.** Открыл: пятистрочный редирект на `parallel-agents.md`. Входов в `.profi` — один (`worktree_deploy_safety.md:101`), второй — в памяти проекта (по записи 19.08, проверить нечем).

### У2 — ссылка на `git_merge_strategies.md`. **Принять.** Файла нет; предостережение про `-X theirs` стоит в `parallel-agents.md:66`.

### Раздел «Ожидает с 19.08» (Г1, сироты, Д1, Д2, Д3, У1, У2, П3→П13). **Принять таблицу как факт** — состояние «не применено» проверил по своду: `INDEX.md:39–43` — пять строк таблицы вне таблицы; `big7-consultation.md:11, 50–57` — семь ролей; `grep 'потерянный вход'` → пусто; `grep 'ОБЯЗАТЕЛЬНО ДО ПЕРВОГО'` → пусто. **Две правки к таблице:** (1) Д1 — «до 22 файлов» → «не меньше 28» (раздел 1, расхождение 1), и список ожидающих должен прямо сказать, что П1 и П13 в редакции PE-01 нарушают условие Д1; (2) добавить строку «Г1 → `templates/memory_index.md`» (см. удаление 1).

---

## 4. Семнадцать «не меняющих смысла» — перепроверка разряда

Правило: сомневаюсь — считаю, что смысл менялся, и требую полную форму.

| № | Вердикт по разряду | Причина |
|---|---|---|
| 1 | **Переквалифицировать — меняет смысл** | Замена ссылки на несуществующее правило ссылкой на **другой** документ — это выбор, какое правило теперь действует. `verify-no-break` (строки 13, **21, 27**, 30 — PE-01 назвал две из четырёх) — по смыслу «ничего не сломано после выкладки», то есть `regression-safety-on-new-feature.md`, а не `verify-deploy-actually-shipped.md` (это «выложилось ли вообще»). Четыре замены — четыре полных формы: что утверждало старое имя, покрывает ли новый адрес. |
| 2 | **Переквалифицировать** | `function-passport rule` → `documentation-first-golden-rule`: в MDMy это, по всей видимости, правило памяти с форматом паспорта; `documentation-first` содержит «паспорт до кода», но совпадает ли объём — не показано. Полная форма с сопоставлением. |
| 3 | **Переквалифицировать** | Тот же класс, что 1–2 (три замены адреса). |
| 4 | мелкая — принять | Скобка дублирует текст той же фразы; убрать — нейтрально. |
| 5 | мелкая — принять | `knowledge/README.md:24` говорит то же, что подразумевало имя. |
| 6 | мелкая — принять | Подчёркивания → дефисы — то же имя; `task_clarification` → `workflow_loop.md` п. 1 — открыл, там «верно понял: X и Y?» — совпадает. |
| 7 | мелкая — принять **с одной правкой** | Пути — нейтрально. Но «(запись в памяти проекта)» вместо `feedback_autonomous_no_owner_questions` теряет имя — оставить: «(в MDMy — `feedback_autonomous_no_owner_questions`)». Та же логика, что П9, п. 2. |
| 8 | мелкая — принять | Пометка «пример из MDMy» — нейтрально. |
| 9 | **Переквалифицировать — меняет смысл, и текст неверен** | Правка меняет, какой формат обязателен для стандартов, — это правило, а не разметка. И утверждение «стандарты идут по форме Статус · Применимость · …» — не факт: строку «Статус» имеют **29 из 43** (`grep -L 'Статус' knowledge/standards/*.md` → 14 файлов). Одна мёртвая норма заменяется другой, выполненной на две трети. Полная форма; предлагаемый текст: «Формат ниже — для `guides/` и `recipes/`. Для `standards/` обязательны два поля в любом виде: когда правило действует и не действует («Применимость» / «Условия применения») и откуда оно («Источник»: дата, случай); остальное свободно» — и честно понизить до пожелания, если и это не выполняется. |
| 10 | мелкая — принять | Неполный перечень → «см. каталог» — по правилу о списках. |
| 11 | мелкая — принять | Скобка не про интерактивность; запрет на `rebase -i` остаётся. |
| 12 | мелкая — принять | Перестановка пункта, текст тот же. |
| 13 | мелкая — принять | `INDEX.md:29–31` сам отсылает туда же. |
| 14 | мелкая — принять | Префикс пути; строка 102 — У2. |
| 15 | мелкая — принять | Дописывает указатели на Шаги −1 и 1.5; краткая версия остаётся ссылкой, не дублем. |
| 16 | **Переквалифицировать — меняет смысл** | Перечень ролей — это множество, под которым QA-02 **обязан** проверить интерфейс (`qa-02.md:84`). «Привести к одному» без направления может убрать «аудитора» у QA-02 — сокращение проверок. Полная форма; направление — **к большему**: добавить «аудитор» в `ux-01.md:103`, не убирать из `qa-02.md:84`; пометка «перечень — в памяти проекта» — принять. Строка: 84, не 83. |
| 17 | мелкая — принять | «`CLAUDE.md` проекта» — уточнение адреса. |

**Итого: 5 из 17 переквалифицированы (1, 2, 3, 9, 16)**, 12 остаются мелкими, одна из них (7) — с правкой.

---

## 5. Главное требование владельца — есть ли правки ради видимости работы

По каждой из П1–П14 и удалению 1 проверял: названо ли, чем **старая** редакция неверна (а не чем новая лучше), и подтверждается ли это открытым файлом.

| Правка | Чем старое неверно — названо? | Подтверждено мной? | Вкусовая? |
|---|---|---|---|
| П1 | да — предписывает эскалируемое первым шагом | да, три файла | нет |
| П2 | да — отменяет обязательный порядок | да | нет |
| П3 | да — ложная гарантия | да, делом (`curl`) | нет |
| П4 | да — неверная причина, один файл противоречит себе | да | нет |
| П5 | да — роль против протокола в точке трёх рецидивов | да | нет |
| П6 | да — дубль с меньшим объёмом, роль отклоняет своё же задание | да | нет |
| П7 | да — правило без входа | да | нет |
| П8 | да — неполный список, выглядящий полным | да (и недосчитан) | нет |
| П9 | да — три множества, путь одной машины, ссылка в никуда | да | нет; но пп. 3–4 «Стало» (байты, «только 1M») — лишнее, снято правкой |
| П10 | да — нет момента пересмотра | да | нет |
| П11 | да — артефакт на несуществующем месте | да | нет |
| П12 | да — 0 исполнений при первом применении | да | нет |
| П13 | да — общее правило не знает исключений | да | нет |
| П14 | да — обязательный артефакт не существует вне MDMy | да | нет, но зависит от ответа владельца |
| Удаление 1 | да — производит дубли, запрещённые `session-start.md:59` | да | нет |

**Вкусовых правок среди меняющих смысл нет.** Среди мелких вкусовой назову одну: **№ 12** (`hr-d.md:15–19`, вынести Read/Grep из списка Edit/Write) — список действительно неаккуратен, но исполнитель его не прочтёт неверно; это правка «чтобы было ровно». Принимаю её только потому, что она бесплатна, и отмечаю, что «не меняющая смысла» здесь значит «не меняющая ничего».

Отдельно о «39 файлов — нечего править»: это честный ответ в духе задания владельца, и он поимённый — проверяем. Я его **не перепроверял** (раздел 0).

---

## 6. Безопасность и указания владельца

Проверял каждую правку на ослабление. Итог:
- **Ослабляют в предложенной редакции:** П3 (Phase A — «read-only SSH можно» против «не выполнять SSH» в том же файле) — снято правкой; П12 (правка, меняющая смысл без разбора, осталась бы без формы) — снято правкой; П8 (переезд списка из того, что читают, в то, что читают не всегда) — снято правкой; П2 не ослабляет, но **не закрывает** случай плейбука — снято правкой.
- **Указания владельца:** П10 — текст запрета Fable не тронут; П7 — проводит указание 19.08 во входы; П9 — строка 21 (owner 06.07) меняется в сторону большего набора (60 → 62), формулировка «СВОИМИ tool-call'ами (Read)» и «система-reminder не засчитывается» — не тронуты; поручение 08.06 (авто-подгрузка) — усилено словом «с путём». П13 — указания 07.07/08.07 не тронуты, текст чинится так, чтобы общее правило им не противоречило.
- **Ничего не ослаблено в П1, П4, П5, П6, П11, П14, удалении 1.**

---

## 7. Проверка делом

| Случай (файл, дата) | Что предписывает новая редакция | Вердикт |
|---|---|---|
| DO-01 при ENOSPC сам выполнил `builder prune -af && image prune -af`, снёс образы отката (`prod_escalation.md:79`, дата в файле не названа) | П1: `builder prune -f` — сам; всё, что удаляет образы, — STOP, Big 7. Брифинг: «anything that deletes images or volumes → escalate». | **В редакции PE-01 — неоднозначно:** `prod_escalation.md:18` называет production-affecting любую команду с `-f`, и исполнитель, сверившись, либо эскалирует кэш (ложный стоп), либо решит, что раз `-f` разрешён для builder, то и для image. **С правкой (оговорка про `-f` и ссылка на routine) — приводит к верному действию.** |
| Агент в worktree пересобрал и выложил бандл со stale-базы (`worktree_deploy_safety.md:3`, дата не названа) | П2: база из главного каталога + хеш + маркер; П3: build/up/cp/push из копии — red flag. | **В редакции PE-01 — не приводит:** брифинг (то, что агент читает) после правки запрещает ветвиться от `origin/master`, но **не запрещает выкладку**; блок Permissions из `worktree_deploy_safety.md:77–86` в шаблоне брифинга отсутствует. Агент со свежей базой выложит — и формально не нарушит брифинг. **С правкой (запрет выкладки в строке 20 брифинга) — приводит.** |
| Авто-resume после компакта → деплой на прод без перезагрузки `.profi` (`session-start.md:17`, 2026-07-01) | П9: набор-перечень, артефакт `ГЕЙТ .profi: N из M` до первого действия; П5: роль координатора отсылает к протоколу, а не к «релевантным файлам». Шаг −1 не тронут. | **Приводит.** Запрет был и раньше; новое — артефакт, по которому пропуск виден до действия, и снятие лазейки в роли. Оговорка: артефакт защищает от пропуска, не от чтения по summary — но это Шаг −1, и он остаётся дословно. |
| Два агента подряд встали на проверке stale-`origin` в изолированных копиях (`PE-01-разбор.md:152–166`, память 16.08.2026) | П2: «Do NOT branch from `origin/master` inside a worktree»; `task-template.md` — оговорка для копии. | **Приводит.** Оба входа, по которым агент шёл к ремоуту (брифинг, шаблон задания), теперь отсылают к главному каталогу. |

Два из четырёх случаев в редакции PE-01 не ловятся; оба — в правках, которые он сам поставил первыми по цене. После правок — ловятся все четыре.

---

## 8. Где мой разбор мог быть пристрастен

Смотрел в обе стороны.

- **В сторону «принять»:** П12 — новая редакция делает **мой** разбор частью обязательной формы (то есть придаёт моей работе вес) и избавляет от 19 абзацев задним числом. Это мне удобно. Противовес: потребовал поле «Регрессы» и не отдал случай «правка без разбора» — обе правки добавляют работы, а не снимают. П7 и стандарт о PE-01/PQ-01 — расширяют полномочия пары, в которой я состою; принял без правки. Перепроверил: вход у стандарта действительно один (`grep`), и правка не расширяет признаки серьёзного задания, а лишь проводит существующие. П9 — артефакт `ГЕЙТ N из M` — механизм, который 19.08 предлагал я (§55); соблазн принять своё. Противовес: четыре правки к П9, две из них — против текста, который я бы и сам написал (байты в правиле).
- **В сторону «отклонить»:** П8 — вернул девять пунктов в брифинг, то есть удлинил текст; проверял, не «чтобы наверняка» ли это (`rule-expiry…` п. 4). Нет: брифинг — вход, а не второй дом, и правило о списках выполняется первым способом. Переквалификация пяти мелких — добавляет работы PE-01, не мне; но правило «сомневаюсь — значит менялся» — из моей инструкции, и все пять я обосновал, а не сослался на сомнение.
- **Что не проверял и где мог ошибиться:** токены; 39 «пустых» файлов; память MDMy; случаи DO-01 и stale-выкладки — по записи. Там, где число разошлось (22/28), я нашёл причину, а не выбрал своё; но локаль PE-01 — моё объяснение, не его признание.

---

## 9. Итог для владельца

**Счёт.** П1–П14: **принять — 5** (П4, П5, П7, П10, П11), **принять с правкой — 9** (П1, П2, П3, П6, П8, П9, П12, П13, П14), **отклонить — 0**. Удаления: принять — 2 (У1, У2), принять с правкой — 1 (удаление 1). Мелкие: 12 остаются мелкими (одна с правкой), 5 переквалифицированы (1, 2, 3, 9, 16). Расхождений в числах — 7 (существенное одно: 22 → 28), неточностей в ссылках — 3 (строка 84, входы `verify-no-break`, `main`).

**Применять стоит — но не как есть, а после совместной редакции, и в таком порядке:**
1. **П1 + П2 + П3** — с моими правками: это три места, где свод сегодня велит исполнителю сделать то, ради запрета чего написан соседний файл. Цена — прод. Два из четырёх прогонов делом в редакции PE-01 не ловятся, с правками — ловятся.
2. **П9 + П5 + П11** — протокол старта и ревизия: одним ходом, с условием по пути к `.profi` и без байтов в правиле. К 26.08.2026 — первая строка «Ревизия свода».
3. **П4, П7, П8, П13** — доведение правок 19.08 до всех входов и связка стандарта о PE-01 с местами, где пишут задания.
4. **П6, П12, удаление 1, У1, У2** — порядок и форма.
5. **П10, П14** — ждут слова владельца (запрет Fable; универсальная библиотека или свод одного проекта).

**Что должен сделать PE-01 до совместной редакции:** пересчитать Д1 в `C.UTF-8` (28, не 22); убрать «ядро» из П1 и П13 до хода Д1; дать полную форму по пяти переквалифицированным мелким; поправить семь чисел и три ссылки. Где мы не сойдёмся — обе позиции одним текстом владельцу, по `pq-01.md`, «Если не сошлись».

---

## Проверка применения, 2026-08-24

PQ-01 [2026-08-24 02:40]: проверен **факт** — `git -c core.quotepath=off diff` по всем 33 изменённым файлам свода и удалённому `phase_a_b_split.md`, плюс новый раздел в `PE-01-разбор-2026-08-24.md`. Не намерение, а дифф, строка за строкой.

### 1. Сверка с вердиктами

**Девять «принять с правкой» — в моей формулировке дословно:** П1 (`deploy_workflow.md:10, 20, 92`; `agent_briefing.md:42, 47` — оговорка про `-f`, ссылка на routine, «Big 7» вместо «ядро»), П2 (`agent_briefing.md:20` — с запретом выкладки), П3 (`parallel-agents.md:15–17, 76, 78` — все три входа, строгий промпт Phase A сохранён), П6 (дата 24.08), П8 (девять пунктов + ссылка, с оговоркой для builder prune), П9 (четыре правки — см. п. 3 об отступлении), П12 (поле «Регрессы», правка без разбора — абзацем), П13 (без «ядра», с оговоркой о watch-агентах), удаление 1 (каркас «ОБЯЗАТЕЛЬНО ДО ПЕРВОГО ДЕЙСТВИЯ» добавлен в шаблон). **Пять «принять»** (П4, П5, П7, П11 с моим замечанием про K, У1, У2) — применены как предлагалось. **Пять переквалифицированных мелких** — в полной форме в разделе PE-01, тексты сверены: М1 — все четыре входа `verify-no-break` (13, 21, 27, 30), `regression-safety` вместо `verify-deploy-actually-shipped`; М9 — мой текст дословно; М16 — «аудитор» добавлен в `ux-01.md:103`, из `qa-02.md:84` не убран. Мелкая 7 — имя `feedback_autonomous_no_owner_questions` сохранено. П10 и П14 — не применены, в ожидающих названы. Все новые адреса ссылок (`task-testing-all-stages.md:23`, `documentation-first…` «Гейт», `delegate-documentation…` §2.5 п. 2, guide шаг 4) открыты — существуют.

**Сверх согласованного:** Д2 и Д3 (ожидали с 19.08) применены по текстам 19.08 — согласовано вердиктами §64/§65 и «Действуйте». Но в таблице `INDEX.md` колонка «Главное „нельзя“» при переписывании **потеряла два ограничения**: у SEC-01 исчезло «Не занимается infra-security в одиночку», у BE-01 «security-middleware» сузилось до «auth-middleware». Текст взят из `PE-01-разбор.md:432–462` и принят 19.08 — тогда это не было замечено; по `pe-01.md`, п. 2 («пересказ обязан совпадать по объёму») это потеря, и дешёвая к возврату.

**Потеряно из согласованного:** (а) `README.md:13` — «subagent не имеет SSH, поэтому код и деплой разделены» — тот же ложный факт, что чинит П3, остался; четвёртый вход, и `README.md` — в обязательном наборе старта. (б) `session-start.md:9` — Шаг −1 по-прежнему велит «загрузить `memory/MEMORY.md` + **все файлы памяти** + `.profi`», тогда как Шаг 1 и пункт (3) строки 21 теперь говорят «перечень раздела»: две меры в одном разделе — ровно класс дефекта, который Г1 устраняла. (в) `templates/memory_index.md` — строка-ссылка на `templates/agent_briefing.md` библиотеки, которую PE-01 сам предлагал взамен `agent_briefing_template.md`, не вставлена; указание «обязательный блок в каждый coding-промпт» из шаблона памяти выпало.

**Безопасность и указания владельца:** строка запрета Fable (`big7-consultation.md:19`) — не тронута, сверено дословно. Строка owner 06.07 (`session-start.md:21`) — «СВОИМИ tool-call'ами (Read)», «`system-reminder` НЕ засчитывается» — не тронуты; изменён только пункт (3), см. п. 2. Списки production-affecting, запреты worktree, гейт перед ответвлением — усилены, не ослаблены. Ослаблений не найдено.

### 2. `session-start.md:21`, пункт (3) — согласование или сокращение?

**Вердикт: согласование Шага −1 с Шагом 1 — принять**, с двумя оговорками.

По существу это сокращение обязательного чтения, но сокращение сделано **Г1** (Шаг 1), которую PQ-01 принял 19.08 (§54–55) с проверкой тройки дорогих правил, и которую владелец разрешил применять сегодня. Оставить в пункте (3) «КАЖДЫЙ файл из индекса» при Шаге 1 «перечень раздела» значило бы держать две меры для одного гейта — исполнитель выбрал бы меньшую и был бы формально прав. Запретительная часть Шага −1 (никаких действий до гейта) не тронута.

Оговорки: (1) **Граница проверки.** «С разрешения владельца 19.08.2026» — по записи PE-01 (`PE-01-разбор.md:1336`: «сокращать разрешил владелец»); прямой цитаты владельца я в файлах не нашёл; сегодняшнее «Действуйте» передано через HR-D. Строка в своде утверждает слова владельца — у такого утверждения должен быть адрес: дописать в скобку «(запись — `PE-01-разбор.md`, блок 2; применение — «Действуйте» 24.08.2026 через HR-D)». Нет адреса — через месяц это неотличимо от самовольного смягчения самой жёсткой строки свода. (2) Строка 9 того же файла — см. п. 1(б): пока она говорит «все файлы памяти», согласование неполное.

### 3. Два отступления PE-01 от моей буквы

**П9 п. 1 — путь не снят, а переведён в датированную переходную скобку.** **Принять.** Моё условие было «снимать тем же ходом, которым путь проверен в памяти проекта»; из этого репозитория проверить нельзя, и PE-01 выбрал консервативную сторону: путь остаётся, с датой, с пометкой «в MDMy» и с условием снятия. Это исполнение условия, а не обход. В список «вне этого репозитория» скобка внесена — проверил.

**Д1 — не применена.** **Принять.** Условие 19.08 — «все нормативные места одним ходом или никак»; 57 мест в памяти MDMy отсюда недоступны. «Никак» — верный выбор. Замечание: `INDEX.md` озаглавлен «Ядро команды — Большая 10», а 28 файлов говорят «Big 7» — это не Д1 (число не заменено именем), а состояние до хода Д1, и `INDEX.md:3` его прямо оговаривает. Приемлемо.

### 4. Удаление `phase_a_b_split.md`

Файл удалён (`git status`: `D`). Смысл: файл был пятистрочным редиректом на `scenarios/parallel-agents.md` — переезжать было нечему. Входы: `grep -rn phase_a_b_split . --include=*.md` (без файлов разбора) → **пусто**; `worktree_deploy_safety.md:100` → `scenarios/parallel-agents.md`; `README.md:11, 24` — переписаны. Второй вход — `memory/reference_profi.md` MDMy — вне репозитория, назван в «вне этого репозитория». Принять.

### 5. Раздел «Совместная редакция и применение» — соответствует ли диффу

**Да, с тремя поправками.** Таблица применения сверена построчно с диффом; адреса файл:строка верны. Числа применения воспроизведены: 33 / 1 (`git status`), 25 файлов с «История правок», 0 имён правил без файла, 0 вики-ссылок в никуда, 24 стандарта с «## Источник». Колонка «Регрессы» есть по каждому пункту — требование П12 выполнено при первом применении, в отличие от 19.08. Поправки: (а) в таблице нет строки о `README.md:13` (П3, четвёртый вход) — потому что правка не сделана; (б) в строке Г1 не отражено, что `session-start.md:9` не приведён к перечню; (в) в строке Д2 не названа потеря двух «нельзя» в `INDEX.md`. «Не сошлись — пусто» — подтверждаю: спора по существу нет.

### 6. Четыре случая — по применённым текстам

| Случай | Применённый текст | Вердикт |
|---|---|---|
| DO-01, ENOSPC, `builder prune -af && image prune -af` | `deploy_workflow.md:92`, `agent_briefing.md:42, 47`: кэш сборки — сам, с оговоркой про `-f` и ссылкой на routine; образы — STOP, Big 7 | **Приводит к верному действию.** Двусмысленность с `-f` снята в обоих входах. |
| Выкладка со stale-базы из worktree | `agent_briefing.md:20`: база по абсолютному пути + хеш + маркер; «From a worktree you NEVER deploy» с перечнем; `parallel-agents.md:15–17, 78` | **Приводит — для исполнителя.** Для координатора — **неоднозначно до правки `README.md:13`**: читая там «subagent не имеет SSH», HR-D может не вложить блок Permissions, сочтя, что песочница запретит сама. |
| Авто-resume → деплой без перезагрузки `.profi` (01.07) | Шаг −1 не тронут; Шаг 1 — перечень + `ГЕЙТ память: N из M`; Шаг 1.5 — перечень + `ГЕЙТ .profi: N из M`; `hr-d.md:34` — по протоколу | **Приводит.** «Стоп, читать» предписано однозначно; артефакты делают пропуск видимым. Оговорка: `session-start.md:9` и Шаг 1 называют разную меру — на «делать или не делать» не влияет, на «сколько читать» — влияет; поправить до коммита. |
| Два агента встали на stale-`origin` (16.08) | `agent_briefing.md:20` «Do NOT branch from `origin/master` inside a worktree»; `task-template.md:22–23` | **Приводит.** |

### 7. Вердикт: можно ли коммитить

**Как есть — нет. После четырёх правок — да**, и они на десять минут:

1. **`README.md:13`** — «subagent не имеет SSH, поэтому код и деплой разделены» → «выкладка из изолированной копии запрещена правилом, а не песочницей (`scenarios/parallel-agents.md`), поэтому код и деплой разделены». Без этого П3 легла на три входа из четырёх.
2. **`scenarios/session-start.md:9`** — «(загрузить `memory/MEMORY.md` + все файлы памяти + `.profi`)» → «(загрузить `memory/MEMORY.md` + набор раздела «ОБЯЗАТЕЛЬНО ДО ПЕРВОГО ДЕЙСТВИЯ» по Шагу 1 + обязательный набор `.profi` по Шагу 1.5)». Без этого в Шаге −1 две меры.
3. **`scenarios/session-start.md:21`**, пункт (3) — к «с разрешения владельца 19.08.2026» дописать адрес записи: «(запись — `PE-01-разбор.md`, блок 2; применение — «Действуйте» 24.08.2026 через HR-D)».
4. **`roles/big7/INDEX.md`**, таблица — вернуть SEC-01 «Не занимается infra-security в одиночку» и BE-01 «security-middleware» (вместо «auth-middleware»).

Желательно, не блокер: (5) `templates/memory_index.md`, раздел Doctrine — добавить строку «[Agent briefing](<путь к .profi>/templates/agent_briefing.md) — обязательный блок в каждый coding-промпт»; (6) П10 — срок вопроса о Fable не зависит от ответа владельца и мог быть применён; оставлен в ожидающих — приемлемо, но назвать явно в списке «вне репозитория/ожидает», а не только в таблице.

После правок 1–4 — коммитить; в сообщении коммита — строка-адрес по П12.

### 8. Пристрастность этого раздела

Три из четырёх блокеров — входы, которые пропустил PE-01, а не я; соблазн был считать это чужой ошибкой. Но `README.md:13` и `session-start.md:9` я не назвал и в своём разборе — проверял входы П3 и Г1 по тексту PE-01, а не `grep`-ом по формулировке; это тот же класс пропуска. Назвал обе как свои. Отступление П9 п. 1 принято легко — оно исполняет моё же условие; проверил, что скобка датирована и имеет условие снятия, а не просто оставляет путь. Пункт 2 (согласование Шага −1) — место, где мне удобно принять: мера Г1 от 19.08 частично моя (§55). Противовес — требование адреса для слов владельца, которое добавляет работы и мне, если запись окажется неточной.

---

## Блок 2: разбор Б1–Б16 (схема ролей и моделей), 2026-08-24

PQ-01 [2026-08-24 03:40]: независимый разбор раздела «Блок 2» файла `PE-01-разбор-2026-08-24.md` (строки 586–908) и файла `big7-консультация-2026-08-24-роли-и-модели.md`. Файлы свода PE-01 этим блоком не менял — подтверждено: `git status` показывает только файлы первого цикла.

### 0. Что проверил сам и граница

**Своими командами повторены:** счёт имён моделей (`grep -rniE 'opus|sonnet|haiku|fable'` → **76 строк в 14 файлах** — сошлось дословно); «матрица ownership» → **ровно 3 входа** (`hr-d.md:28`, `pm-01.md:14`, `workflow_loop.md:20`) — сошлось; входящие ссылки на пять стандартов оси → **5 строк** в названных файлах — сошлось; объём пяти стандартов → **99 строк** — сошлось; числа консультации (набор Шага 1.5 — 339 062 байта; `roles/big7/*` — 106 617 байт) — сошлись; опорные строки `session-start.md:73` и `big7-consultation.md:13` — на месте. **Открыты целиком:** пять стандартов оси (все 99 строк — для Б15), четыре `delegate-*` (строки замен Б12 сверены выборочно по восьми адресам — совпали), файл консультации (195 строк), `git-dr-mirror-sync.md`, `git-branch-discipline.md` (строки 5, 28–38, 58–62), `task-testing-all-stages.md:23`, `regression-safety…:31`, `agent-review-loop…:6, 24`.

**Граница:** все «решения владельца 24.08» — по записи HR-D, первоисточника нет; PE-01 назвал эту границу сам, я её подтверждаю как границу и своей проверки. Память MDMy недоступна — переносы «в память проекта» проверяемы только как обязательства.

### 1. Расхождения в числах и ссылках PE-01 (четыре)

1. **«Консультация — 240 строк»** — файл имеет **195 строк** (`wc -l`). 2. **Б9, «Проверено существованием»: «условия С2/С4/С6 — строки 200–235»** — этих строк в файле нет; условия С1–С7 стоят на строках ~179–191. 3. **Б12: «18 строк, где имя только в имени файла»** — механическая проверка (удалить из каждой строки выдачи имена трёх файлов `delegate-*` и проверить остаток) даёт **12** строго адресных строк и 64 с именем вне адреса; 18 не воспроизводится — вероятно, PE-01 считал и обрезанные имена (`delegate-to-sonnet` в `agent-review-loop:6`). Требую в артефакте применения поимённый перечень всех 76 строк по трём классам — Б12 сам это обещает («каждая строка выдачи относится к одному из трёх классов»). 4. **Б12 пропустил нормативный вход:** `task-testing-all-stages.md:23` — «ТЗ на реализацию (в т.ч. **делегирование Sonnet**) ОБЯЗАНО включать тест-файлы» — имя модели в нормативной фразе, не в адресе и не в цитате (цитата owner 16.06 — «Всё с тестами», остальная строка — не цитата). После правки Б12 в нынешнем виде артефакт «спорных нет» не выполнился бы.

### 2. Вердикты Б1–Б16

**Б1 — принять с правкой.** Неисполнимость старого признака №3 подтверждена (моё же возражение), новый перечень — слова владельца по записи. Две правки: (1) из старого признака №2 выпало «меняющая структуру продукта» — заменено неопределённым «глобальные изменения»; вернуть явно: «глобальные изменения (в том числе меняющие структуру продукта)». (2) «Тестирование» сталкивается с «Не считается: повтор уже проверенного задания с другими входными данными» в том же стандарте — дописать «тестирование — как класс заданий (новый план проверки), а не каждый прогон тестов внутри рядовой задачи», иначе два абзаца одного файла спорят.

**Б2 — принять.** Цитата владельца датирована, источник назван записью HR-D; различие «датированный случай — можно, действующее указание — нельзя» проведено верно и решает ровно тот дефект, из-за которого 19.08 проектные директивы прошли оба разбора.

**Б3 — принять с правкой (порядок).** Текст верен, потеря найдена не в тексте, а в **окне применения**: до подтверждённого переноса в память MDMy действующий запрет владельца («писать только в GitHub; GitLab — постоянно, только чтение/архив») не живёт нигде, кроме файла разбора и истории git, — ровно то, о чём вопрос 6 HR-D. Правка: Б3 и Б4 применяются **только после** предъявления HR-D строки записи из памяти MDMy (дата + «постоянно» + статус зеркала); до этого в обоих файлах остаётся переходная датированная скобка по образцу П9 п. 1: «(переходно: в MDMy на 24.08.2026 действует — писать только в GitHub, GitLab только чтение/архив, решение владельца 18.08+24.08; скобка снимается ходом, которым запись подтверждена в памяти проекта)». Дата в случае внизу Б3 («18.08 приостановил, 24.08 сделал постоянным») сохранена — это опыт, подтверждаю.

**Б4 — принять с правкой.** Та же правка порядка, что Б3 (обе части (б) и (в) снимают действующие записи — адрес записи и долг уборки — до их переноса). Остальное верно: (а) имя-пример вместо утверждения, (в) обезличенный урок с датированным примером; «одним ходом по файлу» — три места названы.

**Б5 — принять.** Даты и механизм случая 19.08 сохранены полностью, сняты только имена и ссылка на файл памяти; артефакт (`grep` → 0) назван.

**Б6 — принять.** Датированный пример вместо недатированного утверждения о составе ремоутов; проверка остальных вхождений `origin` в файле — выполнена и объяснена.

**Б7 — принять.** Условие П14 («только после ответа (б)») наступило ответом владельца «библиотека универсальная»; тексты — уже одобренные в первом цикле, повторного разбора не требуют.

**Б8 — принять.** По заданию HR-D проверено отдельно: (1) источник снятия запрета назван в самом тексте как «ответ через HR-D, запись — `PE-01-разбор-2026-08-24.md`, блок 2» — запись, не первоисточник, и это сказано; (2) датированная история сохранена, а не стёрта: 08.07 (грабля наследования), 12.07 (запрет, метка TEMP-OPUS-INSTEAD-OF-FABLE), 24.08 (снятие) — все три даты в «Стало»; (3) на будущее задан порядок («новые запреты — только записью владельца с датой и условием снятия»). Ослабления нет: запрет снят владельцем, не редакцией.

**Б9 — принять с правкой.** С2, С4, С6 встроены дословно, включая «агент не принимает задач без ГЕЙТ-строк» и «префикс роли — только в ответе агента ядра». Две правки: (1) заголовок абзаца называет четыре вида постоянных агентов (ядро, поиск, документация, наблюдение), а строка учёта — три: `Агенты: ядро=<id>, поиск=<id>, доки=<id>` — добавить `наблюдение=<id>` (если запущен), иначе watch-агент — единственный неучтённый постоянный контекст; (2) поправить ссылку «строки 200–235» на фактические строки условий (~179–191).

**Б10 — принять.** Артефакт SEC-02 — дословно условие С3/требование SEC-02, форма та же, что у Д3; единственный шаг постдеплоя без артефакта его получает.

**Б11 — принять с правкой.** Дом ярусов в Шаге 0 — верно (тот же приём, что Г2). Правка — из проверки делом (случай 3 ниже): критерий кодового яруса «**следующий после флагмана ярус той же линейки**» неоднозначен, когда флагман — новое семейство: сегодня флагман Fable 5, и «та же линейка» не определена — исполнитель не выведет из системного контекста, Sonnet это или Opus. Заменить на: «**Кодовый** — рабочая модель для реализации классом ниже флагмана (в системном контексте — алиасы кодовых моделей; на 24.08.2026 при флагмане Fable это Sonnet или Opus — датированный пример, слова владельца). **Старший кодовый** (флагман) — когда ТЗ требует проектных решений по ходу, и решения возвращаются ядру на аудит.» Датированный пример здесь — не имя-норма, а якорь чтения; без него критерий не исполняется.

**Б12 — принять с правкой.** Замены сверены выборочно по восьми строкам — номера и содержимое совпали; решение не переименовывать файлы (адрес ≠ утверждение, пометка первой строкой) — верное, тот же довод, что Д1. Правки: (1) добавить пропущенный вход `task-testing-all-stages.md:23`: «(в т.ч. делегирование Sonnet)» → «(в т.ч. делегирование кодовому ярусу)» — остальная строка (цитата 16.06, прецедент #209) не трогается; (2) артефакт применения — поимённый перечень 76 строк по классам «правится / адрес / датированная цитата-случай» (см. расхождение 3: моё разнесение даёт 12 адресных, его — 18; спор решается перечнем, а не числом).

**Б13 — принять.** Три входа подтверждены grep'ом; оба адреса существуют; строка `Роли: …` — артефакт С1 в формулировке PM-01, «минимум координатора» — подтверждение `session-start.md:73`, не новое правило.

**Б14 — принять.** Закрывает риск 3 SEC-01 дословно; цена (ждать пересоздания агента) названа честно.

**Б15 — принять с правкой.** Главная проверка по заданию: инвентаризация всех случаев/дат/цитат пяти стандартов против текста `ask-or-act.md`. **Сохранено:** цитата владельца 19.07 — дословно, сверена посимвольно; «реальный проект» (NC) и «прецеденты» (TE) — в «Источниках»; примеры мелочи (имя переменной, файл по конвенции, формат лога, идиома) — ст. 1; примеры развилки (подход, scope, устройство, дефолт) — ст. 3; «цена предположения выше цены вопроса» — ст. 2; «широкая» трактовка слабого решения, форма (1)(2)(3), «к собственным прошлым советам», «молча исполнять = нарушение» — ст. 5; «вопрос = сигнал к probe», «зачем X = возможно избыточен» — ст. 6; HARD ступени 4–5; границы no-silent-installs, no-guessing, rendezvous; `feedback_autonomous_no_owner_questions` (MDMy). **Потеряно — вернуть:**
1. **HARD-статус ступеней 2–3.** `confirm-understanding-before-acting` весь был HARD (заголовок), у PE-01 HARD только на 4–5 — это понижение класса действующего правила. Шапка нового файла: «**Статус:** обязательный; **HARD — ступени 2–5**».
2. **Граница ступени 1а «когда всё же спросить владельца»** — выпал весь раздел «Граница» `autonomous-self-verify`: секреты/пароли (sudo, ключи); биллинг/платные ресурсы (новый VPS); физические/внешние действия вне доступа (домашний роутер port-forward, DNS у регистратора, чужая админ-консоль). Без него ст. 1а читается как «не спрашивай никогда» — и агент полезет туда, где обязан спросить. Дописать в ст. 1а: «Граница: проба физически невозможна — секрет/пароль, биллинг/платный ресурс, физическое действие вне нашего доступа (роутер, DNS у регистратора, чужая консоль) — тогда вопрос владельцу уместен и обязателен.»
3. Мелкие, вернуть или объявить осознанно снятыми: «reboot/**shutdown** устройства» в ст. 4 (сейчас только reboot); пример «достижимость хоста извне — пробить с подходящей vantage-точки»; связи `probe-before-implement` (ст. 6) и `documentation-first-golden-rule` (ст. 5 — «обоснование возражения — фактами из доки/кода»); антипаттерн «принять вопрос как риторический и не сделать probe».
Механика (пять файлов → трёхстрочные ссылки, не удаление; шесть входов; артефакт-grep) — верна, входы подтверждены; в каждом стабе должна стоять своя ступень (таблица соответствия у PE-01 есть — NC→1, CU→2–3, AS→1а, CS→5, TE→6).

**Б16 — принять с правкой.** Порядок применения дополнить гейтом из Б3/Б4: «Б3–Б4 — только после предъявленной записи в памяти MDMy; до этого — переходные скобки». Прогон С7 — принять как приёмку; запись о нём — в мой файл при применении, как и сказано.

### 3. Соответствие условиям С1–С7 — попунктно

| Условие | Где встроено | Вердикт |
|---|---|---|
| С1 (минимум координатора, «матрица» → два адреса, строка `Роли:`) | Б13 | встроено дословно |
| С2 (жизненный цикл, 5 пунктов) | Б9 (пп. 1–3), Б9 п. 3 (ГЕЙТ-строки), Б14 (п. 5), Б11 (строка в Шаг −1) | встроено; правка — watch в строке учёта |
| С3 (триггеры — обязанность агента ядра; @SEC-02) | Б9 («Триггерные проверки»), Б10 | встроено дословно |
| С4 (ленивое чтение, строка загрузки, префиксы только у агента) | Б9 | встроено дословно |
| С5 (ярусы без имён, одним ходом, метка Fable по ответу владельца) | Б11 + Б12 + Б8 | встроено; правки — критерий кодового (Б11), вход task-testing:23 (Б12) |
| С6 (секреты — file:line) | Б9, последняя строка | встроено |
| С7 (приёмка прогоном) | Б16 | встроено |

Условие консультации «внедрение только после ответа владельца на П10» — выполнено: ответ есть (запрет снят), Б8 фиксирует.

### 4. Вопрос 6 — не удаляются ли действующие запреты до переноса

Найдено одно настоящее окно — Б3/Б4 (запрет GitLab и долг уборки), закрывается гейтом и переходными скобками (вердикты выше). Проверил остальные снятия: Б5/Б6 — снимают имена, не запреты (механизм и даты остаются в тексте); Б8 — снимает запрет, который снял владелец, с сохранённой историей; Б15 — не удаляет файлы, но понижал HARD и терял границу 1а — возвращено правками. Строка запрета Fable в текущем своде (`big7-consultation.md:19`) до применения Б8 остаётся дословно — проверено, не тронута.

### 5. Проверка делом — три случая

| Случай | По предложенным текстам | Вердикт |
|---|---|---|
| Агент ядра потерян после компакта, на очереди деструктивная прод-операция (класс `prod_escalation.md:66–72`) | Б9 п. 2: проверка жизни, нет ответа за ход → завести заново по файлам ролей, не по summary; Б14: решать «за ядро» координатор не вправе, ждёт вердикта; Б9 п. 3: агент не примет задачу без ГЕЙТ-строк координатора | **Приводит к верному действию** |
| Интерфейсная правка: дошла ли до QA-02/SEC-02 | Б13: строка `Роли: …` до делегирования — пропуск роли виден; Б9: агент ядра сам отвечает `Проверено: интерфейс — да/нет; выкат — да/нет` и созывает UX/UI/QA-02; Б10: после выката без строки `@SEC-02: проверено снаружи…` выкат не завершён | **Приводит**; мелкая правка не ускользает — «к каждому изменению интерфейса» (INDEX, применено в первом цикле) плюс триггер у агента, а не у координатора |
| Выбор модели для нового кодового агента (сегодня флагман — Fable 5) | Б11: «следующий после флагмана ярус той же линейки» — какой линейки? Fable — новое семейство; из системного контекста не выводится, Sonnet это или Opus | **Неоднозначно** → по моей инструкции = не приводит; снято правкой Б11 (критерий + датированный пример-якорь) |

### 6. Пристрастность

Б11 и Б15 «ask-or-act» строятся на моих же находках первого цикла (П10, вопросы владельцу) — принять их мне удобно, и обе я принял с правками, которые добавляют работы PE-01, а не мне. Обратная сторона: строгость к Б12 (перечень 76 строк поимённо) и к Б3/Б4 (гейт переноса) удлиняет цикл, в котором я — проверяющий; проверил, что обе строгости выводятся из чужих правил (артефакт, обещанный самим Б12; вопрос 6 HR-D), а не из моего вкуса. Разбор Б15 по инвентаризации — место, где легче всего было сказать «всё перенесено»: нашёл три потери, две из них — класс «оговорка и статус», который теряют первым; искал по полному тексту пяти файлов, открытых целиком, а не по разделу «Источники» нового файла.

### 7. Итог блока 2

**Счёт: принять — 8** (Б2, Б5, Б6, Б7, Б8, Б10, Б13, Б14), **принять с правкой — 8** (Б1, Б3, Б4, Б9, Б11, Б12, Б15, Б16), **отклонить — 0. «Не сошлись» — пусто.**

До применения исправить: HARD 2–5 и граница ступени 1а в `ask-or-act.md` (Б15); гейт переноса в память MDMy для Б3/Б4 + переходные скобки (Б16); критерий кодового яруса с датированным якорем (Б11); вход `task-testing-all-stages.md:23` и поимённый перечень 76 строк (Б12); «меняющая структуру продукта» и оговорка про тестирование (Б1); watch в строке учёта агентов и номера строк консультации (Б9); плюс два числа в тексте PE-01 (195, не 240; строки условий).

**После этих исправлений применять можно** — в порядке Б16 с гейтом; прогон С7 обязателен в живой сессии, его запись — в этот файл.

---

## Проверка применения блока 2, 2026-08-24

PQ-01 [2026-08-24 04:40]: проверен факт — `git -c core.quotepath=off diff 68fef96` (33 файла + новый `ask-or-act.md`, файл консультации не тронут). Дифф сверен с вердиктами «Блок 2: разбор Б1–Б16» построчно.

### 1. Восемь «принять с правкой» — все в моей формулировке дословно

- **Б15 / `ask-or-act.md`** — прочитан целиком (31 строка): шапка «**HARD — ступени 2–5**» и пометки (HARD) на ст. 2 и 3; **граница 1а** полная (секреты/пароли — sudo, ключи; биллинг/платные ресурсы — новый VPS; физические действия вне доступа — роутер и port-forward, DNS у регистратора, чужая консоль; «вопрос владельцу уместен и обязателен»); возвращены shutdown (ст. 4), vantage-точка (1а), [[probe-before-implement]] (ст. 6), [[documentation-first-golden-rule]] (ст. 5), антипаттерн «принять вопрос как риторический» (ст. 6). Цитата владельца 19.07 — дословна. Пять стабов — каждый с именем своей ступени, заголовки (включая «(HARD)» у confirm-understanding) сохранены; повторная инвентаризация пяти исходников против нового текста потерь **не нашла**.
- **Б3/Б4** — переходные скобки на местах и дословны: `git-dr-mirror-sync.md:27` и `git-branch-discipline.md:37` («в MDMy на 24.08.2026 … GitLab — только чтение/архив; решение владельца 18.08 + 24.08; скобка снимается ходом, которым запись подтверждена в памяти проекта») плюс третья в выноске долга (`git-branch-discipline.md:57`). **Действующие запреты не ослаблены и не остались без дома**: до гейта записи в памяти MDMy они читаются в обоих файлах. Гейт записан в разделе применения PE-01 как обязательный до снятия скобок.
- **Б11** — якорь в таблице Шага 0 дословно («на 24.08.2026 при флагмане Fable это Sonnet или Opus — датированный пример, слова владельца»); критерий «Старший кодовый» продублирован в `delegate-implementation…:22`.
- **Б12** — вход `task-testing-all-stages.md:23`: «делегирование кодовому ярусу», цитата 16.06 и прецедент #209 не тронуты.
- **Б1** — обе правки внесены («глобальные изменения (в том числе меняющие структуру продукта)»; «тестирование — как класс заданий … а не каждый прогон тестов»).
- **Б9** — `наблюдение=<id, если запущен>` в строке учёта; ГЕЙТ-строки, «по файлам ролей, не по summary», ленивое чтение, секреты `file:line` — дословно.
- **Б16** — гейт Б3/Б4 записан; С7 честно помечен «вне этого репозитория».

### 2. Контрольные grep'ы — повторены мной

| Проверка | Результат |
|---|---|
| Нормативные имена моделей | **0**: из 76 строк осталось 30 вхождений; после вычета имён файлов `delegate-*` — только датированные цитаты владельца (07.07 ×2, 08.06, 08.07 ×2), датированные случаи (`session-start.md:38`, грабля 08.07, история запрета) и датированный якорь Шага 0. Ярусы в четырёх `delegate-*` — по 7–8 вхождений; пометка «имя файла — исторический след» — строка 3 всех трёх файлов с именем модели в имени |
| «Матрица ownership» | **0** (три входа заменены на два адреса) |
| Метка `⏳TEMP-OPUS` | **0**; история 12.07 → снятие 24.08 сохранена строкой «История запрета» с адресом записи (ответ владельца через HR-D — граница названа в самом тексте). `⏳` в своде — только обезличенная форма-образец в `git-dr-mirror-sync` |
| Пять старых имён оси | только «Источники» `ask-or-act.md`; пять входящих ссылок заменены на `[[ask-or-act]]` со ступенями; `hr-d.md` — строка «Полная шкала…» |
| `gitlab-old` / `feedback_push_github` | **0**; `GitHub`/`GitLab` — только две переходные скобки (датированы, с условием снятия), обезличенный пример «GitHub↔GitLab и т.п.» и датированные случаи |
| `console/src` в ролях | **0** (П14: обобщённые имена, пути MDMy — в датированных скобках) |

### 3. Перечень 76 строк (58/12/6+2) — выборочно

Приложение в разделе применения PE-01 существует, сумма сходится (58+12+6+2=76), прежний счёт «18 адресных» честно разобран как ошибка. Выборка десяти строк: класс П — `search:23` («написание кода (Sonnet)» — в базе есть, в текущем заменено на ярус), `impl:11–12` (`model:'sonnet'` — снято), `doc:19` («координатора (Opus)» — снято), `bg:29` («Haiku не крутит цикл» — снято), `big7:19–22` (раздел заменён Б8); класс А — `search:46`, `doc:4`, `rendezvous:12`, `prod_escalation:72` (имя только в адресе, не тронуты); класс Ц — `search:51` и `impl:56` (цитаты владельца, не тронуты дословно), `session-start:38` (перечень-доказательство, не тронут). Все десять соответствуют классу.

### 4. Сверх/потеряно/безопасность

Сверх согласованного — ничего: все изменённые файлы соответствуют целям Б1–Б16, «История правок» стоит в каждом. Потерь согласованного не нашёл — с одним исключением, оно же единственный блокер (ниже). Безопасность и указания владельца: запреты GitLab — под переходными скобками до гейта; HARD-статусы — восстановлены (2–5); запрет Fable снят самим владельцем, история датирована; строка owner 06.07 в `session-start.md:21` — не тронута этим диффом.

### 5. Блокер — один, на одну строку

**`knowledge/standards/git-dr-mirror-sync.md:25`** — ссылка «(`git-branch-discipline.md`, «Срок оговорки»)» указывает на заголовок, который Б4(в) **снял** (его смысл теперь в выноске «Непустой вывод — …»). Читатель, пришедший по якорю, не найдёт раздела — класс «битая внутренняя ссылка», который весь цикл и чистили; несостыковка между моими же Б3 и Б4, называю её своей. Замена: «(`git-branch-discipline.md`, выноска «Непустой вывод…»: оговорка без даты снятия — постоянная)».

### 6. Три случая — по применённым текстам

| Случай | Применённый текст | Вердикт |
|---|---|---|
| Агент ядра потерян после компакта, деструктивный прод-SSH на очереди | `big7-consultation.md`, «Жизненный цикл» пп. 1–4 + `prod_escalation.md` (новая строка): проверка жизни за один ход → завести заново по файлам ролей → ждать вердикта; без ГЕЙТ-строк агент задач не примет | **Приводит** |
| Интерфейсная правка — дошла ли до QA-02/SEC-02 | `hr-d.md:29` строка `Роли: <перечень>` до делегирования; «Триггерные проверки» — агент ядра сам отвечает `Проверено: интерфейс — …` и созывает UX/UI/QA-02; «Постдеплой» п. 2 — без строки `@SEC-02: проверено снаружи…` выкат не завершён | **Приводит** |
| Выбор модели для нового кодового агента (флагман — Fable 5) | Шаг 0, таблица ярусов с датированным якорем; `delegate-implementation…`: обычное ТЗ → кодовый, «ТЗ требует проектных решений по ходу» → старший кодовый с возвратом ядру на аудит | **Приводит** (неоднозначность «той же линейки» снята якорем) |

### 7. Пристрастность

Блокер — ошибка, восходящая к моему же тексту Б3; дешевле было промолчать (ссылка «почти верна»), и именно поэтому проверил перекрёстные якоря отдельно. Выборку перечня 76 строк делал по базе `68fef96` командой, а не по таблице PE-01; десять строк — из всех трёх классов, включая те, что подтверждают его правоту против моего прежнего счёта «12 адресных» (его разнесение 58/12/6+2 точнее моего механического — моя sed-проверка не отделяла шесть строк смешанного класса, это признаю́).

### 8. Вердикт

**Коммитить можно после одной строки** — правка `git-dr-mirror-sync.md:25` (замена битого якоря, класс «мелкая, не меняющая смысла», разбора не требует — только запись в строке коммита). Всё остальное — зелёное: 16/16 применены, восемь правок — дословно, контрольные grep'ы сошлись, три случая приводят к верному действию. После коммита и вливания в main остаются открытыми (не блокируют, записаны у PE-01): гейт Б3/Б4 (запись в памяти MDMy → снятие трёх переходных скобок), прогон С7 в живой сессии с записью в этот файл, перенос указателей пяти старых имён оси в памятях проектов, ревизия свода до 26.08.2026 (первая строка — этот цикл).

---

## Прогон-приёмка С7, 2026-08-24

PQ-01 [2026-08-24 05:10]: разбор отчёта агента ядра о приёмке С7, переданного HR-D.

### 1. Граница проверки — прежде вердикта

Отчёт агента ядра — **запись, не само событие**: строки триггеров, строки загрузки ролей и ход проверки SEC-02/QA-02 я вижу пересказом HR-D, первоисточник — транскрипт агента, который мне не предъявлен. Поэтому вердикт двухслойный: что я перепроверил своими руками — проверено; остальное — принято по записи, и это названо.

**Перепроверил сам (все команды — из этой рабочей копии):**
- `git fetch` + `git rev-parse origin/main` → `4fb3e51344f865a454b1b859135363250752af7e` == HEAD — **сошлось посимвольно** с отчётом;
- `git status --porcelain` → пусто — сошлось;
- форматные паттерны секретов (та же строка классов: ghp_, github_pat_, AKIA, xox, sk-ant-, PRIVATE KEY, ssh-rsa AAAA, AIza) по **всем 95** tracked-файлам → **0**; присвоения token/password/api_key → **0** — вывод SEC-02 подтверждён на множестве строго шире его;
- строки загрузки ролей: `wc -c roles/big7/sec-02.md` → **5259**, `wc -c roles/big7/qa-02.md` → **10137** — оба числа сошлись посимвольно со строками `Роль … загружена`; это и есть артефакт С4 — число из команды, не из памяти;
- выборка целей ссылок QA-02 (6 из README/INDEX, включая новый `ask-or-act.md`) — все существуют.

**Расхождения записи (два, оба объяснимы, ни одно не меняет вывода):**
1. «по 91 tracked-файлу» — tracked всего **95**; 91 получается при исключении четырёх файлов разбора, но множество в отчёте не определено — тот самый класс «число без определения множества» (`pe-01.md`, п. 2). Мой прогон по всем 95 дал тот же ноль, так что вывод стоит.
2. «43 упоминания .md (36 уникальных)» — мой образец даёт 49 упоминаний; образец агента дословно не приведён, сверить нельзя (дословный образец не сверяет числа — сверяет повторный прогон той же строки). Существенный вывод («битых 0») перепроверен выборкой — стоит.

### 2. Вердикт по С7

**С7 закрыт — в той части, которая исполнима на этом репозитории; одна ветка честно не прогнана и переносится, а не замалчивается.**

Что доказано делом: триггерная строка в каждом ответе (`интерфейс — нет; выкат — да; прод — нет`) — есть и **честная**: у библиотеки правил интерфейса нет, и «нет» здесь — правильный ответ, а не пропуск; ленивое чтение — две строки загрузки с байтами, сошедшимися с `wc -c`, плюс **явный отказ** грузить DO-01/QA-01 с названной причиной — это сильнее буквы С4 (видно не только что загружено, но и что решено не грузить); артефакт SEC-02 — с предметом (хэш, классы паттернов, счётчики, оценка четырёх словесных упоминаний), не «ок»; выкат-триггер отработал по факту push.

Что НЕ доказано этим прогоном: ветка «интерфейс — **да** → строки `@UX-01/@UI-01/@QA-02` с предметом» — та самая формулировка С7 «пустяковая интерфейсная задача». На этом репозитории она непрогоняема в принципе (интерфейса нет), и приёмка выполнена на ближайшем исполнимом классе (выкат → SEC-02/QA-02). По моей инструкции неисполнимое здесь не понижает приёмку, если названо: **остаток С7 — прогон интерфейсной ветки в первом проекте с интерфейсом (MDMy), при первой же интерфейсной задаче; запись — сюда же.** До той записи утверждение «интерфейсный триггер созывает три роли» проверено чтением текста, но не делом.

### 3. Наблюдение про guard изоляции — кандидат в правку свода: да

Отчёт агента: «guard изоляции worktree отвергает составные bash-команды — дробить на простые». **Подтверждаю делом из собственной сессии, независимо от отчёта:** 24.08 guard этой рабочей копии многократно отверг мои составные вызовы (циклы `for` с `cat`, `&&`-цепочки с `git ls-tree`/`git grep`, heredoc-дозапись с хвостовой командой) с формулировкой «command is too complex to verify that it stays inside the worktree»; дробление на одиночные вызовы решало всякий раз; на результат не влияло, время терялось на повторы. Два независимых воспроизведения в один день — это уже случай по А2, а не наблюдение.

Разряд: **новое правило → полная форма, через PE-01 → PQ-01** (следующий цикл; свод сам не правлю). Кандидат для PE-01:
- **Дом:** `playbooks/worktree_deploy_safety.md`, рядом с «Что МОЖНО» (это свойство среды исполнения worktree-агента, не git и не проекта). **Вход:** одна строка в `templates/agent_briefing.md`, в пункт про `isolation:"worktree"`.
- **Текст-кандидат:** «В изолированной копии страж харнеса отвергает составные команды оболочки, где участвует git, — `&&`-цепочки, циклы, heredoc с хвостовыми командами: он не может доказать, что составной вызов не выйдет за пределы копии. Это не отказ git и не запрет работы: дели на простые одиночные вызовы (одна команда — один вызов; многострочная запись в файл — инструментом правки, не heredoc'ом с довеском). Случай: 24.08.2026, две независимые сессии (приёмка С7 и разбор PQ-01) — страж многократно отвергал составные вызовы; результат не пострадал, время терялось на повторные попытки.»
- Причина при правиле есть (А1), «что делать вместо» есть (А3), случай датирован (А2); границы: действует только в изолированной копии, вне её — нет.

### 4. Пристрастность

Вердикт «закрыт с остатком» мне удобнее, чем «не закрыт»: цикл, где я проверяющий, закрывается сегодня. Противовес: остаток сформулирован проверяемо (запись о прогоне интерфейсной ветки — в этот файл, и до неё интерфейсная ветка числится непроверенной делом), а оба расхождения записи названы, хотя оба можно было списать как мелочь. Кандидат про guard частично опирается на мой же опыт сессии — но именно поэтому он подтверждён двумя независимыми источниками, и решение по нему всё равно не моё: полная форма у PE-01, разбор у меня, применение после согласия.

---

## Блок 3: разбор В1–В10, 2026-08-24

PQ-01 [2026-08-24 06:20]: независимый разбор раздела «Блок 3» файла `PE-01-разбор-2026-08-24.md` (строки 973–1122). Свод PE-01 не менял — `git status` чист, только файлы разбора.

### 0. Проверено существованием — своими руками

Открыты по месту: `workflow_loop.md:55–80` (шаг 7 — строки 63–71, «QA-01 утверждает» — 71; `grep -c 'SEC-02'` → **0** — подтверждено); `README.md:7` («постоянный security gate после каждого изменения»), `:19`, `:36`, «Быстрый старт» (**строка 60, не 59** — микрорасхождение); `prod_escalation.md:47–52` и `:71` (слэш «SEC-01/SEC-02» — есть; два соседних определения кворума — без слэша); `INDEX.md:43`; `big7-consultation.md:55–95` (блок формата — строки 58–71, «Три последних» — 73, «Артефакт созыва» — 75, «Нет строки…» — 83, `@SEC-02` — 91); девять строк В5 (`grep -c 'Входишь в Большую семёрку'` → по 1 в шести файлах; `sec-02.md:5, 55, 68` — открыты полными строками); `session-start.md:85`; `hr-d.md:34`; `ask-or-act.md:29` (фрагмент treat-owner — действительно «баг» без «/неоптимальность» и без «немедленно»); адреса-заголовки `sec-02.md:39` «Триггеры запуска», `:45` «Бюджет на регресс», `qa-02.md:85` «После каждого выката» — существуют; `ls knowledge/standards/ | grep no-regression` → нет файла, `scenarios/no-regression.md` есть. Старые редакции challenge/treat-owner сверять повторно не требовалось: обе прочитаны мной целиком во втором цикле, цитаты PE-01 из `git show 68fef96` совпадают с тем чтением.

**Граница:** «пятилинзовая сверка» мне не предъявлена — я проверял правки В1–В10 по своду, а не сверку по её линзам; утверждение «линзы ссылки/модели/безопасность чисты» не перепроверял, и PE-01 честно пишет то же о себе.

### 1. Вердикты

**В1 — принять.** Дыра подтверждена: канонический цикл (единственное место, куда отсылает «Быстрый старт») завершал выкат одним QA-01 при трёх местах свода, требующих SEC-02 «после каждого». Новый текст ставит гейт в производящий шаг, сохраняет QA-01 и rollback-путь («падение любого из трёх — rollback»), бюджет SEC-02 назван — соседние требования не ослаблены, добавлен только признак завершённости. 🔴 оправдан.

**В2 — принять.** Двоякость слэша реальна, и опасное прочтение — кворум деструктивного прод-SSH без SEC-01 — именно то, что выберут однажды. Новый текст повторяет уже записанный в `:49` и `INDEX.md:43` кворум, третий вход перестаёт расходиться с двумя. На двоякость перечитал сам: «плюс SEC-02, если меняется security-surface» — одно прочтение.

**В3 — принять.** README:19/36 действительно предписывают отвергнутую схему (B); различие «инженеры и PE-01/PQ-01 — агент на роль; ядро — один агент, ленивое чтение» — дословно по применённым Б9/консультации.

**В4 — принять с правкой; выбор основного варианта (убрать блок) — подтверждаю, запасной — отклоняю.** Запасной («пример с датированной пометкой») отклоняю по доводу самого PE-01, который проверил и подтверждаю: пример-перечень без обязанности пересмотра — второй дом состава с отложенным сроком годности; ровно так разъехались 13 мест к 19.08. **Правка обязательна:** PE-01 пишет «строки 73–80 не трогаются», но строка 73 — «**Три последних** — обязательны, если изменение видно в интерфейсе» — после удаления блока теряет антецедент: «трёх последних» больше нет, читатель не знает, о ком речь. Строку 73 заменить на: «**UX-01, UI-01 и QA-02 — обязательны, если изменение видно в интерфейсе**, и пропускаются, только если не видно.» (Три имени здесь — не дубль состава: это перечень обязательных-при-интерфейсе, он уже стоит в «Артефакте созыва» строками 77–79 и в `INDEX.md:44`; убрать его — потерять правило.) Диапазон «73–80» фактически 73–83 — мелочь, назвал.

**В5 — принять с правкой; разграничение с Д1 — подтверждаю (вынесенное место 2).** Проверил по условию Д1 из 19.08 (`PE-01-разбор.md:1549–1563`: «одним ходом по всем 72 нормативным местам либо никак; промежуточного состояния быть не должно» — про замену **имени** команды). В5 имени не заменяет: «Big 7», «Большая семёрка» как имя-адрес остаются нетронутыми во всех прочих местах (`big7-consultation.md:1, 5, 48, 94`, `prod_escalation`, `hr-d.md:27, 33`, `workflow_loop:25, 49` — проверено grep'ом по своду); правятся девять **утверждений о числе состава** («Входишь в Большую семёрку» при составе 10 — устаревшее вдвойне), и слово «ядро» — канон с Д2 (`INDEX.md:1`), а не третье имя. Промежуточного состояния имени В5 не создаёт — состояние «имя Big 7 + описание „ядро“» существует с Д2 и этой правкой не меняется. **Правка обязательна:** `sec-02.md:55` — полная строка «3. Отчёт в Большую 7 (не напрямую пользователю). SEC-01 координирует фиксы с DevOps / Backend / Frontend.»; «Стало» PE-01 обрывается на точке после скобки — указать явно: заменяется фрагмент до первой точки, хвост «SEC-01 координирует…» сохраняется дословно. Артефакт-grep В5 честно ограничен `roles/big7/*.md` — принят.

**В6 — принять.** Строка 85 после Б9 полуверна; новый текст различает три случая и никого не учит схеме (B).

**В7 — принять.** Столкновение двух применённых правил подтверждено чтением обеих строк; решение «дословная цитата с пометкой „по ответу ядра“ — предъявление, не речь от лица роли» разводит их без ослабления: запрет префиксов у координатора остаётся, артефакт становится доносимым до пользователя.

**В8 — принять.** Потеря «неоптимальность»/«немедленно» против заголовка «ничего не потеряно» — подтверждена посимвольно (текущая строка 29 против оригинала, который я читал целиком во втором цикле). Это **мой пропуск** в инвентаризации Б15 — см. пристрастность.

**В9 — принять.** Связь «на необратимом/прод возражение обязательно, не опционально» была в старом challenge (я её читал — «Баланс с другими правилами») и в сведении выпала; усиление, потерянное первым, — тоже мой пропуск Б15. Адрес восстановлен верно: файла `no-regression` в standards нет и не было, смысл — `scenarios/no-regression.md`; снос оформлен, а не молчалив.

**В10 — принять с правкой (вынесенное место 3).** Вставку-границу («действует только в изолированной копии») — **принять**: это моё же требование Б3-класса. Вставку «**конвейеры с `cd`**» — **отклонить как недоказанную и опровергнутую делом**: в этой сессии конвейеры и `cd &&`-цепочки с git многократно **проходили** (`cd … && git log … && git diff --stat | tail`, `cd … && git ls-files | wc -l` и десятки подобных), отвергались же циклы `for`, функции `p(){…}`, heredoc с хвостом, длинные многозвенные цепочки. Значит поведение стража для исполнителя **недетерминировано**, и правило, перечисляющее классы как гарантированно отвергаемые, повторит ошибку П3 («упадёт на sandbox-блокировке») в зеркальном виде: исполнитель начнёт дробить и то, что прошло бы, или удивится, когда «запрещённая» цепочка пройдёт. Заменить перечень на признак: «страж **может отвергнуть** составной вызов — циклы, функции, heredoc с хвостовыми командами, длинные цепочки; какие именно, заранее не предсказать — признаком служит сам отказ „command is too complex to verify that it stays inside the worktree“. Получив его — не повторяй ту же команду и не обходь стража: дели на простые одиночные вызовы; многострочную запись в файл — инструментом правки, не heredoc'ом с довеском.» Строка брифинга — соответственно: «…the guard MAY reject compound shell commands involving git — on this exact refusal, split into single calls; this is the sandbox, not a git error». Микро: мой текст-кандидат — строка **541** моего файла, не «530–540».

### 2. Три вынесенных места — итог

1. **В4:** основной вариант PE-01 (снять блок) — верный, запасной отклонён; плюс обязательная правка строки 73 (антецедент «три последних»).
2. **В5:** разграничение «входы Д2, не частичная Д1» — подтверждено проверкой по тексту условия Д1 и grep'ом по оставшимся именам; плюс правка sec-02:55 (хвост строки).
3. **В10:** граница — принята; «конвейеры с `cd`» — отклонены с опровержением делом; классы переформулированы в недетерминированный признак с дословным текстом отказа.

### 3. Безопасность и указания владельца

В1 добавляет гейт и никого не ослабляет (QA-01 и rollback сохранены); В2 сужает прочтение до строгого — кворум с обязательным SEC-01; В4 не теряет обязательности трёх ролей при моей правке строки 73; В5 не трогает ни одного «нельзя» ролей; В7 сохраняет запрет префиксов; В8/В9 возвращают потерянное. Ослаблений не нашёл. Указания владельца не тронуты.

### 4. Проверка делом — три случая

| Случай | По текстам В1–В10 | Вердикт |
|---|---|---|
| Выкат по циклу: дойдёт ли до SEC-02 | Шаг 7 в новой редакции сам требует строку `@SEC-02: проверено снаружи…` до завершения выката; при интерфейсном изменении — и `@QA-02`; падение любого — rollback на шаг 3 | **Приводит** (до В1 — не приводил: цикл кончался на QA-01, `grep SEC-02` → 0) |
| Смена состава ядра: сколько мест правки | После В3+В4+В5+В6: утверждений о числе состава вне `INDEX.md` — **ноль** (девять строк В5 — последние; блок В4 снят; README:19/36 без перечня). Правится один файл — INDEX. Остаются имена «Big 7 / Большая семёрка» как адрес — это записанное состояние до Д1 (`INDEX.md:3` его прямо оговаривает), не дыра | **Приводит** |
| Составная команда в worktree отвергнута стражем | По В10 в редакции PE-01 — неоднозначно (класс «конвейеры с cd» опровергнут делом: такие вызовы проходят); по В10 с моей правкой — признак-отказ, действие «дели на простые» | **С правкой — приводит; без — неоднозначно** |

### 5. Пристрастность

Два пункта блока — В8 и В9 — чинят то, что пропустил **я**: инвентаризация Б15 объявила «потерь не нашла», а потери были — два слова в прецеденте и одна связь-усиление. Соблазн был занизить их вес («мелочь, слова»), чтобы не признавать пропуск; вместо этого оба приняты без правок, а пропуск назван здесь и в вердиктах. Обратная сторона: В10 правит текст, который писал я, — и я отклонил чужую вставку («конвейеры с cd»), сохранив свою основу; проверил, что отклонение стоит на воспроизведённом факте (прошедшие конвейеры — в истории этой сессии), а не на авторстве. В5 мне было удобно принять (моё же «ядро» из Д2 расползается дальше) — потому разграничение с Д1 проверял по первоисточнику условия, а не по пересказу PE-01.

### 6. Итог блока 3

**Счёт: принять — 7** (В1, В2, В3, В6, В7, В8, В9), **принять с правкой — 3** (В4 — строка 73; В5 — хвост sec-02:55; В10 — классы отказа и брифинг-строка), **отклонить — 0. «Не сошлись» — пусто** (обе отклонённые детали — внутри «принять с правкой», и по обеим дан готовый текст).

До применения исправить: строку 73 `big7-consultation.md` (антецедент); оговорку о хвосте `sec-02.md:55`; классы отказа в В10 (дом и вход); три микрочисла в тексте PE-01 (README «Быстрый старт» — строка 60; диапазон 73–83; мой кандидат — строка 541). **После этих исправлений применять можно** — в порядке PE-01 (В1+В2 → В3–В7 одним ходом → В8+В9 → В10), с его же артефактами плюс мой: после В4 `grep -n 'Три последних' scenarios/big7-consultation.md` → 0.

---

## Проверка применения блока 3, 2026-08-24

PQ-01 [2026-08-24 07:00]: проверен факт — `git -c core.quotepath=off diff 4fb3e51` (16 файлов свода; в сводке PE-01 стояло «15» — микрорасхождение на один, состав файлов совпадает с целями В1–В10 полностью). Дифф сверен построчно, контрольные grep'ы повторены мной.

### 1. Три «принять с правкой» — дословно мои формулировки

- **В4:** пофамильный блок снят; строка «**UX-01, UI-01 и QA-02 — обязательны, если изменение видно в интерфейсе**, и пропускаются, только если не видно» — посимвольно моя; `grep -c 'Три последних'` → **0**; «Артефакт созыва» и «Нет строки — роль не созывалась» не тронуты.
- **В5:** шесть «Входишь в Большую семёрку» → «Входишь в ядро команды (состав — `roles/big7/INDEX.md`)»; `sec-02.md:5, 55, 68` — правлены, **хвост строки 55 «SEC-01 координирует фиксы с DevOps / Backend / Frontend.» сохранён дословно**. Grep чисел команды по `roles/big7/*.md`: утверждений о числе вне `INDEX.md:1` (дом) — **ноль**; остальные совпадения выдачи — адреса `big7/…` (образец ловит «big7» как «Big 7») и слово «большие», не числа состава.
- **В10:** дом — заголовок «Страж изоляции **может отвергнуть**…», формулировка через признак-отказ с дословным текстом «command is too complex…», «не повторяй ту же команду и не обходи стража», «конвейер» в файле → **0**, граница «только в изолированной копии» и датированный случай (две сессии 24.08) — на местах; вход в брифинге — «guard **MAY reject** compound shell commands involving git — **on this exact refusal** … split into single calls; this is the sandbox, not a git error» — моя строка дословно.

### 2. Семь «принять» — как предложено

**В1:** шаг 7 (строка 71) — «QA-01 утверждает функциональный smoke» сохранён; постдеплойный гейт: выкат не завершён без `@SEC-02: проверено снаружи…`; при интерфейсном изменении — QA-02 с адресом `qa-02.md`, «После каждого выката»; «Падение любого из трёх — rollback и обратно на шаг 3» — rollback-путь не ослаблен; `grep -c 'SEC-02'` по циклу: 0 → 2. **В2:** `SEC-01/SEC-02` → **0**; строка 71 однозначна («минимум PM-01 + DO-01 + **SEC-01**; плюс SEC-02, если меняется security-surface»), хвост про escalate-to-owner не тронут. **В3:** обе строки README — различие «инженеры/PE-01/PQ-01 — агент на роль; ядро — один агент, ленивое чтение», ссылки закрыты. **В6:** строка 85 — три случая различены, хвост «по теме задачи» сохранён. **В7:** абзац о дословных цитатах — в `big7-consultation.md` после «Нет строки…», `hr-d.md:34` дополнен. **В8:** «реальный баг/неоптимальность, который probe немедленно подтверждал» — восстановлено посимвольно. **В9:** «На необратимом и на проде возражение **обязательно, не опционально** (`scenarios/no-regression.md`, `playbooks/prod_escalation.md`)» — в ступени 5.

### 3. Сверх/потеряно/безопасность

Сверх согласованного — ничего: каждый из 16 файлов соответствует своей В-правке, «История правок» со ссылкой на оба разбора — в каждом. Потерь не нашёл: спорные хвосты (sec-02:55, session-start:85, строка 71 prod_escalation, «Артефакт созыва») проверены полными строками. Безопасность: В1 добавляет гейт, В2 сужает прочтение до строгого кворума с обязательным SEC-01, запреты и указания владельца не тронуты. Дерево — только файлы свода из целей блока плюс два файла разбора.

### 4. Три случая — по применённым текстам

| Случай | Вердикт |
|---|---|
| Выкат по циклу: шаг 7 сам требует `@SEC-02` (и `@QA-02` при интерфейсе), без строки выкат не завершён, падение — rollback | **Приводит** |
| Смена состава ядра: утверждений о числе вне `INDEX.md` — ноль (проверено grep'ом после В4+В5); правится один файл; имена-адреса «Big 7» — записанное состояние до Д1 | **Приводит** |
| Составная команда отвергнута стражем: признак — дословный текст отказа, действие — «дели на простые, не обходи»; ложной гарантии «отвергнет всегда» в тексте нет | **Приводит** |

### 5. Вердикт

**Коммитить и вливать можно — блокеров нет.** 10/10 применены, три правки — дословно, семь — как предложено, контрольные grep'ы сошлись, три случая приводят к верному действию. Открытыми через main переезжают (записаны, не блокируют): гейт Б3/Б4 (запись в памяти MDMy → снятие трёх переходных скобок), остаток С7 (интерфейсная ветка — при первой интерфейсной задаче в MDMy, запись сюда), перевод указателей пяти старых имён оси в памятях проектов, Д1 (72+ мест одним ходом либо никак), ревизия свода до 26.08.2026 — первая строка раздела «Ревизии свода» в `README.md` за этим циклом.

---

## Задание MDMy, 2026-08-24 — разбор до запуска

PQ-01 [2026-08-24 07:50]: независимый разбор файла `задание-MDMy-2026-08-24.md` (103 строки) — серьёзное задание класса «примени» для команды проекта MDMy, по `roles-and-briefs-via-pe01.md` моё согласие обязательно ДО запуска. Свод не тронут; разбор по тексту задания и по факту свода на `8252662` (HEAD, подтверждён).

### 0. Проверено существованием

Все четыре цитаты шага 6 сверены **посимвольно** с фактическими строками на `8252662`: `git-dr-mirror-sync.md:27` — совпала; `git-branch-discipline.md:37` — совпала; `git-branch-discipline.md:57`, хвост « (Переходно: …подтверждена.)» — совпал, включая ведущий пробел, и «что остаётся» («…текущее состояние долга — в памяти того проекта, не здесь.») — на месте; `session-start.md:76`, фрагмент «(Переходная скобка: …проекта.)» — совпал. `grep 'Переходн'` по трём файлам — ровно эти четыре вхождения, других нет. «Случай внизу раздела — не трогать» — существует (`git-dr-mirror-sync.md:31`). Ссылки: `PE-01-разбор.md:1304–1340` — Г1 третьей редакции там (перечень именных — строка 1310); `:1549–1563` — условие Д1 там, процитировано в шаге 8 верно; `templates/memory_index.md` — каркас на месте; мои вердикты Б3/Б4 («применяются только после предъявления записи») и П9 п. 1 («снимается тем же ходом») — процитированы точно. Числа: 343 (`PE-01-разбор.md:258, 320`), 68 🔴, 17 087, `feedback_week_broad_authorization_2026-08-16`, 226/239, рецидивы 17.06/01.07/06.07 — все сверены с записями. Десять именных файлов Г1 задание правильно **не** требует существованием здесь — они в памяти MDMy; исполнителю велено проверить их у себя. **Граница:** состояние памяти MDMy (343 или 20 файлов) отсюда не проверяемо — на этом стоит весь шаг 0, и он честно построен как гейт у исполнителя.

### 1. Расхождение — одно

**Шаг 2: «плюс десять именных» — именных девять.** Перечень в задании и в Г1 (`PE-01-разбор.md:1310`) — девять имён (`cpe_device_hard_rules`, `no_restart_without_approval`, `production_no_disruption`, `destructive_commands`, `test_device_identity`, `never_touch_airplane_mode`, `no_auto_update_prod_group`, `inaccessible_only_bootproven_builds`, `prod_escalation`); «разрешения со сроком» — отдельная категория, не десятый именной. Исполнитель, поверив числу, будет искать десятый файл. Число из строки убрать — перечень решает, число врёт (сумма 68+9+1=78 сходится с «78 файлов» первого цикла).

### 2. Вердикты по шапке и шагам

**Шапка — принять с правкой.** Границы («ничего сверх списка; нужно больше — вернись с вопросом, не расширяй сам») — точная реализация следствия 1 `roles-and-briefs`. Правка: строка «согласие на применение всех шагов ниже уже получено» была написана **до** этого разбора — до него она опережала факт. Заменить на: «разбор PQ-01 проведён, включая текст этого задания (`.profi/PQ-01-разбор-2026-08-24.md`, раздел «Задание MDMy, 2026-08-24»); согласие на применение получено». После правки строка станет правдой с адресом.

**Шаг 0 — принять с правкой (главное).** Замысел верный и цена названа честно (записи в мёртвую копию + гейт скобок по ложному предъявлению). Дефект: критерий — два именованных состояния («сотен» / «двадцати») без третьего; исполнитель со 150 файлами (или с сотнями файлов, но июньскими) не покрыт ни одним пунктом и решит сам — а дефолт обязан быть запретительным. И «порядка сотен» — признак, а не перечень: сам свод учит мерить перечнем. Точная замена пп. 2–3:
> 2. Вердикт «та» — только при **обоих** признаках: (а) файлов порядка сотен (~343 на 19.08; больше — нормально, память растёт), **и** (б) в каталоге существуют файлы из перечня шага 2 — проверь три: `feedback_cpe_device_hard_rules*`, `feedback_prod_escalation*`, `feedback_week_broad_authorization_2026-08-16*`. Оба сошлись — продолжай.
> 3. **Любое другое состояние** — двадцать июньских, каталог не найден, число между (например, 150), сотни файлов без именных из (б) — **СТОП**. Верни владельцу ровно одну строку: «память не та, что разбиралась 19.08» + путь, число и какой признак не сошёлся. Ничего из шагов 1–8 не выполняй: записи легли бы не туда, и через месяц их приняли бы за действующие.

**Шаг 1 — принять.** Хэш `8252662` — сверен с HEAD; оговорка «или потомок — назови» — правильная.

**Шаг 2 — принять с правкой** (см. расхождение: «десять именных:» → «именные (перечень — дословно из Г1, `.profi/PE-01-разбор.md:1310`):»). Остальное сверено с Г1 построчно: 68 🔴, разрешения со сроком с оговоркой «проверь актуальные», порядок ведения и признак соблюдения — дословно из каркаса, «до первого старта по новой редакции» — верно закрывает дыру N=M=0, артефакт `N == M` — строже моего «N из M», принимаю.

**Шаг 3 — принять.** Шесть записей — один в один шесть решений владельца 24.08 (GitLab постоянный; origin подтверждён; Fable снят + ярусы; признак №3 — все пять пунктов совпадают с применённым Б1; «проектное — в проекте»; ось → ask-or-act). Переоформление `feedback_push_github_only_temporarily.md` — обязательное (имя и текст предписывают отменённое — класс В2), со строкой-указателем при входах и grep-проверкой. Артефакт «предписаний „вернуть двойной push“ не осталось» — проверяемый.

**Шаг 4 — принять.** 226/239 и требование даты/срока — из применённого Б4; артефакт файл:строка.

**Шаг 5 — принять.** Оба grep-нулевых артефакта; оговорка «датированные записи-истории не трогать» — обязательная и она есть.

**Шаг 6 — принять с правкой.** Тексты «удалить/остаётся» — посимвольно верны (раздел 0). Согласие на снятие — действительно моё и записано (Б3/Б4, П9 п. 1), процитировано точно. Запреты не ослабляются: запрет GitLab после снятия скобок живёт (а) в памяти MDMy — это гарантировано гейтом самого шага («в сообщении коммита привести файл:строку записей») плюс артефактом шага 3, и (б) в датированном случае `git-dr-mirror-sync.md:31`, который задание прямо велит не трогать. Полномочия не расширены: коммит в main `.profi` — практика всех четырёх коммитов этого дня, push в GitHub — по решению владельца. **Правка:** гейт «только **после** шагов 2–3» → «только **после** шагов 2, 3 и 4» — скобка №3 снимается по записи **долга** (шаг 4), и сам же текст коммита требует файл:строку шага 4; без правки гейт разрешает снять скобку раньше записи, ради которой она стоит.

**Шаг 7 — принять.** Отдельно отмечаю запрет дописывать в мой файл задним числом («правки в нём неотличимы от вердиктов») — верная защита, совпадает с моим остатком С7: запись в память проекта, я забираю её на ревизии.

**Шаг 8 — принять.** Условие Д1 процитировано без искажений: одним ходом либо никак, пересчёт своим grep'ом, отдельное серьёзное задание через PE-01/PQ-01.

**Форма отчёта — принять.** «НЕ ТРОНУТО… git status приложен» — артефакт границы; «„готово“ без артефактов — ложный отчёт» — по task-template.

### 3. Самодостаточность и полномочия

Задание исполнимо чужой сессией: все основания — адресами в `.profi` (файлы разборов закоммичены и придут с шагом 1), все цитаты решений владельца — внутри текста с датой и источником «через HR-D», отсылок к «нашему разговору» нет. Сверка каждого шага с решениями владельца 24.08 — расширений не нашёл; шаг 6 — единственная правка `.profi` чужой командой, и она ограничена четырьмя named-строками с посимвольными цитатами и запретом «ничего рядом».

### 4. Проверка делом

| Исполнитель | Ход | Вердикт |
|---|---|---|
| Память ~343 | Шаг 0: оба признака → «та»; шаг 2: раздел 68+9+разрешения, следующий старт `ГЕЙТ память: N == M`; шаг 3: шесть датированных записей, grep «вернуть двойной push» → 0; шаг 6: после 2–3–4, коммит с файл:строками, `grep Переходн` → 0 | **Приводит** |
| Память 20 июньских | Шаг 0 п. 3: СТОП, одна строка владельцу с путём и числом; шаги 1–8 не выполняются | **Приводит** |
| Память 150 файлов | По исходному тексту — не покрыт ни «сотнями», ни «двадцатью»: исполнитель решает сам, цена — записи в мёртвую копию и снятие скобок по ложному предъявлению. По правке шага 0 — СТОП | **С правкой — приводит; без — неоднозначно** |

### 5. Пристрастность

Задание цитирует мои вердикты как основание своих гейтов — принять его мне удобно вдвойне: и согласие «уже записано», и мой файл возведён в источник. Поэтому цитаты из себя я перепроверял так же, как чужие: скобки — посимвольно по своду, а не по своей записи; вердикты Б3/Б4/П9 — перечитаны в собственном тексте. Найденные дефекты — не мои («десять/девять», дыра критерия шага 0, гейт без шага 4), и соблазна занизить их не было; заниженным могло оказаться моё внимание к шагу 7, который мне льстит запретом трогать мой файл, — проверил его не на лесть, а на механику: запись в памяти + указатель + мой забор на ревизии — путь замкнут.

### 6. Итог

**Счёт: принять — 7** (шаги 1, 3, 4, 5, 7, 8, форма отчёта), **принять с правкой — 4** (шапка, шаг 0, шаг 2, шаг 6), **отклонить — 0.** Правки точечные, все четыре даны точными формулировками (шапка — строка согласия с адресом; шаг 0 — пп. 2–3 с двумя признаками и запретительным дефолтом; шаг 2 — «десять» → перечень без числа; шаг 6 — гейт «после шагов 2, 3 и 4»). **После внесения четырёх правок задание можно передавать владельцу** — повторный разбор не нужен: правки — мои же формулировки, применение механическое.

---

## Задание MDMy v2, 2026-08-24 — разбор до запуска (после утраты памяти 22.08)

PQ-01 [2026-08-24 09:10]: разбор `задание-MDMy-2026-08-24-v2.md` (154 строки). Контекст принят по записи HR-D: память MDMy уничтожена 22.08 тестом `wipe.go`; гейт шага 0 v1 сработал (исполнитель остановился); владелец выбрал путь 1 — новая память из 17 файлов, именные — реконструкция. v1 помечен «ЗАМЕНЕНО v2» первой строкой — проверил.

### 0. Проверено существованием — своими руками

**Скобки шага 8:** все четыре цитаты в v2 — те же тексты, что я посимвольно сверял с фактом на `8252662` в разборе v1; сверил заново по тексту v2 — идентичны, включая ведущий пробел хвоста строки 57. **Строки-источники (Р) шага 3, выборочно 3 из 9 плюс общие:** таблица именных `PE-01-разбор.md:1275–1294` — все десять имён на месте; `:1310` (стартовый состав), `:1330–1332` (тройка необратимых) — на месте; `:345` — цитата «перед любым `docker compose up --force-recreate` … дождаться явного „да“» дословно; `:384, 879–880, 903` — включая перекрытую строку «Тестовых машин больше нет», которую задание верно велит переносить как историю; `:1055` — прямая цитата строки 10 погибшего `feedback_compact_reload_profi.md`; `:2140–2141` — `ls -l` с 5173 Б; `PQ-01-разбор-роли.md:783–788` (шесть кратких формулировок), `:816–819`, `:924–926` (тройка с ценой), `:559` (заголовок «восстановленное правило безопасности», §35) — всё существует и по смыслу совпадает с назначением в таблице. **Форма реконструкции** сверена с `pe-01.md:104` пункт в пункт: первая строка + `description`, дата, источники файлами и строками, «добавлено по смыслу» отдельной пометкой, срок и адресат подтверждения, «нет ответа — действует как реконструкция, а не превращается в оригинал молчанием» — дословно. **Guide** `compact-survivable-autonomous-task.md:7–9` — «живой трекер — в репо/доке проекта; якорь в памяти; указатель в `MEMORY.md`» — существует дословно; находка И1 (`PE-01-разбор.md:246`) существует. **Хэш шага 1** `8252662` = HEAD.

**Журнал undelete** (`…\wf_f039a06d-048\journal.jsonl`, 10 записей, 47 КБ) — перепроверил факты «спасательного фонда»: `prod_escalation.md` R1, 4175 Б, «хвост слегка замусорен» — сошлось; «ПРОДЛЕНО владельцем до 2026-10-01» — сошлось; `checkin-update_13.md` 13 268 Б с перечнем именных правил точными именами — сошлось; `MEMORY_1.md` 4616 Б, md5 `94081e67…`, «единственная чистая из 28 worktree-копий» — сошлось; «145 префиксных файлов, живым содержимым подтверждены 6–7» — сошлось; прецедент `feedback_naming_1.md` жив при мусорной копии — сошлось. **Важное наблюдение из журнала:** ранняя запись давала `project_backlog_cpe_locate.md` вердикт «точно оно» по размеру («5 копий, все 7534»), поздняя — «ИСПОРЧЕН: 28 нулевых байт, бинарный мусор от начала до конца». Задание взяло позднюю: его тезис «одинаковый размер ≠ одинаковое содержимое, решает только приёмка» — не осторожность, а воспроизведённый факт.

**Граница:** состояние диска D:, содержимое R1/R2 и 17 новых файлов отсюда не проверяемы — на этом стоят гейты самого задания; журнал — запись прогонов, не сами файлы.

### 1. Вердикты (шапка, шаги 0–11, форма)

**Шапка и вводная — принять.** Катастрофа названа с причиной; «уроки записаны, не дублируем» — по backlog-dedup; R1/R2 read-only в границах; правило приёмки — в шапке, до шагов.

**Шаг 0 — принять.** Критерий трёхчастный (путь + ≥17 + уникальный маркер `memory-loss-2026-08-22.md`) и дефолт запретительный («путь другой, файлов меньше, ЛИБО файла нет — СТОП»). Обратное понимание закрыто: июньский срез маркера не содержит; рост числа файлов сверх 17 — норма и гейт не ломает. Жёсткий путь машины в одноразовом задании конкретной машине — допустим (это не свод).

**Шаг 1 — принять** (хэш сверен).

**Шаг 2 — принять с правкой (три дополнения).** Правило приёмки (а)(б)(в) против ловушки — рабочее, и журнал показывает, что каждая проверка ловит свой класс: (в) — cpe_locate с 28 нулями, (а) — чужой текст под знакомым именем, (б) — обрезки. Решение «трекер 525 КБ — в `docs/trackers/`, не в память» — **принять**: обоснование тройное и всё проверено (guide строки 7–9 дословно требует «трекер в репо, якорь в памяти» — и строка-указатель в `MEMORY.md` заданием предписана; 525 КБ в стартовом чтении — класс И1; git-выживаемость — урок катастрофы; целостность — md5 идентичен в двух прогонах, по журналу). Правки:
1. **Пункт (б) не наблюдаем на обрезанном хвосте без нулевых байтов:** файл с валидным frontmatter и связным началом может кончаться на полуслове — «связный от начала до конца» превращается в оценку. Дополнить (б): «…и завершается законченным предложением или секцией; обрыв на полуслове/полусписке — не прошёл (либо переносится с пометкой „хвост утрачен“, как шаг 2.1)».
2. **Шаг 2.1 (prod_escalation, «хвост отрезать с пометкой») — не потеря смысла, если добавить сверку с эталоном:** правило эскалации в памяти — проекция `.profi/playbooks/prod_escalation.md`, эталон жив. Дополнить: «после отрезания сверь оставшийся текст с `.profi/playbooks/prod_escalation.md` и в пометке назови, соответствует ли утраченный хвост финальным строкам эталона — тогда утрата в ~2 строки восполнима по `.profi` при подтверждении владельцем». Без этого пометка честная, но смысл хвоста повисает; с этим — потери смысла нет.
3. **Гейт доступности фонда** (из проверки делом, случай 2): первой строкой шага 2 — «Проверь доступность обоих прогонов: `ls R1`, `ls R2` (≈10 898 и 12 807 md-файлов). Недоступны — СТОП, одна строка владельцу: „спасательный фонд недоступен: <путь>“. Шаги 2–3 без фонда невыполнимы (источники И и В лежат в R1); о шагах 5–7, от фонда не зависящих, спроси владельца отдельно.»

**Шаг 3 — принять.** Строгость — по `pe-01.md:104` без зазора (форма совпадает дословно, включая судьбу молчания владельца); «где источники расходятся — брать строгий вариант и помечать» и «устаревшее — как датированную историю» — обе оговорки, которые теряют первыми, здесь есть. Источники И/Р/В: Р — проверены выборочно (выше), В — прогоняются через ту же приёмку, слабость источников destructive-правила честно помечена («копии текстов `.profi`, использовать только как перечень операций»). Девятый файл — по прямой цитате строки погибшего оригинала (`PE:1055`) — самый сильный из доступных источников.

**Шаг 4 — принять.** «Честно назвать НОВЫМ перечнем» — правильное решение: 14–15 строк не выдаются за реконструкцию погибшего набора из 78; три правила, чьё нарушение стоило памяти, — в наборе; каркас, порядок ведения, признак соблюдения, `N == M` — как в согласованной Г1.

**Шаг 5 — принять.** Новое имя без «temporarily» с упоминанием погибшего имени (на него могут ссылаться спасённые тексты) — верно; в `.profi` ссылок на старое имя не осталось (проверено во втором цикле, grep → 0).

**Шаг 6 — принять.** «Пересчитай на сегодня» — правильнее v1: величина живая, репозиторий катастрофу пережил, число 226 могло измениться.

**Шаг 7 — принять.** Без ссылки на удалённый `phase_a_b_split.md`, с пометкой «заведён заново, прежний текст не восстановлен».

**Шаг 8 — принять.** Тексты — те же посимвольно сверенные четыре; гейт «после шагов 4–6» корректен: записи, которых требует сообщение коммита, создаются именно шагами 5 (п. 1 — GitLab) и 6 (долг), шаг 4 в гейте — строже необходимого и не вредит; проверка пути перед снятием скобки №4 — внутри пункта. Согласие моё записано и процитировано точно.

**Шаг 9 — принять с правкой.** Развилка отдана владельцу по ст. 3 `ask-or-act` — верно; но цена обратного понимания не закрыта: исполнитель, прочитавший «подготовить git init + задать вопрос» как «сделать», **запушит память проекта в облако без согласия** — а память содержит чувствительное (учётки тест-стенда, внутренние пути). Дополнить: «До ответа владельца — никакого remote и никакого push: `git init` локален и обратим. В `.gitignore` сразу — паттерны секретов; перед первым push — прогнать по памяти grep форматных паттернов секретов (классы — как в приёмке SEC-02, `.profi`-сессия 24.08) и исход назвать владельцу вместе с вопросом.»

**Шаг 10 — принять** (повтор согласованного в v1, включая запрет дописывать в мой файл). **Шаг 11 — принять** (условие Д1 процитировано без искажений). **Форма отчёта — принять** («R1/R2 не изменялись» — артефакт границы).

### 2. Проверка делом

| Исполнитель | Ход | Вердикт |
|---|---|---|
| Новая память, 17 файлов | Шаг 0 «та» → шаг 2: приёмка бракует cpe_locate по нулевым байтам, берёт живую копию под суффиксом (прецедент naming_1), prod_escalation — с пометкой об отрезанном хвосте, трекер — в `docs/trackers/` с указателем → шаг 3: 8+1 с объявлением и сроком 07.09 → шаг 8: после 4–6, коммит с файл:строками | **Приводит** |
| R1/R2 недоступны (диск отключён) | По исходному тексту — не покрыт: исполнитель может молча перескочить к шагу 3 и реконструировать только по (Р), слабее и без пометки. По правке 3 к шагу 2 — СТОП с одной строкой | **С правкой — приводит; без — не приводит** |
| Файл с валидным frontmatter и обрезанным хвостом без нулей | По исходному (б) — «связный» решается на глаз; по правке 1 — наблюдаемый признак конца | **С правкой — приводит** |

### 3. Пристрастность

Вводная v2 хвалит гейт шага 0 v1 — мою формулировку («сработал как задуман»), и мне выгодно принять всю конструкцию на этой волне; противовес — правки 1–3 к шагу 2 и правка к шагу 9 найдены именно там, где v2 опирается на мои прежние тексты. Отдельно: вердикт «принять» по шагу 3 совпадает с тем, что реконструкцию будут сверять по строкам моего файла, — проверил не своё удобство, а форму по `pe-01.md:104` и источники по номерам строк. Наблюдение из журнала про cpe_locate (раннее «точно оно» опровергнуто поздней приёмкой) — довод в пользу правила задания, не мой; привёл его, потому что он же показывает предел моей проверки: я сверял журнал, не файлы.

### 4. Итог

**Счёт: принять — 12** (шапка, шаги 0, 1, 3, 4, 5, 6, 7, 8, 10, 11, форма), **принять с правкой — 2** (шаг 2 — три дополнения; шаг 9 — запрет push до ответа владельца + проверка секретов), **отклонить — 0.** Все правки — точными формулировками выше, применение механическое. **После внесения правок задание можно отдавать владельцу**; повторный разбор не нужен.
