> ЗАМЕНЕНО v2 — память MDMy утрачена 22.08.2026 (деструктивный тест wipe.go), см. `задание-MDMy-2026-08-24-v2.md`. Оставлено как история. Гейт Шага 0 сработал как задуман: исполнитель остановился со строкой «память не та, что разбиралась 19.08».

# Задание ИИ-команде проекта MDMy — хвосты циклов правок `.profi` от 24.08.2026

**От:** владелец, через HR-D и PE-01 (`.profi`-сессия 24.08.2026). **Составил:** PE-01; **разбор PQ-01:** проведён, включая текст этого задания (`.profi/PQ-01-разбор-2026-08-24.md`, раздел «Задание MDMy, 2026-08-24»); согласие на применение получено, повторный цикл «предложи → проверь» не нужен; это задание — «примени», и по каждому шагу назван артефакт, которым доказывается выполнение. Записи оснований — в `.profi`: `PE-01-разбор-2026-08-24.md` (разборы и применённые тексты), `PQ-01-разбор-2026-08-24.md` (вердикты), `PE-01-разбор.md` и `PQ-01-разбор-роли.md` (разборы 19.08).

**Почему это задание существует.** 19–24.08 библиотека `.profi` прошла три цикла правок; всё, что можно было сделать в её репозитории, сделано. Осталось то, что живёт **в среде проекта MDMy** — в его памяти и настройках, — куда из `.profi`-сессии доступа не было. Пока эти хвосты не закрыты, три правила свода стоят на переходных скобках, а гейт памяти нового протокола старта формально проходится при пустом наборе (N=M=0). Устаревшая запись опаснее отсутствующей — её принимают за факт (`.profi/roles/big7/pe-01.md`, В2).

**Границы.** Правишь только перечисленное: память проекта (`memory/`, `MEMORY.md`, `CLAUDE.md` проекта) и — в шаге 6 — четыре named-строки `.profi`. Ничего сверх списка не трогать: ни код продукта, ни трекер, ни прочие файлы `.profi`. Деструктивных действий в задании нет; прод не затрагивается. Задание не серьёзнее своего текста: если по ходу обнаружится, что нужно больше, — остановись и верни вопрос, а не расширяй объём сам (`.profi/knowledge/standards/roles-and-briefs-via-pe01.md`).

---

## Шаг 0 — ГЕЙТ: та ли это память (обязателен, до всего)

Разборы 19.08 делались по памяти из ~343 файлов. В git-репозитории MDMy на машине владельца лежит только 20 файлов памяти от июня — **не та память**. Задание адресовано той памяти, которую видит **твоя сессия**.

1. Назови путь каталога памяти, который читает твоя сессия (из твоего `CLAUDE.md`/`MEMORY.md`), и предъяви число: `ls <путь>/memory/*.md | wc -l` (или эквивалент твоей среды).
2. Вердикт «та» — только при **обоих** признаках: (а) файлов порядка сотен (~343 на 19.08; больше — нормально, память растёт), **и** (б) в каталоге существуют файлы из перечня шага 2 — проверь три: `feedback_cpe_device_hard_rules*`, `feedback_prod_escalation*`, `feedback_week_broad_authorization_2026-08-16*`. Оба сошлись — продолжай.
3. **Любое другое состояние** — двадцать июньских, каталог не найден, число между (например, 150), сотни файлов без именных из (б) — **СТОП**. Верни владельцу ровно одну строку: «память не та, что разбиралась 19.08» + путь, число и какой признак не сошёлся. Ничего из шагов 1–8 не выполняй: записи легли бы не туда, и через месяц их приняли бы за действующие.

**Артефакт шага:** строка `ПАМЯТЬ: <путь> · файлов: <N> · вердикт: та / не та` — первой строкой отчёта.

## Шаг 1 — Обновить `.profi`

`git -C <путь к .profi> pull` → `git rev-parse origin/main` обязан дать **`8252662`** (или потомка — тогда назови его и почему он новее). **Артефакт:** хэш в отчёте. До этого шага остальные бессмысленны: ты будешь исполнять старый свод.

## Шаг 2 — Раздел «ОБЯЗАТЕЛЬНО ДО ПЕРВОГО ДЕЙСТВИЯ» в `MEMORY.md` (до первого старта по новой редакции!)

Новый Шаг 1 протокола старта (`.profi/scenarios/session-start.md`) читает память **по перечню этого раздела**. Пока раздела нет, перечень пуст и гейт формально проходится при N=M=0 — прочитано ничего, артефакт зелёный. Поэтому шаг 2 выполняется **до первого старта любой сессии по новой редакции**.

Состав — по Г1 третьей редакции, принятой PQ-01 19.08 (`.profi/PE-01-разбор.md:1304–1340`, каркас — `.profi/templates/memory_index.md`):
- все файлы, помеченные 🔴 в указателе (на 19.08 — 68), **плюс** именные (перечень — дословно из Г1, `.profi/PE-01-разбор.md:1310`): `feedback_cpe_device_hard_rules`, `feedback_no_restart_without_approval`, `feedback_production_no_disruption`, `feedback_destructive_commands`, `feedback_test_device_identity`, `feedback_never_touch_airplane_mode`, `feedback_no_auto_update_prod_group`, `feedback_inaccessible_only_bootproven_builds`, `feedback_prod_escalation`, **плюс все действующие разрешения со сроком** (на 19.08 — `feedback_week_broad_authorization_2026-08-16`; проверь актуальные);
- дата раздела; порядок ведения («правило, чьё нарушение стоило инцидента, вносится тем же ходом, что запись об инциденте») и признак соблюдения (дата раздела не старше свежайшей записи об инциденте) — дословно из каркаса.

Почему перечень, а не «всё»: 17 087 строк не читались трижды (рецидивы 17.06, 01.07, 06.07), владелец разрешил меру-перечень 19.08. **Артефакт:** строка `ГЕЙТ память: N из M` твоего следующего старта, где M — число строк раздела, и `N == M`.

## Шаг 3 — Датированные записи решений владельца 24.08.2026

Источник каждой записи: «ответ владельца через HR-D, `.profi`-сессия 24.08.2026; записи — `PE-01-разбор-2026-08-24.md`, разделы 8, блок 2». Свод правится по решению владельца **включая память** (`.profi/knowledge/standards/rule-expiry-and-ownership.md`, п. 6) — иначе память продолжит учить прежнему, а её читают раньше свода. Записать в память проекта (существующие файлы править, не дублировать; `backlog-dedup` действует):

1. **GitLab — запрет постоянный:** писать только в GitHub; `gitlab-old` — только чтение/архив (18.08 приостановка → 24.08 навсегда). **Переоформить `feedback_push_github_only_temporarily.md`:** имя («temporarily») и текст («приостановлено, вернуть двойной push») устарели и теперь предписывают отменённое — переименовать/переписать по порядку памяти проекта, старое имя оставить строкой-указателем, если на него есть входы (проверь grep'ом).
2. **`origin` = GitHub** — переназначение 19.08 подтверждено владельцем задним числом.
3. **Запрет Fable снят** (12.07 → снят 24.08); модели теперь ярусами — `.profi/scenarios/session-start.md`, Шаг 0.
4. **Признак №3 серьёзного задания** уточнён: прод/устройства/данные, дольше сессии, новый функционал, тестирование-как-класс, глобальные изменения (`.profi/knowledge/standards/roles-and-briefs-via-pe01.md`).
5. **«Проектное — в проекте»** — статус прямого указания (`.profi/knowledge/README.md`).
6. **Пять стандартов оси сведены** в `.profi/knowledge/standards/ask-or-act.md` — см. шаг 5.2.

**Артефакт:** перечень созданных/правленных файлов памяти с датами в отчёте; по п. 1 — grep по памяти: предписаний «вернуть двойной push» не осталось.

## Шаг 4 — Долг уборки веток: запись со сроком

19.08 после починки имени ветки аудит показал **226 влитых при 239 локальных** (норма ≤1–2). Записать в память: число, дата замера, срок уборки (или «кем и до какого числа отложена»). Без записи с датой оговорка в `.profi` стала бы постоянной («временная оговорка без даты — постоянная», `.profi/knowledge/standards/git-branch-discipline.md`). **Артефакт:** файл:строка записи.

## Шаг 5 — Указатели памяти

1. `reference_profi.md` (и всё, что найдёт grep): ссылка на удалённый `playbooks/phase_a_b_split.md` → `scenarios/parallel-agents.md`. **Артефакт:** `grep -rn 'phase_a_b_split' <память>` → 0.
2. Указатели `[[…]]` на пять старых имён оси (`no-confirm-reasonable-nondestructive`, `confirm-understanding-before-acting`, `autonomous-self-verify-nondestructive`, `challenge-suboptimal-owner-decisions`, `treat-owner-as-expert`) → `[[ask-or-act]]` (ступень указать по таблице в файле-ссылке). **Артефакт:** grep по пяти именам в памяти → 0 вхождений-указателей (упоминания в датированных записях-историях — не трогать).

## Шаг 6 — Снять три переходные скобки в `.profi` + путь в `CLAUDE.md`/`MEMORY.md`

**Гейт:** только **после** шагов 2, 3 и 4, и в сообщении коммита привести файл:строку записей из шага 3 (п. 1) и шага 4 — скобки писались именно до появления этих записей. Согласие PQ-01 на снятие записано заранее: `.profi/PQ-01-разбор-2026-08-24.md`, «Блок 2», вердикты Б3/Б4 («применяются только после предъявления записи») и П9 п. 1.

Точные правки (только эти четыре строки, ничего рядом):

1. `.profi/knowledge/standards/git-dr-mirror-sync.md:27` — **удалить строку целиком**:
   «(Переходно: в MDMy на 24.08.2026 действует — писать только в GitHub, GitLab — только чтение/архив; решение владельца 18.08 + 24.08. Скобка снимается ходом, которым запись подтверждена в памяти проекта.)»
   Остаётся: раздел «Приостановка или прекращение зеркалирования…» без скобки (общая форма + случай внизу раздела — не трогать).
2. `.profi/knowledge/standards/git-branch-discipline.md:37` — **удалить строку целиком**:
   «(Переходно: в MDMy на 24.08.2026 адрес записи — GitHub; GitLab — только чтение/архив, решение владельца 18.08 + 24.08. Скобка снимается ходом, которым запись подтверждена в памяти проекта.)»
   Остаётся: ритуал мержа со строками «какой ремоут — git remote -v + память проекта» — не трогать.
3. `.profi/knowledge/standards/git-branch-discipline.md:57` — **удалить только хвост строки** (последнее предложение в скобках):
   « (Переходно: до подтверждённой записи долга в памяти MDMy этот пример — единственная его запись; скобка снимается ходом, которым запись подтверждена.)»
   Остаётся всё до него, дословно, включая «…текущее состояние долга — в памяти того проекта, не здесь.»
4. `.profi/scenarios/session-start.md:76` — **удалить только фрагмент в скобках**:
   «(Переходная скобка: в MDMy на 24.08.2026 это `C:\Users\orlovmy\Projects\.profi`; скобка снимается тем же ходом, которым путь проверен или добавлен в `CLAUDE.md`/`MEMORY.md` проекта.)»
   — но **сначала** проверь, что в `CLAUDE.md`/`MEMORY.md` проекта есть пункт «при старте подгрузить `.profi` из <путь>» с действующим путём; нет — добавь (поручение владельца 08.06.2026), и только затем снимай скобку.

Коммит в `.profi` (ветка main, сообщение: «MDMy: сняты переходные скобки — записи в памяти проекта <файл:строка>, <файл:строка>») + push в GitHub. В каждый из трёх правленых файлов `.profi` — строка «История правок: задание MDMy 24.08.2026, шаг 6; записи — <файл:строка>» (`.profi/scenarios/no-regression.md`, «Где форма записывается»). **Артефакт:** хэш коммита; `grep -rn 'Переходн' <тремя файлами>` → 0.

## Шаг 7 — Интерфейсная ветка приёмки С7 (при первой интерфейсной задаче)

При первой же задаче, видимой в интерфейсе: агент ядра обязан выдать строки `Проверено: интерфейс — да; …`, `Роль <X> загружена: <файл>, <байт>` и `@UX-01/@UI-01/@QA-02: …` с предметом (`.profi/scenarios/big7-consultation.md`). Результат записать **в память проекта** (датированная запись + строка-указатель в `MEMORY.md`), а в `.profi/PQ-01-разбор-2026-08-24.md` ничего не дописывать: файл чужого разбора — не журнал проекта, и правки в нём задним числом неотличимы от вердиктов; PQ-01 заберёт запись из памяти на своей ревизии (остаток С7 назван в его файле, «Прогон-приёмка С7», п. 2). **Артефакт:** файл записи + строки из отчёта агента ядра дословно.

## Шаг 8 — Д1 (имя «ядро» вместо числа) — НЕ обязательное

Не выполняй как рутину. Условие 19.08 (`.profi/PE-01-разбор.md:1549–1563`, принято PQ-01): переименование — **одним ходом по всем нормативным местам (на 19.08 — 72: 15 с перечнем + 57 по имени; пересчитать своим grep'ом на день хода) либо не выполняется вовсе**; промежуточного состояния с двумя именами быть не должно; ход не начинается, пока не назван исполнитель и не подтверждено, что все места пройдены за одну работу. Если владелец захочет — это отдельное серьёзное задание через PE-01/PQ-01.

---

## Финальный отчёт (форма)

```
ПАМЯТЬ: <путь> · файлов: <N> · вердикт: та / не та
Шаг 1: .profi = <хэш>
Шаг 2: раздел заведён, M = <число строк>; следующий старт: ГЕЙТ память: N из M
Шаг 3: записи — <файл:строка × 6>; feedback_push_…: <как переоформлен>
Шаг 4: долг веток — <файл:строка, срок>
Шаг 5: grep phase_a_b_split → 0; grep пяти имён оси → 0 указателей
Шаг 6: коммит .profi <хэш>; grep 'Переходн' → 0   (либо: не выполнялся — гейт не пройден, причина)
Шаг 7: отложен до первой интерфейсной задачи (или: выполнен, запись — <файл>)
Шаг 8: не выполнялся (условие Д1), либо вопрос владельцу
НЕ ТРОНУТО ничего сверх перечисленного: git status по .profi и по памяти приложен.
```

Не готово — пиши «ЗАДАЧА НЕ ЗАВЕРШЕНА» и что осталось; «готово» без артефактов — ложный отчёт (`.profi/scenarios/task-template.md`).
