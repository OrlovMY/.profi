# Стандарт: дисциплина веток и деплоя (git)

**Класс:** HARD. **Появился:** инцидент 2026-06-03 (MDMy) — фикс закоммичен не на ту ветку, деплой затёр прод-фичу.

## Политика веток (Big-7 verdict, owner-directive 2026-06-03)
**Решение Big-7 (PM-01/AR-01/BE-01/SEC-01/DO-01/QA-01):**
1. **Разработка ВСЕГДА на отдельной ветке** (`feat/<name>` или `fix/<name>` от свежего main). Прямые коммиты в main запрещены (кроме тривиальных доков, и то лучше через ветку).
2. **Держать ≤1–2 активных dev-веток.** Не плодить. Если срочно потребовался фикс в другом месте — допустимо завести 2-ю ветку, НО обязательно ВЕРНУТЬСЯ и доделать первую.
3. **Готовое решение = merge в main** (`--no-ff`). Только после завершения+верификации (+деплоя для прод-фич). Ветка после мержа удаляется/архивируется.
4. **main всегда актуален** — отражает прод. Нет «кучи потерянных веток» с недокатанным кодом.
5. **Перед стартом новой ветки** — закрыть/смержить текущую (или явно зафиксировать почему держим параллельно — срочность).
6. **Долгоживущую ветку** регулярно подтягивать к main (`git merge main`), иначе дрейф → деплой откатит чужое (см. инцидент ниже).
**Обоснование (AR-01/DO-01):** дивергентные осиротевшие ветки = источник «фикс есть в репо, но не в проде» (инцидент 2026-06-03: #166 ушёл на CPE-ветку, деплой потерял #168-роут). Единая линия main = единый источник истины, нет потери выкаченных фич.

## Проблема, которую предотвращаем
Фикс был закоммичен на feature-ветку (`fix/cpe-ws-thread-leak`), которая ответвилась от СТАРОГО master (до того как на master приехала другая фича — provisioning-эндпоинт). Деплой этой ветки собрал бинарь, в котором **прод-фича отсутствовала** (роут пропал) → 401 в проде. Симптом коварен: и фикс, и фича по отдельности «есть в репо», но НЕ в одном коммите.

## Правила (выполнять ВСЕГДА)

### 1. Перед любым коммитом — проверь активную ветку
`git branch --show-current` (или `git status` сверху). Коммить ровно туда, куда задумано:
- мелкий фикс/серверное изменение → на `master` (или dedicated `fix/<name>` от СВЕЖЕГО master);
- изменение в составе незавершённой фичи → на её feature-ветку.
Не коммить «куда оказался переключён» после возни с ветками/stash.

### 2. Feature-ветку ответвляй от СВЕЖЕГО master
`git checkout master && git pull && git checkout -b fix/<name>`. Если ветка живёт долго, пока master ушёл вперёд — **смержи master в неё перед деплоем** (`git merge master`), иначе деплой ветки откатит чужие прод-фичи.

### 3. Перед деплоем — убедись, что коммит содержит ВСЁ ожидаемое
Деплоить из единой линии (`master`), не из ad-hoc ветки. Проверка-чеклист перед `docker build`:
- `git branch --show-current` = ожидаемая ветка;
- `git merge-base --is-ancestor <последний-задеплоенный-коммит> HEAD` → деплоим потомка, не дивергента (не теряем уже выкаченное);
- grep ключевых строк прод-фич в рабочем дереве (напр. роут/хэндлер), которые ДОЛЖНЫ быть.

### 4. Чистый деплой на VPS (не «грязный pull»)
`git pull <bundle> HEAD` падает на divergent → собирается СТАРЫЙ working-tree молча. Правильно:
```
git fetch <bundle> HEAD
git checkout --detach FETCH_HEAD
git branch -f master FETCH_HEAD && git checkout master
git reset --hard FETCH_HEAD   # убрать грязный working-tree
grep -c '<ключевая строка фичи>' <файл>   # верификация ПЕРЕД build
```

### 5. После деплоя — верифицировать что задеплоено то самое
curl/grep на проде: фича отвечает, ключевой код присутствует в `/opt/.../src`. «git push OK» ≠ «в проде нужный бинарь».

### 6. Сведение веток
Если фикс уехал не туда — не передеплоивать обрывок: **смержить расходящиеся ветки в одну линию** (`git checkout master && git merge --no-ff <ветка>`), убедиться что объединённый коммит содержит всё, и деплоить его.

## Быстрый предполётный чеклист (перед commit→deploy)
1. `git branch --show-current` — та ветка?
2. содержит ли HEAD последний прод-коммит? (`merge-base --is-ancestor`)
3. grep ключевых строк всех ожидаемых фич в рабочем дереве;
4. деплой: fetch + detach + reset --hard + grep-верификация ПЕРЕД build;
5. после: curl/grep на проде.

Связано: deploy-bundle pitfall (detach HEAD), verify-own-deploys.
