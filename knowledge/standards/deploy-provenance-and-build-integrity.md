# Стандарт: deploy-provenance-and-build-integrity

**Статус:** канон (универсально — все компоненты и проекты).
**Происхождение:** вердикт Большой 7, 2026-07-04. Root-cause граблей: vc84-87 stale-builds (номер БЫЛ, но артефакт собран из залипшего worktree → embedded-версия ВРАЛА); integ/build72 — источник задеплоенного кода — жил только локально (риск потери до merge); build72 собирался частичными пакетами.

## Принцип
Задеплоенный артефакт всегда однозначно прослеживается до неизменяемого, запушенного git-ref; версия, вшитая в артефакт, не может врать.

## Правила

1. **Монотонный build-счётчик на компонент.** Номер живёт в git как source-of-truth (versionCode / BUILD в коде), коммитится вместе с изменением, строго растёт каждый релиз. Никакого ручного присвоения номера в момент деплоя.

2. **Источник задеплоенного кода — запушенный ref.** Ветка, из которой собирается деплой (integ или feature — порядок мержа задаёт `scenarios/git-workflow.md`, «Когда merge в master»), немедленно пушится: код, уехавший на прод, не имеет права жить только локально (иначе источник задеплоенного кода можно потерять — так чуть не потеряли integ/build72). **Никаких частичных пакетов** (для OTA — всегда полный пакет всех компонентов; это правило §2 целиком, отдельного стандарта нет); «всё одобренное — разом» — `ship-freshest-approved.md`.

3. **Сборка только из запушенного ref.** Build-скрипты собирают из чистого checkout запушенного git-ref, НЕ из локального/stale worktree. Freshness-guard в скрипте: fail, если рабочее дерево грязное или ref не совпадает с origin.

4. **Artifact↔git checksum-соответствие.** После сборки верифицировать, что версия/build, вшитые в артефакт (versionCode APK, BUILD binary), == коммиту, из которого собрано. Несовпадение = стоп-деплой.

5. **Тег на задеплоенный build.** Каждый задеплоенный артефакт получает immutable git-tag (`cpe-build72`, `mdm-server-<sha>`). Отвечает на «что реально работает в проде?» без гадания.

6. **Provenance-запись.** Логировать: какой commit/tag → куда задеплоен → когда → результат проверки «ничего не сломано» (`regression-safety-on-new-feature`; deploy-лог / паспорт). Дрейф становится обнаружимым.

## DoD
Задача не «done», пока:
- задеплоенное == запушенному, тегированному git-ref;
- нет local-only источника задеплоенного кода;
- проверка «ничего не сломано» (`regression-safety-on-new-feature`, «Механизм») пройдена до/после деплоя.

## Кросс-ссылки
`ship-freshest-approved` · §2 этого файла (полный пакет) · `playbooks/workflow_loop.md` п. 0 (закончить текущее до нового) · `regression-safety-on-new-feature` (ничего не сломано) · `verify-deploy-actually-shipped` (guide) · урок о stale-сборках — в памяти проекта.

История правок: `archive/2026-08/PE-01-разбор-2026-08-24.md`, раздел «Совместная редакция и применение», мелкая 1 (переквалифицирована); вердикты — `archive/2026-08/PQ-01-разбор-2026-08-24.md`; 2026-08-24.

История правок: `archive/2026-08/ревизия-свода-2026-08-24.md`, раздел 13, Б4 (§2: «единая integ-ветка до деплоя» → «источник — запушенный ref», порядок мержа — дом `scenarios/git-workflow.md`; «на все зеркала» снято по решению владельца 24.08 о зеркале; разбор ослабления — PQ-01, п. 1); вердикты — `archive/2026-08/PQ-01-ревизия-2026-08-24.md`, «Дополнение по разделу 13»; 2026-08-25.
