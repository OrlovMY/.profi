# Regression safety on new functionality (HARD)

**При создании любого нового функционала тестами проверять, что предыдущий функционал не деградировал и продолжает работать** — если его удаление/изменение не было запланировано явно.

## Правило
- Новая фича/фикс поставляется не только со «своими» тестами (см. [[task-testing-all-stages]] / tests-mandatory), но и с **подтверждением, что смежный/затронутый существующий функционал жив**.
- «Собралось» и «новый тест зелёный» ≠ «ничего не сломалось». Нужен прогон существующих тестов + проверка ключевых рабочих путей, которые мог затронуть change.
- Если изменение НАМЕРЕННО меняет/удаляет старое поведение — это фиксируется явно (в ТЗ/паспорте/changelog), и тогда регресс на этот путь ожидаем и допустим.
- Применяется на всех слоях: BE (go test всего затронутого пакета + смежных, не только нового файла), FE (tsc + затронутые компоненты), агент/устройство (контракт со смежными модулями), интеграция (OTA/совместимость версий).

## Почему
Узкая проверка только нового кода регулярно прячет регрессии в соседнем поведении. Дешевле поймать тестом в том же PR, чем на проде. Локализация «где сломалось» возможна только если есть зелёный baseline до изменения.

## Как применять
1. До change — знать, какой существующий функционал рядом (импортирующие пакеты, общие данные/таблицы, общий endpoint/воркер).
2. После change — прогнать тесты не только нового, но и затронутого/смежного; проверить рабочий путь руками/логами, если автотеста нет.
3. В отчёте указать: что нового покрыто + что из старого проверено-не-деградировало.
4. Если что-то деградировало незапланированно — change не готов (даже для теста), чинить до завершения.

## Механизм (конкретно — добавлено после инцидента #231, 2026-06-19)
Просто «прогнать тесты затронутого пакета» НЕДОСТАТОЧНО: #231 — новая фича (operator-ЛК) сломала ЧУЖОЙ функционал (device-метрики/SMS/shell) через побочный эффект роутинга (`api.Group("",mw)` налип на чужие маршруты). Узкие unit-тесты этого не ловят. Нужен **контрактный регресс-smoke на ключевые сквозные инварианты**, который гоняется на КАЖДОЕ изменение и как ГЕЙТ деплоя:

1. **Smoke-набор инвариантов** — список «что всегда должно работать», проверяемый end-to-end независимо от того, что меняли. Для MDMy минимум:
   - device-JWT принимается ВСЕМИ `/api/v1/mdm/device/*` (ни одного 401) — поймал бы #231;
   - healthz ok; admin-login ok; checkin 200.
   Реализация: скрипт-смоук (forge-токен + curl по всем endpoint) + Go-тест авторизационной матрицы (`middleware/authmatrix_test.go`).
2. **Гейт деплоя (verify-own-deploys):** после сборки сервера — прогнать smoke ДО объявления «готово». Расхождение код↔рантайм (как #231: симметричный код, асимметричное поведение) ловится только так.
3. **Расширять smoke при каждой новой сквозной фиче** (новый device-endpoint → добавить в матрицу; новый канал → инвариант связи).
4. Где unit/integration не покрывает (рантайм-эффекты Fiber-групп, порядок middleware) — обязателен smoke/контрактный тест, не только модульный.

## Почему именно так
Цена пропуска: #231 = весь флот без health-графиков/SMS/shell ~3 дня, незаметно (checkin жил). Контрактный smoke-инвариант ловит «новое сломало старое» там, где модульные тесты слепы.

Связано: [[task-testing-all-stages]], [[delegate-implementation-to-sonnet]] (в ТЗ Sonnet включать регресс-проверку + smoke), no-regression scenario, verify-own-deploys, [[documentation-first-golden-rule]]. Прецедент: [[feedback_fiber_empty_prefix_group_leak]] (#231).
