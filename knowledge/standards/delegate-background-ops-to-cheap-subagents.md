# Standard: делегировать background-операции (polling / monitoring / wait-for-condition) дешёвым персистентным субагентам

**Статус:** обязательный · **Применимость:** все проекты, любой момент, когда нужен фоновый цикл наблюдения/поллинга/ожидания условия.
**Происхождение:** директива владельца 2026-07-08 → вердикт Большой 7 (APPROVE). Сестринский паттерн к [[delegate-search-to-persistent-haiku]] (та же модель-роль Haiku-персистент; retrieval = найти факт, background-ops = ждать условие).

## Rationale
Каждый тик `Bash run_in_background` + чтение результата расходует контекст/токены координатора (Opus). Background-bash к тому же самозавершается — длинные наблюдения требуют перезапуска. Вынос цикла в персистентного дешёвого субагента переносит итерации в ЕГО контекст; координатор получает только **дистиллят** (итог/значимое событие), не сырые тики.

## §1. Что делегировать (scope)
- Поллинг-до-условия (build_number ≥ N на устройстве, checkin-статус, файл/процесс появился, миграция применилась).
- Deploy/apply-мониторы, soak-watch, ожидание сходимости внешнего процесса.
- Любой цикл «крутить и ждать наступления условия».

## §2. Что НЕ делегировать (остаётся у координатора)
- **Решение-по-результату** (деплой / откат / эскалация) = суждение → Opus/Big-7.
- **Разрушающие / прод-мутирующие действия** (reboot / deploy / UPDATE / ADB-non-read-only). Watch-агент **READ-ONLY**: детектирует условие и возвращает; действие принимает координатор через нормальные prod-гейты (`prod_escalation` / `validate-recovery-before-destructive` не обходить).
- Интерпретация неоднозначного результата.

**🔴 Исключение — owner-авторизованный ПОВТОРЯЮЩИЙСЯ retry-dispatch (owner 2026-07-12, дважды).** Когда owner прямо санкционировал повторяющуюся ОГРАНИЧЕННУЮ идемпотентную операцию (напр. «гоняй retry OTA-раскатки на устройство X пока не встанет»), ВЕСЬ цикл — включая re-dispatch (прод-write) — живёт в дешёвом Haiku-агенте, НЕ в координаторском фоновом bash. Это перекрывает буквальное «прод-мутирующее→координатор» выше для узкого класса: (а) owner явно авторизовал конкретную операцию, (б) ограниченная (один device / фикс-артефакт), (в) идемпотентная (тот же cpe_ota, health-gate+rollback защищают), (г) координатор всё равно судит ИТОГ (verify-гейт) и НЕОБРАТИМЫЕ развилки (reboot→owner). Рационал owner: не жечь токены/контекст координатора на поллинг-с-редким-действием. Общее «прод-write→координатор» держится для НЕ-санкционированных/незапланированных/масштабных мутаций. Credential-hygiene: дешёвый агент НЕ пишет секрет в отдельный .sh/tmp-файл (inline в команде; классификатор флагнул persistent cred-файл).

## §3. Выбор модели (граница простое ↔ суждение)
- **Haiku (default)** — условие остановки записывается как ДЕТЕРМИНИРОВАННЫЙ предикат: сравнение чисел (`build ≥ 110`), grep-совпадение (лог содержит строку), булев (HTTP 200 / файл есть / процесс жив).
- **Sonnet** — цикл требует суждения на каждом тике (многофакторное «здоров ли деплой», неоднозначные логи, решение продолжать/прервать по качественному признаку).
- **Тест границы:** «можешь записать условие остановки как детерминированный предикат?» да → Haiku, нет → Sonnet.
- Действие по итогу — **ВСЕГДА координатор** (Opus/Big-7), не watch-агент любой модели.

## §4. Механика
- Запуск `Agent run_in_background`, **персистентный** (переиспользуется через SendMessage для следующих наблюдений; «персистентный» = reuse, не «никогда не возвращается» — агент возвращает дистиллят текущего наблюдения и ждёт след. задачи через SendMessage, как Big-7/Haiku-search).
- 🔴 **Watch ЖИВЁТ В ПЕРСИСТЕНТНОМ СУБАГЕНТЕ, НЕ в main-треде (owner 2026-07-08, усилено).** Прогонять `Monitor`/поллинг из координатора = тратить контекст координатора на тики — запрещено. Координатор ТОЛЬКО спавнит/реюзит watch-агента и получает дистиллят. (Ранее был обратный workaround «Monitor из main, т.к. Haiku не крутит цикл» — ОТМЕНЁН owner'ом: не тащить watch в main, а научить агента блокироваться правильно.)
- 🔴 **Механизм ожидания внутри watch-агента = инструмент `Monitor` (until-loop), НЕ самонарративный «цикл» в промпте.** Дешёвая модель НЕ умеет «сама крутить цикл с паузой» по текстовой инструкции — делает одну проверку и возвращается. Брифинг watch-агента ОБЯЗАН явно: «используй инструмент **Monitor** с until-условием X и таймаутом T (ОДИН вызов), эмить только на смену состояния/terminal, по завершении верни дистиллят». Foreground `sleep` заблокирован — Monitor штатный механизм блокирующего ожидания. Если watch-агент всё равно возвращается рано, не получив условия — переспавнить/дослать ему через SendMessage «продолжай Monitor до X», НЕ забирать watch в main.
- Явное условие остановки (детерминированный предикат) + **TIMEOUT** (не бесконечно).
- Возвращает ТОЛЬКО итог при достижении условия ИЛИ значимое событие/аномалию — **НЕ сырые тики**.
- **Реюз через SendMessage** для следующих наблюдений (как Big-7 / Haiku-search); связанная работа → тот же agentId (копит familiarity, не платит рамп-ап).
- Токен-экономика: тики живут в контексте субагента, координатор получает дистиллят.

## §5. Границы / анти-паттерны
- Прямой `Bash run_in_background` остаётся ОК **только для одноразовой быстрой команды без цикла** (сборка, разовый запрос, единичный scp). Есть цикл / ожидание-условия → watch-агент.
- **Cap:** не плодить вечные watch-агенты (ср. [[agent-question-rendezvous]] §2) — реюзить существующий через SendMessage, закрывать по завершении программы наблюдения.
- Watch-агент не действует на проде/устройстве — только наблюдает.
- Секреты в возвращаемом итоге — как `file:line`, НЕ значения (наследуется от search-стандарта / SEC-02).

## §6. Связь / иерархия делегирования
Сестринский паттерн к [[delegate-search-to-persistent-haiku]] (та же модель-роль Haiku-персистент; retrieval = найти факт, background-ops = ждать условие).
Иерархия: **Haiku (retrieval | watch) → Sonnet (impl | judgment-loop) → Opus (coord / action-decision) → Big-7 (audit)**. Дешёвые ДЕЛАЮТ механическое (найти/ждать); Opus/Big-7 СУДЯТ и ДЕЙСТВУЮТ.

## Пропагация
Универсальный стандарт, источник истины здесь (`.profi`). В памяти каждого проекта — строка-указатель (одна строка в `MEMORY.md` с `[[...]]`), как для `delegate-search-to-persistent-haiku`. Универсальное знание → `.profi`, НЕ дублировать в проектную память (`knowledge/README.md`, «Что НЕ сюда»).

## Источник
Владелец 2026-07-08: «всё, что запускается в отдельных потоках shell — запускать в не-закрываемых персистентных субагентах; для простых операций Haiku/дешёвая модель, чтобы не расходовать токены координатора». Вердикт Большой 7 (APPROVE, watch-vs-judgment граница, READ-ONLY watch, cap+reuse, дистиллят-не-тики). Связано: [[delegate-search-to-persistent-haiku]], [[agent-question-rendezvous]], [[delegate-implementation-to-sonnet]], `prod_escalation`, `validate-recovery-before-destructive`.
