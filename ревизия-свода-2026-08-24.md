# Ревизия свода 2026-08-24 — разбор PE-01 (первая ревизия по rule-expiry-and-ownership.md, «Ревизия»)

PE-01 [2026-08-24]. **Статус: предложения.** Свод этим файлом не правлен, ничего не закоммичено. Применение каждого пункта — только после вердикта PQ-01, отдельным шагом (порядок прежний; поручение владельца 24.08: «всё доделать, чтобы двигаться дальше» — доделано здесь до состояния «готовые тексты», дальше очередь PQ-01).

**Основание:** `knowledge/standards/rule-expiry-and-ownership.md`, раздел «Ревизия» (строки 38–48), прочитан целиком. Предписано: раз в квартал пройти по датам, отметить не сработавшее, архивировать устаревшее; первая ревизия — до 26.08.2026; артефакт — раздел «Ревизии свода» в `README.md` со строкой счёта, K — командой из `scenarios/session-start.md`, Шаг 1.5.

**Входы:** пять линз разведки (journal.jsonl workflow `wf_f724cda3-7c6` — все пять результатов прочитаны); `ответ-MDMy-2026-08-24-v2.md` (целиком); черновик стандарта в `PE-01-разбор-2026-08-24.md:1167`; Д1 в `PE-01-разбор.md:1549–1563`; собственные команды по worktree (ветка = origin/main = 8022a35). Каждое утверждение линз, на котором стоит правка, перепроверено своей командой или чтением файла; где не перепроверено — граница названа в поле «Проверено существованием».

---

## 1. Счёт ревизии

| Разряд | Значение | Команда / определение множества |
|---|---|---|
| K — проверено файлов | **62** | `ls README.md knowledge/standards/*.md scenarios/*.md playbooks/*.md roles/hr-d.md roles/big7/INDEX.md \| wc -l` → 62; та же команда, что M в `scenarios/session-start.md:87` (требование rule-expiry:48), выполнена мной в worktree 24.08 — сошлось с датированным числом в тексте Шага 1.5. Определение: обязательный набор старта — перечень, а не признак (session-start.md:78–83). Глубина проверки — пять линз разведки по всему корпусу (включая guides/recipes/templates/roles вне набора) плюс мои проверки существованием; «проверено» = пройдено линзами по датам/срокам/артефактам/ссылкам, а не «прочитан каждый байт каждым». |
| N — не сработало | **4** | Поимённый список — раздел 3. Определение: правило свода, чей обязательный артефакт или заявленное состояние на 24.08.2026 отсутствует либо ни разу не предъявлено в предписанной форме (по журналам сессии 24.08 и своду). Множество зависит от состояния, не от формы → поимённый список, не образец (rule-expiry п. 5). |
| M — архивировано | **0** | Единственный найденный класс кандидатов — 5 заглушек оси ask-or-act — имеет несозревшее условие снятия, записанное в них самих («после правки указателей в памятях проектов»); архивировать до выполнения условия — сломать адреса, ради которых заглушки и оставлены. Раздел 6. |

**Проект строки для README.md** (вставляется после вердикта PQ-01):

```
## Ревизии свода

Порядок и форма — `knowledge/standards/rule-expiry-and-ownership.md`, «Ревизия».

Ревизия свода: 2026-08-24 · проверено файлов: 62 · не сработало: 4 · архивировано: 0 · разбор: ревизия-свода-2026-08-24.md
```

---

## 2. Правки полной формой

### П1. `roles/big7/pe-01.md:3` — дата шапки

**Было:** «**Файл датирован 2026-08-19.** Всё, что здесь сказано о составе команды, моделях и структуре свода, — на эту дату. Устаревшую строку правят, а не дополняют.»
**Стало:** «**Файл датирован 2026-08-24.** Всё, что здесь сказано о составе команды, моделях и структуре свода, — на эту дату. Устаревшую строку правят, а не дополняют.» (Применяющий ставит дату дня применения, если он не 24.08.)
**Почему:** строка неверна фактом: файл правлен 24.08.2026 (коммит 68fef96), датировка осталась 19.08 — шапка нарушает собственное третье предложение. С 19.08 менялись ровно те предметы, актуальность которых шапка гарантирует: состав (Большая 10, `INDEX.md` «Состав на 24.08.2026»), модели (запрет Fable снят владельцем 24.08), структура свода (пять стандартов сведены в ask-or-act 24.08).
**Проверено существованием:** `git log -- roles/big7/pe-01.md` → 68fef96 2026-08-24; INDEX.md прочитан целиком; big7-consultation.md:33–34 (снятие запрета) — по совпавшим результатам линз 1 и 3; grep по метке TEMP я не выполнял — граница: строку о снятии запрета сам не открывал, на неё правка не опирается.
**Регрессы:** нет — датировка становится точнее; у pq-01.md:3 та же дата 19.08 честная (git: не менялся с 49610ab 2026-08-19, проверено командой) — его не трогать.

### П2. `roles/big7/pe-01.md:135` — проектный мандат в универсальном файле роли

**Было:** «Выкладка работе не мешает: в этом проекте она непрерывна (мандат владельца 11.08.2026), и ждать её окончания значит не работать никогда.»
**Стало:** «Выкладка сама по себе — не инцидент и работе не мешает. Непрерывна ли она в конкретном проекте — частность этого проекта и живёт в его памяти. Датированный случай: в проекте-источнике владелец 11.08.2026 объявил выкладку непрерывной, и ждать её окончания значило не работать никогда.»
**Почему:** старая редакция держит действующее проектное указание («в этом проекте», мандат 11.08) в универсальном файле роли — против прямого указания владельца 24.08.2026 «Проектное — в проекте» (`knowledge/README.md:26`, README.md:55): читатель из другого проекта не может определить, какой проект «этот» и действует ли мандат у него. Тот же класс уже чинился 24.08 в `git-dr-mirror-sync.md` (частность → память проекта, в своде — датированный случай).
**Проверено существованием:** knowledge/README.md:20–40 прочитаны (раздел целиком, включая оговорку про датированный случай); README.md прочитан целиком; git-dr-mirror-sync.md:30 — по дословно совпавшим цитатам линз 1 и 3, сам открывал этот файл на строках 24–28 и 41–45, строку 30 — нет (граница).
**Регрессы:** смысл мандата сохраняется датированным случаем; риск — в памяти MDMy мандата 11.08 может не быть записи (память погибла 22.08). Условие применения: исполнитель MDMy подтверждает запись мандата в живой памяти; нет записи — сначала запись, потом эта правка (иначе указание владельца потеряет последний дом).

### П3. `README.md:5–15` — раздел «Что нового по сравнению с прошлой версией»

**Было:** заголовок «## Что нового по сравнению с прошлой версией» и девять строк под ним: история трёх поколений состава в обратной хронологии («Большая 7 вместо Большой 6» — без даты; «Большая 10 вместо Большой 9 (19.08.2026)» ВЫШЕ «Большая 9 вместо Большой 7 (18.08.2026)»), недатированная строка PE-01/PQ-01 (дата есть), и пять строк-описаний механизмов (`playbooks/`, `templates/`, Phase A/B, Production escalation, Resource tracking).
**Стало** (весь раздел, вставляется как есть):

> ## История состава и ролей
>
> Датированные записи в хронологическом порядке; текущее состояние здесь не описывается — состав ядра живёт в `roles/big7/INDEX.md` и не дублируется.
>
> - **Дата не записана** (помечено при ревизии 24.08.2026) — добавлен `SEC-02` (white-hat pentester) как постоянный security-гейт после каждого изменения; Большая 7 вместо Большой 6.
> - **18.08.2026, прямое указание владельца** — добавлены `UX-01` (структура и навигация) и `UI-01` (внешний вид и тексты) как ПОСТОЯННЫЕ участники проверок; Большая 9 вместо Большой 7. Повод: «количество функций растёт, а ты их просто накидываешь, не используя вкладки» и «иногда перевод не применяется». Оба проверяют В МОМЕНТ изменения, а не потом.
> - **19.08.2026, прямое указание владельца** — новые роли и серьёзные задания составляет `PE-01`, проверяет `PQ-01`; согласие PQ-01 обязательно ДО запуска. Повод: роли и задания писались по наитию — UX-01 завели с числовой границей вместо проектной работы, и он молчал, пока десять родственных разделов лежали в меню рядом. Первое, что эти двое делают, — проверяют СВОИ СОБСТВЕННЫЕ инструкции: составитель, чья инструкция не выдерживает его же требований, не имеет права править чужие.
> - **19.08.2026, прямое указание владельца** — добавлен `QA-02` (проверяющий интерфейс руками); Большая 10 вместо Большой 9. Повод: «QA по сайту должен проверять, что все кнопки/вкладки/ссылки/раскрываемые меню/скруллы/клики правой клавишей и т.д. работают так как ожидается». Найдено в тот же день: вкладка «Установка» на странице подключения не открывалась В ПРИНЦИПЕ, и ни один прогон этого не поймал — логика была «правильной» с точки зрения кода. QA-02 не читает код и не верит прогонам: открывает продукт и трогает его, под каждой ролью и после каждого выката.

Пять строк-механизмов удаляются из раздела; их смысл переезжает или уже дома: `playbooks/` и `templates/` — целиком покрыты разделом «Структура» (строки 24–25); Phase A/B — «Ключевые принципы» (строка 48); Production escalation — «Ключевые принципы» (строка 47, дубль по содержанию); Resource tracking — единственная непокрытая, переезжает в «Ключевые принципы» новой строкой:

> - **Resource tracking** — фиксация peak CPU/RAM/disk/bandwidth в финальных отчётах агентов; перенос в журнал — частью приёмки отчёта. → `playbooks/resource_tracking.md`

**Почему:** заголовок обещает дифф против «прошлой версии», которая нигде не определена; раздел — нарост истории трёх поколений в обратной хронологии, то есть ровно механизм «дополняли, а не правили» (rule-expiry п. 1), и он читается в обязательном наборе каждый старт как актуальный; строка SEC-02 не датирована; четыре строки — дубли «Структуры» и «Ключевых принципов», а дубль расходится при первой же правке (rule-expiry п. 4). Датированные случаи-поводы (А2) сохранены дословно — удаляется рамка, не опыт.
**Проверено существованием:** README.md прочитан целиком (66 строк, `wc -l` = 66); дубли сверены построчно (11–15 против 24–25 и 47–49); внешних ссылок на заголовок нет: `grep -rn "Что нового" --include="*.md"` по своду → только README.md:5.
**Регрессы:** дата добавления SEC-02 не восстановлена — честная пометка вместо выдуманной даты; порядок «удаляемых» строк проведён по форме «перед удалением» (происхождение: описание диффа неназванной версии; что защищают: ничего уникального, покрытие названо построчно; куда переезжает: названо).

### П4. Кворум прод-эскалации — два дома, уже разошедшиеся по существу

**Было (дом 1 — `playbooks/prod_escalation.md:47–52`):**
«## Состав Big 7 для эскалации … Минимум три голоса: **PM-01 (priority/risk) + DO-01 (technique) + SEC-01 (security)**. Дополнительно по необходимости: QA-01 — если есть тест-импакт. AR-01 — если затрагивает архитектуру. SEC-02 — если security-surface меняется.»
**Было (дом 2 — `roles/big7/INDEX.md`, «Минимальный кворум»):**
«- Прод-эскалация: минимум `PM-01 + DO-01 + SEC-01` (плюс SEC-02 для security-affecting).»

**Стало (единственный дом — INDEX.md, строка кворума):**
«- Прод-эскалация: минимум `PM-01 + DO-01 + SEC-01`; **SEC-02 обязателен, если меняется security-surface**; дополнительно по предмету — QA-01 (тест-импакт), AR-01 (архитектура).»

**Стало (prod_escalation.md, вместо раздела 47–52 целиком):**
«## Кворум эскалации

Кворум — `roles/big7/INDEX.md`, «Минимальный кворум»; здесь не дублируется.»

**Тем же ходом (второй вход):** в prod_escalation.md:71 фрагмент «тот же кворум, что в «Состав Big 7 для эскалации» выше и `roles/big7/INDEX.md`» → «тот же кворум, что в `roles/big7/INDEX.md`, «Минимальный кворум»».

**Почему:** у одного требования две независимо правимые редакции — признак дубля по rule-expiry п. 4, и они уже разошлись: INDEX делает SEC-02 частью кворума при security-affecting, плейбук ставит его в список «дополнительно по необходимости»; QA-01/AR-01 в INDEX вовсе не названы. Дом — INDEX: он сам объявляет «Этот файл: состав + кворум» (строка 5), внесён в гейт старта поимённо (session-start.md:83, п. 5) и стал единственным домом состава правкой Д2. Заголовок «Состав Big 7 для эскалации» заодно называет состав числом против INDEX.md:3. Из двух редакций SEC-02 выбрана более строгая (обязателен) — выбор в сторону усиления безопасности; ослаблять мне запрещено, обе редакции датированы одним днём, страховка выбора — вердикт PQ-01.
**Проверено существованием:** prod_escalation.md:40–76 прочитаны; INDEX.md целиком; session-start.md:83; входы посчитаны командой: `grep -rn "Состав Big 7 для эскалации"` по своду → ровно 2 (prod_escalation.md:47 и :71), обе правятся этим ходом.
**Регрессы:** «QA-01 при тест-импакте, AR-01 при архитектуре» не теряются — перенесены в строку INDEX; риск — INDEX-строка стала длиннее (перегруз п. 7 роли) — одна строка, приемлемо.

### П5. `knowledge/standards/git-branch-discipline.md:56` — срок долга наступил этой ревизией

**Было (хвост выноски):** «…скрипт дал **226 строк `MERGED(local)` при 239 локальных ветках** — долг записан со сроком до первой ревизии свода (26.08.2026); текущее состояние долга — в памяти того проекта, не здесь.»
**Стало:** «…скрипт дал **226 строк `MERGED(local)` при 239 локальных ветках** — долг записан со сроком до первой ревизии свода (26.08.2026). На первой ревизии (24.08.2026) долг погашен не был: замер исполнителя того же дня — 23 влитых при 37 локальных, причём падение с 226 не объяснено записью об уборке; срок перенесён на 07.09.2026 датированной записью в памяти того проекта (`memory/project_branch_cleanup_debt_2026-08-24.md`). Следующий перенос — тоже только датированной записью при ревизии или решением владельца: молчаливый перенос делает оговорку постоянной, о чём этот абзац и написан. Текущее состояние долга — в памяти того проекта, не здесь.»
**Почему:** после 24.08 старая редакция неверна умолчанием: срок «до первой ревизии» наступил, и текст, не фиксирующий исход, превращает оговорку ровно в то, от чего абзац предостерегает строкой выше («оговорка без даты превращается в постоянную»). Долг не прячется: числа, необъяснённое падение 226→23 и новый срок названы в самом правиле; вопрос о необъяснённой уборке — на владельце (раздел 9).
**Проверено существованием:** git-branch-discipline.md:50–60 и :100–105 прочитаны; замер 23/37 и срок 07.09 — `ответ-MDMy-2026-08-24-v2.md`, шаг 6 и раздел 12 (прочитан целиком); сама запись `memory/project_branch_cleanup_debt_2026-08-24.md` существованием НЕ проверена — память MDMy из worktree недоступна, адрес взят из «Истории правок» этого же файла (:104) и сообщения коммита 84640ad; граница названа.
**Регрессы:** в тексте появляется величина состояния (23/37) — по rule-expiry п. 6 величины не фиксируют; здесь это датированный замер-случай при дате (как уже стоящие 226/239), не текущее состояние — текущее по-прежнему отсылается в память проекта.

### П6. `scenarios/big7-consultation.md:16` — строка учёта агентов (не сработало, №2–3)

**Было:** «1. Координатор ведёт в чате сессии строку `Агенты: ядро=<id>, поиск=<id>, доки=<id>, наблюдение=<id, если запущен>` и обновляет её при каждом спавне.»
**Стало:** «1. Координатор ведёт строку `Агенты: ядро=<id>, поиск=<id>, доки=<id>, наблюдение=<id, если запущен>`, обновляя её при каждом спавне, — и **предъявляет её в записи сессии** (итоговый отчёт или файл разбора — место, у которого есть дата изменения). Чат сессии задним числом не наблюдаем, и строка, живущая только в нём, непроверяема (`rule-expiry-and-ownership.md`, п. 2). Запись сессии, в которой агенты спавнились, а строки нет, приёмку не проходит — как «нет строки — роль не созывалась» ниже.»
**Почему:** за полный день работы 24.08 строка не предъявлена ни разу — в журналах только цитаты самого правила (PE-01-разбор-2026-08-24.md:746 — цитата, PQ-01-разбор-2026-08-24.md:393 — вердикт о тексте правила). Причина не в дисциплине, а в устройстве: артефакт назначен месту без даты последнего изменения, проверка не может упасть — тот же класс, что оговорка исполнителя MDMy о гейте старта («исполнитель сам себе судья»). Лечение — по новому стандарту Н1: шаг привязан к обязательному действию с отказом (приёмка записи сессии), частота носителя равна частоте шага.
**Проверено существованием:** big7-consultation.md:14–18 прочитаны; `grep -n "Агенты:" PE-01-разбор-2026-08-24.md PQ-01-разбор-2026-08-24.md` → 746 и 393, обе — правило, не применение; rule-expiry п. 2 прочитан.
**Регрессы:** соседняя строка «жив, консультаций: N» (:17) — не сработавшее №3 — отдельной правки не получает: она предъявляется в момент проверки жизни, и та же обязанность «предъявить в записи сессии» покрывает её (ответ агента цитируется в записи); если PQ-01 сочтёт покрытие недостаточным — дополнение туда же.

### П7. Журнал ресурсов — четыре входа, носителя нет (не сработало, №1)

**Было (дом — `playbooks/resource_tracking.md:11`):** «После любого деплоя или инцидента, где зафиксирован heavy resource usage, добавлять запись в `memory/project_resource_tracking.md`. Также включать в финальные отчёты агентов вопрос «какие ресурсы это потребляло» (см. `templates/agent_briefing.md`).»
**Стало:** «Запись в `memory/project_resource_tracking.md` — **тем же ходом, каким принимается финальный отчёт deploy-агента**: отчёт, фиксирующий heavy resource usage, не считается принятым, пока строка не легла в журнал. Сбор чисел уже обязателен в самом отчёте (раздел «Что обязательно собрать…» ниже); перенос — часть приёмки, а не отдельный шаг «потом»: отдельный ручной шаг перестаёт делаться ровно тогда, когда работы много (`attach-recurring-step-to-mandatory-action.md`). Признак несрабатывания: дата изменения журнала старше даты последнего деплоя с heavy usage (`rule-expiry-and-ownership.md`, п. 2). В проекте, где журнал не заведён или погиб, — сначала завести файл по `templates/resource_log.md`: на 19.08.2026 журнал проекта-источника был 84 дня без записей, 22.08 погиб вместе с памятью, в реконструкцию 24.08 не вошёл — и четыре места свода продолжали требовать запись в несуществующий файл. Также включать в финальные отчёты агентов вопрос «какие ресурсы это потребляло» (см. `templates/agent_briefing.md`).»

**Тем же ходом — три входа-потребителя сводятся в ссылки** (сейчас это независимо правимые пересказы — дубль по rule-expiry п. 4):
- `playbooks/deploy_workflow.md:117`: «HR-D переносит в `memory/project_resource_tracking.md`.» → «Перенос чисел в журнал ресурсов — частью приёмки отчёта (`playbooks/resource_tracking.md`, «Правило HR-D»).»
- `roles/hr-d.md:36` (п. 9): «**Resource tracking** — переносить peak CPU/RAM/disk/bandwidth из отчётов агентов в `memory/project_resource_tracking.md`.» → «**Resource tracking** — перенос чисел из отчётов агентов в журнал ресурсов частью приёмки отчёта (`playbooks/resource_tracking.md`, «Правило HR-D»).»
- `scenarios/post-incident-memory-update.md:12` (триггер 6): «**Resource observation.** Запись в `project_resource_tracking.md`.» → «**Resource observation.** Запись в `project_resource_tracking.md` (порядок — `playbooks/resource_tracking.md`, «Правило HR-D»).»

**Почему:** правило-производитель журнала не сработало ни разу за жизнь свода: 84 дня тишины на 19.08 (rule-expiry:52), носитель погиб 22.08 и не реконструирован — `grep -ci "ресурс"` и `grep -ci "resource"` по заданию и ответу MDMy v2 дают 0/0/0/0 (моя команда, совпало с линзой 4) — при четырёх требующих входах. Rule-expiry п. 2 велит «вернуть в работу или честно понизить до пожелания»; возвращаю в работу, потому что у шага есть обязательный частый носитель (приёмка отчёта deploy-агента — каждый деплой), то есть стандарт Н1 применим и понижение было бы преждевременной капитуляцией. Поручение исполнителю MDMy: завести журнал заново по `templates/resource_log.md`, первая запись — ближайший деплой.
**Проверено существованием:** resource_tracking.md прочитан целиком (раздел «Что обязательно собрать в финальном отчёте deploy-агента» существует); deploy_workflow.md:113–120, hr-d.md:33–40, post-incident-memory-update.md целиком, templates/resource_log.md:1–15 — прочитаны; входы посчитаны: `grep -rn "project_resource_tracking"` по своду → resource_tracking.md:11, deploy_workflow.md:117, hr-d.md:36, post-incident:12 (правятся) + memory_management.md:40 (пример журнала, не требование — не вход), memory_index.md и resource_log.md (указатель и шаблон — не входы).
**Регрессы:** приёмка отчётов становится строже (отчёт можно «не принять») — это и есть механизм отказа; «heavy resource usage» остаётся без числового порога — как в старой редакции, определение не ужесточаю.

### П8. `templates/memory_index.md` — в каркасе нет раздела «RESUME / активные программы»

**Было:** каркас MEMORY.md с разделами Status → ОБЯЗАТЕЛЬНО ДО ПЕРВОГО ДЕЙСТВИЯ → Doctrine → … — раздела «RESUME / активные программы» нет.
**Стало:** после раздела «ОБЯЗАТЕЛЬНО ДО ПЕРВОГО ДЕЙСТВИЯ», перед «Doctrine», вставить:

> ## RESUME / активные программы
>
> Первая незакрытая запись — действующая программа работ; её читают первой на старте (`scenarios/session-start.md`, Шаг 2). Новая программа встаёт наверх тем же ходом, которым заводится; закрытая помечается ✅ и уходит вниз (`playbooks/memory_management.md`).
>
> - [<активная программа> (<YYYY-MM-DD>)](project_<...>.md) — <что делаем, где остановились>
> - ✅ [<закрытая программа>](project_<...>.md) — закрыта <YYYY-MM-DD>

**Почему:** три места свода велят читать «первую незакрытую запись раздела RESUME» (session-start.md:63 и :97, workflow_loop.md:9), производитель существует (memory_management.md:84–86, с артефактом), а каркас, из которого новый проект получает MEMORY.md, раздела не содержит — протокол старта нового проекта упирается в несуществующий раздел. Классический rule-expiry п. 5: правка про RESUME легла не на все входы.
**Проверено существованием:** memory_index.md прочитан целиком; session-start.md:63, :97, workflow_loop.md:9, memory_management.md:75–86 — прочитаны; `grep -rn "RESUME" scenarios/ playbooks/ templates/` → шесть вхождений, в templates/ — ноль.
**Регрессы:** нет — дополнение каркаса, существующие проекты не трогаются.

### П9. `scenarios/post-incident-memory-update.md:34` — «Раз в N недель»

**Было:** «## Cleanup ритуал

Раз в N недель:»
**Стало:** «## Cleanup ритуал

Раз в квартал, тем же заходом, что ревизия свода (`knowledge/standards/rule-expiry-and-ownership.md`, «Ревизия»), — чтобы календарь оставался один. Артефакт — строка «Уборка памяти: <дата>» в `MEMORY.md` проекта: квартал прошёл, свежей строки нет — ритуал не выполнялся, и это чинят, а не терпят. Чаще — можно; квартал — нижняя планка. Шаги:»
**Почему:** «N недель» — плейсхолдер без значения: по rule-expiry п. 2 правило непроверяемо — «не менялось дольше, чем правило требует» не вычислить, когда «требует» не названо; артефакта срабатывания тоже нет. Привязка к календарю ревизии — тот же приём, которым rule-expiry:40 связал собственные два срока («чтобы календарь оставался один»).
**Проверено существованием:** post-incident-memory-update.md прочитан целиком; rule-expiry:38–48 прочитаны.
**Регрессы:** проекту с бурно растущей памятью квартала может быть мало — оговорка «чаще — можно» оставляет это в руках проекта.

### П10. `knowledge/standards/rule-expiry-and-ownership.md:40` — исход первой ревизии (дополнение, не замена)

**Было:** «Раз в квартал одним заходом: пройти по датам, отметить не сработавшее, архивировать устаревшее. **Первая ревизия — до 26.08.2026** (тот же срок, что у вопроса владельцу о реконструированном правиле, чтобы календарь оставался один).»
**Стало (дописать хвост):** «…чтобы календарь оставался один). Первая ревизия проведена 24.08.2026 (строка — `README.md`, «Ревизии свода»; разбор — `ревизия-свода-2026-08-24.md`). Вопрос владельцу о реконструированных правилах жив: задан исполнителем 24.08.2026 со сроком ответа 07.09.2026, записью в памяти проекта-источника — там же перенесённый срок долга уборки веток.»
**Почему:** это дополнение, а не замена — старая строка не была неверна, она исполнена; но без записи исхода «до 26.08.2026» читается как всё ещё открытое обязательство, а вторая половина общего календаря (вопрос о реконструкции) молча уехала бы на 07.09 без следа в своде — вопрос со сроком, живущий только в памяти одного проекта, уже терялся ровно так 22.08.
**Проверено существованием:** срок 07.09 и формулировка вопроса — `ответ-MDMy-2026-08-24-v2.md`, шаг 3 и раздел 12 п. 1 (прочитан целиком); `grep -rn "реконструир"` по своду — вне rule-expiry:40 нуля не проверял сам, опираюсь на совпавший результат линз 3 и 4 (граница).
**Регрессы:** нет — датированная запись исхода.

---

## 3. «Не сработало» — поимённый список (N = 4)

1. **Журнал ресурсов** (`memory/project_resource_tracking.md`). Требуют 4 места свода (resource_tracking.md:11, deploy_workflow.md:117, hr-d.md:36, post-incident-memory-update.md:12; команда счёта — в П7); носитель не существовал фактически никогда (84 дня тишины на 19.08 — rule-expiry:52), погиб 22.08, не реконструирован (grep по заданию/ответу v2 → 0). Решение: вернуть в работу привязкой к приёмке отчёта deploy-агента — **П7**; поручение MDMy завести файл.
2. **Строка учёта агентов** `Агенты: …` (big7-consultation.md:16). Ни разу не предъявлена в предписанной форме за день работы 24.08 (grep по обоим журналам разбора — только цитаты правила). Причина конструктивная: носитель без даты изменения. Решение: **П6** (предъявление в записи сессии, приёмка с отказом — по стандарту Н1).
3. **Ответ «жив, консультаций: N»** (big7-consultation.md:17). В журналах — только цитаты правила. Решение: покрывается П6 (та же запись сессии); отдельной правки не предлагаю — см. «Регрессы» П6.
4. **«Все прочие файлы… состава не перечисляют — ни числом, ни списком»** (INDEX.md:3). Заявлено выполненным, выполнено не до конца: кворум продублирован поимённо (prod_escalation.md:49–52 — чинит **П4**), имя-с-числом «Big 7 / Большая семёрка» живёт в нормативной прозе 13 файлов (55 вхождений — счёт и команда в разделе 5). Решение: П4 сейчас; проза — ходом Д1 (раздел 5, ждёт владельца).

Отдельно, НЕ в счёт N (вернулось в работу, для полноты картины): форма «Регрессы:» из no-regression.md — 0 заполнений на 19.08, сейчас 30 в PE-01-разборе + 1 в PQ-01-разборе (строка 156 — упоминание поля, не заполненная форма) (линза 4; в этом файле — по строке на правку); «История правок» на чужих правках свода — все 3 файла коммита 84640ad строку имеют.

---

## 4. Новые файлы (полные тексты; заводятся после вердикта PQ-01)

### Н1. `knowledge/standards/attach-recurring-step-to-mandatory-action.md`

**Решение о составе:** базовый текст — дословно черновик из `PE-01-разбор-2026-08-24.md:1167` («вставляется как есть»); в него врезаны четыре дополнения исполнителя MDMy (`ответ-MDMy-2026-08-24-v2.md`, раздел 13) — пунктами 1а, 2а, 3а и оговоркой в «Связано». Заводить только вместе: дополнения адресованы этому файлу и, заведи его кто-то по одному черновику, потерялись бы (это заметила и линза 2). Нумерация врезок моя; содержание — исполнителя. Сверка для PQ-01: черновик — разбор:1167, дополнения — ответ-файл, раздел 13.

Полный текст файла:

> # Standard: повторяющийся шаг — к обязательному действию, а не к памяти человека
>
> **Статус:** обязательный · **Применимость:** любой проект; любой повторяющийся шаг сопровождения — перенос памяти в репозиторий, синхронизация зеркала, резервная копия, обновление указателя, — который держится на «не забывать делать».
> **Происхождение:** MDMy, 19.06–22.08.2026 (раздел «Случай»); формулировка — исполнителя MDMy, 24.08.2026; пункты 1а, 2а, 3а и оговорка в «Связано» — дополнения того же исполнителя по практике 24.08.2026 (`ответ-MDMy-2026-08-24-v2.md`, раздел 13), заведены вместе со стандартом при ревизии свода 24.08.2026.
>
> ## Правило
> **Ручной повторяющийся шаг перестаёт делаться ровно тогда, когда работы много, — то есть когда он нужнее всего.** Поэтому шаг, от которого зависит сохранность данных или работоспособность, либо встраивается в действие, которое исполнитель и так обязан совершать — и это действие отказывает, пока шаг не выполнен, — либо честно объявляется отсутствующим: «делаем руками, когда вспомним» защитой не считается.
>
> 1. **Носитель — обязательное действие**, мимо которого нельзя пройти к цели исполнителя: выкат, merge, гейт старта сессии. Не расписание и не напоминание: у расписания есть свой ручной шаг («посмотреть, что упало»), и умирает он под той же нагрузкой.
>
>    1а. **Носитель обязан быть частым, а не только обязательным:** частота носителя — не реже, чем допустимая давность пропущенного шага. Шлюз на редком действии молчит ровно столько, сколько действие не совершается, и отказ снова становится незаметным. Выкат в случае ниже годится не потому, что обязателен, а потому, что 24.08.2026 случился четыре раза за день. Носителя нужной частоты нет — правило не применимо, и это признаётся записью (п. 4), а не привязкой к редкому.
>
> 2. **Отказ, а не предупреждение.** Механизм обязан уметь остановить носителя (ненулевой код возврата, красный гейт): предупреждение пролистывают ровно тогда же, когда забывают ручной шаг.
>
>    2а. **Аварийный обход предусмотрен — и непроизносим.** Отказ без обхода рано или поздно снимут целиком, в тот час, когда он помешает срочному; обход, набираемый по привычке, равносилен его отсутствию. Обход формулируется так, что его нельзя применить машинально, и сам называет цену. Образец из случая: `MDMY_MEMORY_GATE=skip-and-i-know-169-files-were-lost`.
>
> 3. **Признак несрабатывания назван при введении** (Б1): по какому наблюдаемому состоянию видно, что механизм перестал защищать. Пример из случая: живой файл памяти недостижим из удалённых веток → выкат отказывает кодом 4.
>
>    3а. **Механизм при введении показан отказывающим на подсадке, и подсадка описана.** Назвать признак недостаточно: 24.08.2026 три из четырёх свежезаведённых проверок были зелёными и слепыми, пока им не подсунули то, что они обязаны поймать, — поиск секретов нашёл один из трёх, проверка нулевых байтов считала строки вместо нулей, `chmod 600` не делал ничего и рапортовал успехом. Без подсадки пункт 3 сам оказывается правилом класса «проверка, которая не может упасть».
>
> 4. **Граница.** Не действует на однократные шаги и на шаги без обязательного носителя (в том числе носителя нужной частоты, п. 1а); там решение остаётся за владельцем и записывается («шаг остаётся ручным, потому что …»), а не живёт молчаливым умолчанием.
>
> ## Случай
> MDMy: память проекта велась в приватном git-репозитории с первого коммита, но перенос в него был ручным и встал на `d85e2c84` от 19.06.2026 — за два месяца до того, как деструктивный тест 22.08.2026 стёр живую память. Замер по уцелевшему фрагменту указателя (180 имён, нижняя граница): 1 нашёлся в git, 11 — в живой памяти, 169 — нигде; из окна 19.06–22.08 не вернулось ничего. Репозиторий был; умер шаг. Решение исполнителя 24.08.2026: `infra/scripts/sync-memory.sh` (секреты → копирование → фиксация → push) + шлюз памяти в `deploy.sh` — выкат, единственный путь на прод, отказывает кодом 4, если файл живой памяти недостижим из удалённых веток.
>
> ## Связано
> - `enforced-rotation-for-self-stored-transient-data.md` — тот же принцип для растущих данных («ручная уборка = не считается»); тот стандарт — про данные и переполнение, этот — про шаги и потерю.
> - `git-dr-mirror-sync.md` — образец уже привязанного шага: push в зеркало «после **каждого** push», а не «по расписанию».
> - `scenarios/session-start.md` (гейт памяти) — отказ, а не напоминание, на старте сессии. **Оговорка:** гейт старта не наблюдаем извне — исполнитель сам себе судья; отказ выката останавливает работу видимым образом. Это разные по силе носители, и равнять их нельзя (замечание исполнителя MDMy, 24.08.2026).

**Почему (для нового файла — почему «правила нет» неверно):** обосновано в PE-01-разбор-2026-08-24.md при выдвижении кандидата (три основания: дыра реальна — ближайший `enforced-rotation-…` ограничен своей строкой 3 растущими данными; случай свежий, дорогой, с числами; механизм и артефакт есть). Дополнения закрывают щели, каждая из которых 24.08 открылась делом.
**Проверено существованием:** черновик и обоснование прочитаны (разбор:1130–1260); ответ-файл раздел 13 прочитан; `ls knowledge/standards/ | grep -i attach` → пусто, файла нет. НЕ проверено (граница, та же, что у черновика): `sync-memory.sh`, код 4, замер 1/11/169, коммит `d85e2c84` — репозиторий MDMy отсюда недоступен, числа раздела «Случай» — из одного источника без второго прогона; расхождение 1+11+169=181 при 180 именах записано в разборе как открытый вопрос исполнителю.
**Регрессы:** стандарт добавляет обязательный набор на 1 файл (K вырастет до 63+2=65 с Н2, Н3) — рост обязательного чтения назван, а не молчалив.

### Н2. `knowledge/standards/recovered-data-acceptance.md` — дом для признаков (г), (д), (е)

**Решение о доме:** новый стандарт, а не дополнение существующего и не recipe. Дополнять нечего: правило приёмки с признаками (а)–(в) живёт только в тексте разового задания (`задание-MDMy-2026-08-24-v2.md:15`) — журнальный файл, который не входит в обязательный набор и не читается ни одним гейтом; оставить признаки (г)–(е) там же — значит дописать три строки в файл, который следующий восстанавливающий не откроет (класс «переезд обязательного за пределы читаемого по гейту» — pe-01.md, только здесь правило и не въезжало в свод). Стандарт, а не recipe: это планка приёмки («файл считается живым, только если…»), а не последовательность команд — по определениям `knowledge/README.md` («standards — минимальные планки», «recipes — конкретные команды»). Класс кросс-проектный: undelete, бэкап, копии с суффиксами — любой проект после потери.

Полный текст файла:

> # Standard: приёмка восстановленных данных — по содержимому, не по имени и размеру
>
> **Статус:** обязательный · **Применимость:** любой файл, вернувшийся из недоверенного источника восстановления — undelete/carving, спасательный фонд, старый бэкап, копия с суффиксом. К файлам, взятым из живого git-репозитория, не применяется: их целостность держит сам git (но см. «Порядок» — git проверяется раньше, чем фонд).
> **Источник:** катастрофа памяти MDMy 22–24.08.2026. Признаки (а)–(в) — правило приёмки из `задание-MDMy-2026-08-24-v2.md`, шаг 2 (24.08.2026); признаки (г)–(е) и порядок «git первым» — находки исполнителя MDMy (`ответ-MDMy-2026-08-24-v2.md`, разделы 4–6). Заведён при ревизии свода 24.08.2026: до этого правило жило только в тексте разового задания.
>
> ## Порядок: git проверяется ПЕРВЫМ, восстановление — после
> Прежде чем доставать файл из undelete или фонда — проверить, нет ли его в репозитории. 24.08.2026: 15 из 20 файлов, «спасённых» из фонда, оказались побайтово тождественны git-версиям, а четыре были ложно объявлены погибшими — их undelete-копии были мусором при целых git-версиях. Обратный порядок стоил лишней работы и двух ложных выводов.
>
> ## Правило приёмки (каждый файл, все признаки, без исключений)
> Файл считается живым, только если:
> - **(а)** frontmatter (`name`/`type`/`description`) согласуется с именем и назначением;
> - **(б)** текст связный от начала до конца и завершается законченным предложением или секцией; обрыв на полуслове — не прошёл (либо переносится с пометкой «хвост утрачен»);
> - **(в)** нулевых байтов — 0;
> - **(г)** переводов строки — больше 0: файла разметки без единого `\n` не бывает. Признак добыт отказом: четыре мёртвых файла (фрагменты Go-бинарника под правильными именами) имели ноль NUL-байтов и прошли бы (в);
> - **(д)** содержимое — валидный UTF-8;
> - **(е)** принадлежность проекту подтверждена, а не предположена по префиксу имени: undelete восстанавливает диск целиком — память всех проектов сразу; 24.08.2026 семь файлов «своего» набора оказались памятью чужого проекта, и слепой перенос по префиксам влил бы в правила проекта чужие. Сработавший различитель — `originSessionId` во frontmatter: файлы одной сессии принадлежат одному проекту.
>
> Не прошёл любой признак — файл не переносится, имя заносится в отчёт как «погиб». Одинаковый размер копий ≠ одинаковое содержимое; копии под суффиксами проверяются все — живая копия бывает при мусорной основной (прецедент 24.08: `project_backlog_cpe_locate` — основная 28 NUL, копии `_1` и `_4` живы).
>
> ## Проверку предъявить отказу
> Каждая команда приёмки перед употреблением показана ловящей то, что обязана поймать (`attach-recurring-step-to-mandatory-action.md`, п. 3а): 24.08.2026 `grep -c $'\x00'` в bash оказался пустым шаблоном и считал строки — проверка (в) была зелёной на любом файле.
>
> ## Артефакт
> Таблица приёмки в отчёте восстановления: `имя → источник (полный путь) → байт → вердикт (жив/погиб + чем доказано)`; плюс контрольный прогон признаков (в)–(д) по всем перенесённым.
>
> ## Связано
> `attach-recurring-step-to-mandatory-action.md` (п. 3а — подсадка) · `validate-recovery-before-destructive.md` (проверка восстановимости ДО разрушения; этот стандарт — про приёмку ПОСЛЕ) · `roles/big7/pe-01.md` (объявление реконструкции, если файл собран не по оригиналу).

**Проверено существованием:** признаки (а)–(в) — задание v2:15 прочитано дословно; (г)–(е) — ответ-файл разделы 4–6; `grep -rniE "undelete|восстановлен... (данн|файл)"` по knowledge/scenarios/playbooks → 0 (существующего дома нет); `validate-recovery-before-destructive.md` существует (`ls knowledge/standards/ | grep -i valid`).
**Регрессы:** +1 файл в обязательный набор; числа случая (15/20, 4, 7, 28 NUL) — из отчёта исполнителя, отсюда не воспроизводимы (граница названа в тексте источника).

### Н3. `knowledge/standards/machine-read-names-in-latin.md` — расширение правила латиницы

**Решение:** новый стандарт — да. Правило-предок `feedback_no_cyrillic_identifiers_in_bash` — проектный файл памяти MDMy, а случай 24.08 показал, что правило шире и имени, и проекта: за сутки нарушено в трёх средах, две из которых (ключи JSON-схем инструментов) бьют любой проект с агентами; цена измерена — одиннадцать агентов положены разом за 22 секунды. Дома в своде нет (`grep -rniE "латиниц|кириллиц|cyrillic|latin"` по своду → только англоязычный текст agent_briefing, не о том). «Проектное — в проекте» не препятствие, а довод: общее правило — в `.profi`, страж `guard-cyrillic-shell-vars.sh` — частность в памяти MDMy.

Полный текст файла:

> # Standard: имена, которые читает машина, — латиницей
>
> **Статус:** обязательный · **Применимость:** имена, которые парсит или валидирует программа: переменные и функции оболочки, ключи JSON/YAML-схем (включая схемы инструментов агентов), идентификаторы конфигов, имена в коде. НЕ применяется к текстам для человека: проза документов, комментарии, сообщения коммитов, содержимое памяти — там кириллица законна и этим правилом не трогается.
> **Источник:** проектное правило MDMy `feedback_no_cyrillic_identifiers_in_bash`, оказавшееся 24.08.2026 шире своего имени: за сутки нарушено в трёх средах (`ответ-MDMy-2026-08-24-v2.md`, раздел 11). Заведён при ревизии свода 24.08.2026; формулировка расширения — исполнителя MDMy.
>
> ## Правило
> Имя, которое будет читать машина, записывается латиницей (`[a-zA-Z0-9_.-]`). Не вкусовщина, а устройство слоёв: валидаторы имён отказывают молча и жёстко — слой инструментов агентов требует `^[a-zA-Z0-9_.-]{1,64}$`, и один кириллический ключ схемы 24.08.2026 положил одиннадцать агентов разом за 22 секунды; оболочка ловится стражем только там, где страж заведён. Перечень сред — открытый: правило действует в любой позиции, которую читает парсер, а не человек.
>
> ## Граница и ловушка на стыке
> Имена файлов для человека (журналы, разборы, файлы памяти) — на границе: их читает и человек, и иногда фильтр. Действующая практика (журналы разборов — кириллицей) этим стандартом не отменяется; но при машинной обработке листингов таких имён git экранирует не-ASCII octal-последовательностями, и фильтр по кириллическому образцу их не поймает — обязателен `git -c core.quotePath=false` (случай: 24.08.2026 счёт правленых файлов свода без этого флага пропускал кириллические имена сквозь фильтр-исключение).
>
> ## Артефакт
> Где заведён страж/линт — его отказ (образец: `guard-cyrillic-shell-vars.sh` в проекте-источнике). Где стража нет — признаком служит сам отказ валидатора; при ревью схем и скриптов — `grep -P '[а-яА-ЯёЁ]'` по идентификаторным позициям. Проверку предъявить отказу при заведении (`attach-recurring-step-to-mandatory-action.md`, п. 3а).
>
> ## Связано
> `safe-wording-avoid-false-block.md` (тоже про то, как форма записи ломает машинный слой — но там классификатор, здесь валидатор).

**Проверено существованием:** ответ-файл раздел 11 прочитан; grep по своду выполнен (команда выше, вывод — один нерелевантный файл); `safe-wording-avoid-false-block.md` — существование по README.md:54 (строка «Безопасные формулировки» со ссылкой), сам файл не открывал (граница; ссылка в «Связано» — по имени из README).
**Регрессы:** +1 файл в набор; риск переусердствовать — снят явной границей «тексты для человека не трогаются» и оговоркой о журнальных именах (решение о них — за владельцем, практика не отменяется).

---

## 5. Переоценка Д1 — «одним ходом или никак»

**Было (условие, PE-01-разбор.md:1549–1563):** переименование «Big 7/Большая N» → «ядро» выполняется одним ходом по всем 72 нормативным местам (15 с перечнем ролей + 57 называющих команду по имени) либо не выполняется; ход не начинается, пока не назван исполнитель и не подтверждено, что 72 места пройдены за одну работу. Статус — «ждёт владельца» (сообщение коммита 68fef96).

**Что изменилось.** Объём «всех входов» перестал быть тем, каким был 19.08:
1. **15 мест с перечнем** — уже сведены правками В5/Д2 24.08: утверждений о числе состава вне INDEX.md — ноль (артефакт применения в PE-01-разбор-2026-08-24.md).
2. **57 внешних мест по имени** жили в памяти MDMy и погибли с ней 22.08; реконструировано 8+1 именных правил, спасён 21 файл — прежних 57 входов больше не существует. Живая память MDMy теперь мала и перечислима.
3. **В своде на 24.08** — счёт моей командой: полный образец
   `grep -roE '\bBig[ _-]?(6|7|9|10)\b|Больш[а-яё]+[ -]?(шестёрк|семёрк|девятк|десятк)[а-яё]*|Больш[а-яё]+ (6|7|9|10)\b' --include="*.md" README.md knowledge/ scenarios/ playbooks/ roles/ templates/`
   → **68 вхождений в 16 файлах**. Множество «нормативное/историческое» зависит от состояния строки, не от формы → поимённый список (rule-expiry п. 5): **1** — дом (INDEX.md:1, остаётся); **12** — датированные записи о прошлом, остаются как опыт (README.md:7,9,10 — 6 вхождений; delegate-search-to-persistent-haiku.md:6,51 — 2; delegate-background-ops-to-cheap-subagents.md:4,50 — 2; deploy-provenance-and-build-integrity.md:4 — 1; transient-artifacts-cleanup.md:23 — 1); **к переименованию — 55 вхождений в 13 файлах**: prod_escalation.md (20), big7-consultation.md (6), deploy_workflow.md (5), workflow_scheme.md (4), delegate-search-to-persistent-haiku.md (4), README.md (3), no-regression.md (3), reuse-before-new-section.md (3), hr-d.md (2), workflow_loop.md (2), transient-artifacts-cleanup.md (1), agent-review-loop-before-finish.md (1), agent-question-rendezvous.md (1). Сумма 55 сверена сложением по файлам. П3 и П4 этого разбора, будучи применёнными, уменьшат счёт (README-история, заголовок кворума) — список пересчитывается той же командой перед ходом.

**Вывод: условие Д1 стало исполнимым.** Один ход = (а) 13 файлов свода за одну работу одного исполнителя + (б) одно поручение исполнителю MDMy: тот же образец по живой памяти проекта **и по `CLAUDE.md` и прочим файлам инструкций проекта** (тем же определением множества, что в исходном Д1), правка тем же ходом; исполнитель возвращает 0 по всему набору (правка PQ-01: память погибла 22.08, но `CLAUDE.md` в репозитории пережил — часть прежних 57 внешних входов жива; из worktree не проверяема — граница). Пути (`roles/big7/`, `big7-consultation.md`) не переименовываются — они адреса, внешние указатели на них не ломаются, что снимает главный риск исходного Д1 («два имени одной команды хуже одного устаревшего»: имя в прозе меняется, адрес остаётся один). **Артефакт хода:** образец выше даёт 0 вне INDEX.md и 12 поимённых исторических строк; исполнитель MDMy возвращает тот же 0 по памяти.

**Предлагаю провести. Запуск — за владельцем** (Д1 записан «ждёт владельца», 68fef96): нужно его «да» и названный исполнитель. До «да» — не начинать (условие Д1 в силе).

---

## 6. Кандидаты на архив (M = 0 в этот заход)

Пакет из пяти заглушек оси ask-or-act — `treat-owner-as-expert.md`, `confirm-understanding-before-acting.md`, `no-confirm-reasonable-nondestructive.md`, `challenge-suboptimal-owner-decisions.md`, `autonomous-self-verify-nondestructive.md` — по порядку «перед удалением» (pe-01.md):

1. **Происхождение:** сведение пяти стандартов в `ask-or-act.md` — решение владельца 24.08.2026, записано в строке 3 каждой заглушки (одна открыта целиком: treat-owner-as-expert.md, 5 строк; остальные — по совпавшим результатам линз 3 и 4).
2. **Что защищают сегодня:** адреса — на эти имена указывают памяти проектов; смысл уже переехал (ask-or-act.md), защищается только разрешимость ссылок.
3. **Куда переезжает смысл:** никуда — он уже в ask-or-act.md; переехать должен адрес (правка указателей в памятях проектов).

**Вердикт:** не архивировать сейчас — условие снятия, записанное в самих заглушках («на ревизии, после правки указателей»), не выполнено: перевод указателей — открытый хвост (PQ-01-разбор-2026-08-24.md:640). Кандидаты на следующую квартальную ревизию; снятие уменьшит K с 62 на 5. Прочие линзы кандидатов на архив не дали; правил неизвестного происхождения, требующих `archive_*`, не найдено (каталога/файлов `archive*` в библиотеке нет — `find . -name "archive*"` → пусто; заводить место — при первом реальном архивировании, по rule-expiry п. 7).

---

## 7. Правки, не меняющие смысла (одной строкой; применяются пакетом после вердикта)

- `knowledge/standards/git-branch-discipline.md:36–37` — схлопнуть двойную пустую строку (след снятия скобки коммитом 84640ad); проверено `cat -A`.
- `knowledge/standards/git-dr-mirror-sync.md:26–27` и `:42–43` — то же, два места; проверено `cat -A`.
- `git-branch-discipline.md:104`, `git-dr-mirror-sync.md:44`, `scenarios/session-start.md:128` — в строках «История правок» к адресам `memory/*` дописать «(память проекта MDMy)»: адреса внешние, проект назван только косвенно через «задание MDMy v2» (находка линзы 2; строки открыты мной).
- `knowledge/guides/android-developer-verification.md:10` — будущее время («с сентября 2026 … можно будет») истекает 01.09.2026: при наступлении проверить факт по официальному источнику и переписать с датой проверки; файл вне обязательного набора, срочности нет (по линзе 5; сам файл не открывал — правка отложенная, применяющий откроет).
- `задание-MDMy-2026-08-24-v2.md`, шаг 3 — датированная журнальная пометка (текст истории не переписывается): «[Пометка PE-01, 24.08.2026: артефакт шага задан как grep-счёт 1, а форма шага даёт 2 — декларация и в `description`, и первой строкой тела; исполнитель держал форму, артефакт не сошёлся — расхождение артефакта, не исполнения (`ответ-MDMy-2026-08-24-v2.md`, раздел 7).]»

---

## 8. Открытые хвосты (записаны, в этот заход не входят)

1. **Поле «Источник» у стандартов.** Норма — `knowledge/README.md:30`, «в любом виде». Машинный признак множества не определяет (принадлежность зависит от содержания, не формы — rule-expiry п. 5): `grep -LiE 'Происхождение|Источник' knowledge/standards/*.md | wc -l` → 20 из 44, но среди них rule-expiry-and-ownership.md, чьё «**Заведён:** 19.08.2026, по итогам сплошного разбора» — то же поле другими словами, и 5 заглушек, которым поле не нужно (нечего править, кроме адреса). Реальный объём — поимённый разбор ~14 файлов (список 20 — командой выше, в разборе есть); отдельный механический заход, срок — до следующей квартальной ревизии, иначе хвост станет бессрочным.
2. **Остаток С7** — интерфейсная ветка (@UX-01/@UI-01/@QA-02) делом не прогнана: на библиотеке непрогоняема, ждёт первой интерфейсной задачи в MDMy (PQ-01-разбор-2026-08-24.md:533).
3. **Перевод указателей памятей проектов** с пяти старых имён оси на ask-or-act.md — условие снятия заглушек (раздел 6).
4. **Поручения исполнителю MDMy:** (а) завести журнал ресурсов по шаблону (П7); (б) прогнать образец Д1 по живой памяти и по `CLAUDE.md`/файлам инструкций проекта (тем же определением множества, что в исходном Д1) после «да» владельца (раздел 5); (в) подтвердить запись мандата «выкладка непрерывна, 11.08» в живой памяти (условие применения П2); (г) вопрос из разбора: 1+11+169=181 при 180 именах — что считалось элементом каждой колонки.

---

## 9. Что вынес на владельца (три пункта)

1. **Подтвердить 8+1 реконструированных правил — до 07.09.2026.** Вопрос задан исполнителем 24.08 (ответ-файл, раздел 12 п. 1); календарь первой ревизии (26.08) этим закрыт, новый срок держит запись в памяти MDMy и — после П10 — строка в самом стандарте. Нет ответа — правила продолжают действовать как реконструкции, а не становятся оригиналами молчанием (pe-01.md:104).
2. **Долг уборки веток MDMy:** не погашен — 23 влитых при 37 локальных на 24.08, срок перенесён на 07.09.2026; падение с 226 до 23 не объяснено записью — репозиторий катастрофу пережил, значит уборка была и не была записана: проверить, разбором она делалась или разом (ответ-файл, раздел 12 п. 3).
3. **Д1 — дать ли ход переименованию:** условие «одним ходом или никак» стало исполнимым (55 вхождений в 13 файлах свода одной работой + малое поручение MDMy по живой памяти; пути-адреса не трогаются). Решение и назначение исполнителя — за владельцем, у которого Д1 и ждало (68fef96).

---

## 10. Что прочитал и что не прочитал

**Прочитал целиком:** roles/big7/pe-01.md; knowledge/standards/rule-expiry-and-ownership.md; README.md; roles/big7/INDEX.md; ответ-MDMy-2026-08-24-v2.md; playbooks/resource_tracking.md; scenarios/post-incident-memory-update.md; templates/memory_index.md; knowledge/standards/treat-owner-as-expert.md; все пять результатов разведки (journal.jsonl, 5 result-записей).
**Прочитал фрагментами (строки названы по месту):** playbooks/prod_escalation.md (:40–76); knowledge/standards/git-branch-discipline.md (:34–60, :100–105); knowledge/standards/git-dr-mirror-sync.md (:24–28, :41–45); playbooks/deploy_workflow.md (:10 по грепу, :113–120); roles/hr-d.md (:27–40); scenarios/big7-consultation.md (:1–21 по грепам, :14–18 напрямую, :33–83 по грепу); scenarios/session-start.md (:44–100, :126–129); knowledge/README.md (:20–40); playbooks/memory_management.md (:38–42, :75–86); playbooks/workflow_loop.md (:5–12 + строки по грепу); templates/resource_log.md (:1–15); PE-01-разбор-2026-08-24.md (:1130–1260); PE-01-разбор.md (:1530–1600); задание-MDMy-2026-08-24-v2.md (строки по грепам: 1, 15, 33–47, 87, 128–132).
**Не читал (выводы на них не строятся либо граница названа в пункте):** файлы ролей инженеров и ядра (кроме INDEX, pe-01); pq-01.md (только git-дата); guides/recipes содержимым (кроме имён; android-guide — по линзе); большинство standards целиком (проверялись грепами по предмету); PQ-01-разбор-*.md (только цитаты через линзы и греп «Агенты:»); git-dr-mirror-sync.md:30 и big7-consultation.md:33–34 — по дословно совпавшим результатам двух независимых линз, помечено в П1/П2; память MDMy и репозиторий MDMy — недоступны из worktree, всё оттуда — по отчёту исполнителя, границы названы в П5, Н1, Н2, разделе 5.

**Число, разошедшееся между источниками, одно:** «19 стандартов без поля Источник» (журнал разбора, определение «нет `## Источник`», база 43) против моих 20 из 44 (грепом по слову в любой позиции, база 44 — после заведения ask-or-act). Не усредняю: определения множества разные, оба приведены; решает поимённый разбор (хвост 1 раздела 8).

---

## 11. Применение (2026-08-24, после вердикта PQ-01 `PQ-01-ревизия-2026-08-24.md`: отклонено 0 · принято 12 · принято с правкой 6)

Все шесть правок PQ-01 внесены дословно; условие к П2 заменено переходной датированной скобкой по указанию HR-D от 24.08 (записи мандата 11.08 в живой памяти MDMy нет — grep HR-D по ветке docs/memory-sync → 0, поручение исполнителю отправлено). Коммит — HR-D после сверки; строка README и этот файл уходят одним коммитом (условие PQ-01, раздел 12 его разбора).

| Пункт | Редакция | Файл : место применения | Статус |
|---|---|---|---|
| П1 | моя | `roles/big7/pe-01.md:3` (дата шапки 2026-08-24) | применено |
| П2 | моя + указание HR-D: переходная скобка вместо условия «сначала запись» | `roles/big7/pe-01.md:135–137` (датированный случай + скобка «(Переходно: … мандат действует в этой скобке дословно)»; снятие — коммитом с файл:строка записи в памяти MDMy) | применено переходно |
| П3 | моя + PQ-01: «обязательная» возвращена в строку Resource tracking | `README.md:5–12` («История состава и ролей», хронология, SEC-02 — «дата не записана»); `README.md:53` (строка Resource tracking в «Ключевых принципах») | применено |
| П4 | моя + PQ-01: третий вход | `playbooks/prod_escalation.md:47–49` («Кворум эскалации» — ссылка); `:68` (скобка → «(кворум — `roles/big7/INDEX.md`, «Минимальный кворум»)»); `roles/big7/INDEX.md:43` (SEC-02 обязателен при security-surface; QA-01/AR-01 по предмету). Артефакт: `grep -rn "PM-01 + DO-01 + SEC-01" --include="*.md" playbooks/ scenarios/ templates/ roles/hr-d.md` → 0 | применено |
| П5 | моя | `knowledge/standards/git-branch-discipline.md:55` (исход первой ревизии: 23/37, срок 07.09.2026) | применено |
| П6 | моя + PQ-01: носитель — файл с датой (разбор, сохранённый отчёт, запись памяти по итогам сессии); «чат местом записи не является» | `scenarios/big7-consultation.md:16` | применено |
| П7 | моя + PQ-01: «Инцидент без выката — тот же порядок» | `playbooks/resource_tracking.md:11` (дом); `playbooks/deploy_workflow.md:117`, `roles/hr-d.md:36`, `scenarios/post-incident-memory-update.md:12` (ссылки на дом) | применено |
| П8 | моя | `templates/memory_index.md:21–26` (раздел «RESUME / активные программы») | применено |
| П9 | моя | `scenarios/post-incident-memory-update.md:34` (квартал по календарю ревизии; артефакт «Уборка памяти: <дата>») | применено |
| П10 | моя | `knowledge/standards/rule-expiry-and-ownership.md:40` (исход первой ревизии; вопрос о реконструкциях жив, срок 07.09.2026) | применено |
| Н1 | моя (черновик + 4 дополнения исполнителя) | `knowledge/standards/attach-recurring-step-to-mandatory-action.md` — создан | применено |
| Н2 | моя + PQ-01: git-оговорка — (е) выполняется репозиторием, остальные признаки применяются и к git-версии | `knowledge/standards/recovered-data-acceptance.md` — создан | применено |
| Н3 | моя | `knowledge/standards/machine-read-names-in-latin.md` — создан | применено |
| Д1 | моя + PQ-01: поручение (б) расширено на `CLAUDE.md`/файлы инструкций проекта | текст поручения — этот файл, раздел 5 и раздел 8 п. 4(б); свод не переименовывался | отложено — ждёт «да» владельца (68fef96) |
| Строка ревизии | моя | `README.md:60–64` («Ревизии свода»: 2026-08-24 · K=62 · N=4 · M=0 · разбор: ревизия-свода-2026-08-24.md) | применено |
| Мелочи (5) | мои | двойные пустые: `git-branch-discipline.md:36`, `git-dr-mirror-sync.md:26`, `:42` — схлопнуты; «(память проекта MDMy)» — `git-branch-discipline.md`, `git-dr-mirror-sync.md`, `session-start.md` (строки «История правок» шага 8 MDMy); пометка PE-01 — `задание-MDMy-2026-08-24-v2.md`, под артефактом шага 3 | применено; android-guide — отложенная (при наступлении 01.09) |
| Мелкие PQ-01 (6) | PQ-01 дословно | этот файл: строки П1/П3/N-раздела/Н3 (4 замены); `scenarios/session-start.md:87` — «65 файлов» тем же ходом, что заведение Н1–Н3 | применено |

**Артефакты применения (все выполнены, команды и числа):** `ls README.md knowledge/standards/*.md scenarios/*.md playbooks/*.md roles/hr-d.md roles/big7/INDEX.md | wc -l` → **65** (сошлось с обновлённым session-start.md:87); `grep -rn "PM-01 + DO-01 + SEC-01" --include="*.md" playbooks/ scenarios/ templates/ roles/hr-d.md` → **0**; «История правок» со ссылкой на этот файл — в 13 правленых файлах свода (несмысловые правки git-dr-mirror-sync.md строки не требуют — no-regression.md:39).

**Регрессы применения:** K в строке ревизии остаётся 62 — счёт на момент проведения ревизии; текущий объём набора (65) живёт в session-start.md:87 с датой. Скобка П2 держит мандат 11.08 дословно до записи в памяти MDMy — указание владельца не ослаблено ни на день.
